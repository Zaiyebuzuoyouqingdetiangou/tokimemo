# 施工方案：输入预算 / 快照与 240 拆开 / 审计 1–12

日期：2026-09-21  
状态：预算与审计项已拍板；240 滚动淘汰已拍板（H1 I2 J1 K2 L1，冷归档 100）；日期场景勾选建档见 §7。  
已同意：默认输入 6 万；旧值恰好 32000 升到 60000；可调上限 20 万；字符顶跟 token 走；信封去重；有手选即关 dry-run；死 UI 副本 re-export；本轮不拆新库；不改 Prompt 模板正文；快照投影 + blocked 出口；审计 1–12。

---

## 0. 我的判断（对应你问的第 1 点）

**可以，而且设置项其实已经在。** r84.31 已经把「输入预算」做成 8000–128000 的用户设置，只是默认仍是 32000，界面写「留空为默认 32000」，和「最大输出」（默认也是 60000）并排，所以上次会调错项。

把默认改成 **60000** 能直接放过这次房间现场（34282 tokens）。这是对的 P0。

单靠涨默认不够，建议一起做下面几条，否则下一张更大的多人卡还会炸：

1. **字符硬顶 96000 要跟着松。** 现在 token 过了还可能被 96k 字符拦。中文 6 万 token 很容易接近或超过 9.6 万字符。建议字符顶跟用户 token 预算走，而不是另写死一个 96k。
2. **旧用户已经存成 32000 的，升级后仍会失败。** 需要拍板：是否把「仍等于旧默认 32000」的视为未定制、升到 60000。
3. **仍要做信封去重（审计 5、6）。** 否则 6 万只是把炸弹往后推。
4. **拦截必须带数字和构成。** 否则用户还是觉得「没多少字」。
5. 用户可调上限改为 **8000–200000**，并保留费用提示。

建档请求继续走 r84.23（未知模型不拿 32k 冒充上下文）；本轮改的是派生任务的 `assertPromptBudget` 默认值 + 字符顶 + 文案。

---

## 1. 输入预算（P0）

### 改什么

| 项 | 现在 | 改为 |
|---|---|---|
| `MAX_GENERATION_INPUT_TOKENS` / `DEFAULT_SETTINGS.inputBudgetTokens` | 32000 | **60000** |
| 设置占位与说明 | 「默认 32000」 | 「默认 60000 tokens；与最大输出无关」 |
| 可调范围 | 8000–128000 | **8000–200000** |
| `MAX_GENERATION_INPUT_CHARS` | 写死 96000 | `min(600000, 本次预算tokens × 3)` |
| 非法/空值回落 | 32000 | 60000 |
| 已持久化为恰好 32000 | 保持 32000 | **视为未定制，读设置时升到 60000 并写回** |

`normalizeInputBudgetTokens` 的回落常量跟着默认走，避免两套数。

设置区文案改成两行并排可区分：

- **最大输出（max_tokens）**：模型最多写多长，默认 60000，不拦输入。
- **输入预算（tokens）**：发送前本地保险，默认 60000，范围 8000–200000，越大越贵。

失败时（见 §3 第 1、8 条）必须写出：本次字符、本次 token、当前预算、差多少、去哪调。

### 不改

- 不取消发送前拦截（避免费钱）。
- 不改 Prompt 模板正文。
- 不把建档的「未知模型不拦」倒退回去。
- 不自动重试、不自动换模型。

---

## 2. 240 条 vs 续写快照：拆开方案

两道闸继续都在，但**不再用同一套「储存上限」说话，也不再让满 240 条卡死相簿/ADV/房间。**

```
正式档案 memoryBank（磁盘）     最多 240 条 Mxxx     → 只约束建档入档
派生任务 recovery 快照         最多 120 万字符兜底   → 只存投影，不存整卡整库
Prompt 实际使用                memoryPayload 64×700  → 本来就不用 240 条全文
```

### 2.1 快照改投影（修第 4、10 条的根）

`captureGenerationContent`：

- **当前角色不再 `structuredClone` 整对象。** 与其它卡同一口径：name / avatar / description·personality·scenario·first_mes·mes_example 各有上限。
- `cardFields` 继续用已有 `getCharacterCardFields` 快照（字段本身已 5k 截断），不要再附带 `character_book`、extensions。
- `memoryBank` **默认**写入 Prompt/校验投影：id、title、date、anchors、participants、messageRange、sourceKind、externalSource、summary≤700；库级字段（revision、chatId、archiveName 等）原样。去掉 importProgress / coveredRanges（已在删）以及任何原文级肥字段。
- r84.33 的「超限才蒸馏」改成「一开始就是投影」。预算内不再出现「整库原文」。
- 120 万字符上限**数字不动**，只作投影后的兜底。投影后仍超限才报 `RMT_RECOVERY_SNAPSHOT_TOO_LARGE`，文案带构成数字。

续写校验继续只看 id/title/anchors/库级字段，与投影兼容。活档案仍以正式 240 条为准；快照 digest 对不上走现有身份失败，不取消换卡/换聊天保护。

### 2.2 快照仍超限时的出口（修第 10 条）

现在：挂载 contentSnapshot 失败 → 不建 journal → 没有导出/放弃卡。

改为：

1. 先投影再挂载（绝大多数满档应过）。
2. 若仍超限：写入一份 **blocked 摘要 journal**（无巨型 snapshot、`canContinue=false`），恢复区只给「说明 + 放弃」，不给继续生成。不请求模型。
3. 成功路径不变：投影快照 + 可续写。

### 2.3 240 条闸只说建档的话（修第 9、11 条）

| 闸 | 码/标题 | 用户句 |
|---|---|---|
| 热位 240 且仍有未锁定 | 滚动入档 | 「已将最早的未锁定记忆移入冷归档，腾出热位给新记忆。锁上的条目未动。」 |
| 240 条全锁 / 无可淘汰 | 容量边界 | 「热位已满且均为锁定。新结果在待入档，可导出。已有相簿/ADV/房间仍可生成。」 |
| 快照兜底 | `RMT_RECOVERY_SNAPSHOT_TOO_LARGE` | 「续写资料包 x / 120 万字符，未发请求。」+ 构成 |
| 超大旧草稿 | `RMT_RECOVERY_OVERSIZED` | 「这份未完成草稿不能继续生成，只能导出或放弃。」 |
| 输入预算 | `RMT_INPUT_BUDGET` | 「本次输入 a 字符 / b tokens，预算 c。」 |
| 12MB 缓存 | 缓存码 | 不说「记忆上限」 |

满档时：

- 「重试未完成部分」若只是为了再产出 Mxxx：**禁用或改成「导出待入档 / 查看未完成来源」**，不要再弹出「会使用文本生成额度」。
- 93 未完成 + 3 待入档分开写：未完成 = 没跑完的来源批次；待入档 = 已经生成但挤不进 240。

热窗口仍是 **240 条**。超过时不再只堵死建档，改为滚动：新记忆入档时，从最早的未锁定记忆里腾位。锁定由用户手动打。细节见 §2.4。

### 2.4 热 240 + 锁定（已拍板：H1 I2 J1 K2 L1，冷 100）

现有增量合同写过「不删除已有 Mxxx」。滚动淘汰会改这条，所以不能做成静默物理删除。

已定：

- 新记忆要进正式列表且热位已满时，按剧情日期弹出**未锁定**记忆，当场换入新条。一批最多连续顶 20 条（与建档批次对齐）。
- 被顶掉的进「冷归档」：仍可查看，相簿/ADV 引用的旧 id 仍能取证；不占 240 热位、不默认进每次 Prompt。
- 冷归档最多 **100** 条。再满：直接删最早一条冷存，不另做导出确认。
- 用户锁上的记忆永不进入热位淘汰、也不进冷归档删除队列。
- 240 条全锁：新结果待入档可导出，不收费强塞、不顶锁定。
- 新记忆继续用新 id（M241…），不复用被冷藏/删除的编号。
- 「最早」按剧情日期（`comparableStoryDate`）。**未标注单独一组，当作最晚**（先顶有日期的早条）。日期以手填为主（公历或历年/纪年文字都行）；另有「用 AI 估日期」按钮，预览后才写入。手填和 AI 写入同一套日期字段，改完立刻重排。
- 锁定入口：档案记忆列表每条一个锁，默认不锁；内容页/相簿也能锁（同一 `locked` 字段）。
- 允许锁满 240。锁满后无法再滚动入档。

---

## 3. 审计 1–12 的具体改法

### 1. 错误码不再吞数字

`safeErrorSummary`：对 `RMT_INPUT_BUDGET`、`RMT_RECOVERY_SNAPSHOT_TOO_LARGE`、`RMT_RECOVERY_OVERSIZED`、容量边界，允许使用 `safeUserMessage`（已含计数），或像建档预算那样读附加字段。白名单短句改成前缀，不能整句替换掉数字。

`assertPromptBudget` 把 `{ chars, tokens, budgetTokens, charCap }` 挂到 `error.inputBudget`。快照失败挂 `{ snapshotChars, budgetChars, cardChars, memoryChars }`。

### 2. 双重 toast

- `generateMode` 内：当前页只 `showInlineError`；后台/离页才 `toastr`。不要又横幅又 toast。
- `ui/overlay.js` 里 `generateMode(...).catch(toastr)` **删掉**，避免与内部再弹一次。其它 catch 仅在 generateMode 没有自己报过时再报（可用 `error.notified`）。
- 蓝条「5/19」保持 info 一次。

### 3. `ui/` vs `modes/` 双份

运行入口已经是 `ui/overlay`、`ui/settingsPanel`、`ui/archivePortal`。`modes/` 下大量 *View/overlay/settingsPanel 是历史副本。

本轮不做四十个文件大合并。做法：

1. 从 `heartbeatMemories.js` 画一张可达图，列出 **生产图用不到的 UI 副本**。
2. 确认无生产 import 的副本改成 `export * from '../ui/xxx.js'`（或对等具名 re-export），只留一份实现。
3. `modes/room.js`、`album.js`、`advEvent.js` 等**领域逻辑不动位置**。
4. 以后只改 `ui/` 那一份。冻结哈希测试多失败几项，按现有「精确逆块」登记，不靠改测试凑绿。

若某副本仍被测试当实现 import，只改测试 import 到 `ui/`，不双修。

### 4. 当前角色整卡进快照

见 §2.1。这是满档派生全灭的主因候选。

### 5. 三路设定重复

房间/出行/终端等受控信封：

1. 用户有手选设定或多人 `sourceRefs` 时：**不再 dry-run 激活整本世界书**（或仅补「手选 uid 集合之外」且去重；默认建议直接关，见拍板）。
2. 手选设定与人物 `sourceRefs` 按 `(world, uid)` 去重，只送一份正文。
3. 多人卡信封 `CHARACTER_CARD_JSON` 改为卡名 + **所选人物**短资料，不把全员长描述再塞一遍。

不改 Prompt 字符串模板，只改信封拼装。

### 6. dry-run 扫描词过肥

`buildControlledContextEnvelope`：

- 扫描词 = 角色名 + 该模式短关键词（已有 `generationWorldInfoScanTerms`）。
- **不要**再把 64 条记忆的 1200 字摘要当 scan terms。
- 无手选时才 dry-run；有手选则跳过（与第 5 条同一拍板）。

激活结果仍截到现有世界书袋（12k / 合计 16k），避免关扫描后反而无上限。

### 7. 32k token vs 96k 字符两套死线

与 §1 合并：默认 6 万 token；字符顶跟预算走。派生任务不再出现「字符过了、token 没过却只说安全预算」或反过来。

### 8. 最大输出 vs 输入预算

设置标签、说明、失败下一步都写「打开设置 → 输入预算」。不要再暗示调最大输出。

### 9. 七道闸同一张嘴

统一错误前缀分类，见 §2.3。诊断 JSON 增加只读计数：`memoryCount`/`memoryCap`、`snapshotChars`/`snapshotBudget`、`inputChars`/`inputTokens`/`inputBudgetTokens`、`cacheSourceChars`。不写正文。

### 10. 快照失败也要有可见出口

见 §2.2。满档点相簿不应只剩 toast。

### 11. 待入档 3 条 vs 93 未完成

建档面板两行分开；满 240 时主按钮不是「重试未完成部分（会收费）」；待入档保持导出。旧 Mxxx 只读。

### 12. 60 楼缓存就 80 万源字符

本轮不做新数据库。做三件能立刻瘦的：

1. 恢复 journal 不再嵌整卡整库（§2.1）——这是 journal 肥的主因。
2. 诊断对 `sourceChars` / 压缩尺寸加档位说明（已有雏形，补上与 240/快照的区分）。
3. 写缓存时不要把 `contentSnapshot` 或完整 `memoryBank` 再拷进每个 mode session（若当前 session 里有整库引用则改为 revision + digest）。

把各模式正文拆出 `chatMetadata` 是下一轮，本轮不承诺。

---

## 4. 实施顺序（建议一轮做完，但提交仍可按文件组看）

1. 文案与 toast（1、2、8、9 的展示层）——可单独验证双弹消失、数字出现。
2. 默认 6 万 + 字符顶公式 + 设置文案（§1、7）。
3. 信封去重与扫描词（5、6）。
4. 快照投影 + blocked 出口（4、10）；热 240 滚动 + 锁定（§2.4，待拍板后做）。
5. 诊断字段（9、12）+ 缓存不重复嵌套。
6. 死副本 re-export（3）。这项最容易引爆冻结测试，放最后，必要时单独一刀登记闸门。

Prompt 34 函数正文本轮零改。身份/CAS/删除栅栏/不自动重试保持。

---

## 5. 验收（用日志，不用「语气更温和」）

房间同类现场（约 4.7 万字符 / 3.4 万 tokens）：

- 默认 6 万下应能发出请求，或失败时 toast 含 `47393` / `34282` / `60000` 和构成，且只弹一次。
- 勾选手选后 dry-run 不再叠一份（若拍板「有手选即关」）。

满 240 条 / 1850 楼：

- 点相簿/ADV：`RMT_RECOVERY_SNAPSHOT_TOO_LARGE` 不应再出现在投影之后的正常满档上；`providerRequests` 可以 >0。
- 若人为造一份投影后仍超限的卡：恢复区有放弃、无继续、0 请求。

建档满档：

- 不出现「重试未完成会收费」主路径；待入档可导出；已有派生内容可打开。

回归：换角色/换聊天续写仍阻断；旧草稿身份保护仍在。

---

## 6. 已拍板（240 锁定）

| 项 | 选择 | 含义 |
|---|---|---|
| H | 1 | 被顶掉进冷归档：可查看、可当相簿/ADV 证据，不占热位 |
| I | 2 | 「最早」按剧情日期；未标注单独一组当最晚；手填（公历或历年文字）与「AI 估日期」按钮兼容，改完重排 |
| J | 1 | 一批新记忆可连续顶最多 20 条最早未锁定 |
| K | 2 | 档案列表 + 内容页/相簿都能锁 |
| L | 1 | 可以锁满 240 |
| M | 100 | 冷归档上限 100 条；再满直接删最早冷存，不另导出 |

日期手填与 AI 预填共用同一字段、同一排序，改完立刻重排。能解析的写法：

- 公历：`YYYY/MM/DD`、`YYYY-M-D`、`YYYY年M月D日`
- 历年/纪年文字：`庆历四年二月初五日`、`贞观三年二月` 这类（跟现有 `comparableStoryDate` 纪年口径；缺日则精度降一档，仍可同历年内排序）
- 柏宝书带时分的 `YYYY/M/D HH:mm` 展示保留，排序只取年月日

解析不出的仍留未标注组，不当作已排序。扫描**不会**自动请求 AI。

---

## 7. 按时间场景勾选建档（已拍板）

挂在现有「记忆来源」上，不另开一套账本。生成目标仍是正式档案 **Mxxx**（整体逻辑 §6 的通路 2+3），不是相簿/房间。

### 7.1 一条是什么

**一段「时间」场景 = 1 条**，不是整本世界书、也不是一整天合并。

| 来源 | 怎么切场景 | 日期从哪来 |
|---|---|---|
| 世界书（小洛那种） | 正文里每个 `- 时间: …` 块；折叠看该块剧情 | 标签原文；手填公历/历年，或点 AI 估日期 |
| 柏宝书 | `getHistory().nodes[]` 一个 node 一条 | `timeStart` / `timeEnd` / `timeLabel`（现适配器没映射，要补） |
| 千千结 | 一楼摘要一条 | **不做日历**，只按 `messageIndex` 从早到晚 |

世界书后面条目常把前面场景再写一遍（小洛 uid5 覆盖 1–4）：**全展示、默认都可勾，去重交给用户。**

### 7.2 世界书从哪读

只读，不改绑定、不开关灯。

- 当前角色卡主世界书 + 附加世界书
- 当前已启用的全局 / 聊天世界书
- 酒馆已有世界书列表里再选（`getWorldbookNames` 同类；本插件走 ST `world_names` / `loadWorldInfo`）
- 单选或多选都可以，不限制本数

现有按钮「选择记忆相关世界书」继续当入口，下面加场景列表，不要第三套导入。

### 7.3 界面两种模式（可切换）

1. **勾选**：按日期（或未标注组）列出场景；点开折叠看剧情；勾选；条数 **1–20** 自己选。
2. **从早到晚**：按排序一次送 **20** 条；下一批再 20。

未标注组：先放在列表最晚；展示原文时间标签和剧情。日期以手填为主：公历数字或历年/纪年文字都可以。另加 **「用 AI 估日期」** 按钮，只对当前未标注（或用户勾选的）场景请求一次，先预览再写入。手填和 AI 写的是同一字段，互不覆盖对方已确认的值；任一侧改完立刻按 `comparableStoryDate` 重排。估不出或用户关掉预览的，仍留未标注组。扫描本身不收费。

### 7.4 解析能力（不要假装都能排成日历）

- **柏宝书最好。** node 自带起止时间。现代 `1988/9/29 21:30`，古风纪年也能定位。必须改 `readBaiBaiBookCurrentChat`：现在只认 `date`/`timestamp`/`createdAt`，柏宝书字段是 `timeStart`/`timeEnd`。
- **世界书能抽标签，排不成公历。** `雷雨夜`、`第二天清晨（破庙内）` 对 `comparableStoryDate` 都是 null。列表先按条目 `displayIndex` + 条内场景顺序；手填历年/公历或 AI 确认写入后，才按日期排。
- **千千结公开 `getSnapshot().memory.floors` 没有 chronology。** 内部有，没暴露。本轮按楼层先后，不猜摘要里的日期。

### 7.5 和容量闸的衔接

勾选/从早到晚产出的新 Mxxx 走现有建档批次（每批最多 20 条进热位）。热满则按 §2.4 连续顶未锁定；锁上的不动；冷满 100 删最早冷存。

### 7.6 不改

- 不把自动激活世界书（通路 4）升级成 Mxxx。
- 不改 Prompt 模板正文。
- 不写回角色卡 / 世界书 / 聊天楼层。
- 千千结、柏宝书仍只走公开只读 API，不读它们的内部 store。

回「开工」后按：容量闸 §2.4 → 三路日期适配（含历年手填）→ 场景勾选 UI → 可选「AI 估日期」按钮 → 与现有建档批次接通。
