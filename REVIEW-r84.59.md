# Hearttrace r84.59 复核记录

版本：0.99.14 / 0.99.14-r84.59-reviewed。基线：af1e18c9abcfda1b946677ca32e8f311b51f9499（r84.58）。

## 本轮交付

- 镜译语音设置位于「设置 → 镜译 · 语音设置」。往返页面复用同一音色控件，不复制镜译密钥。内容页仍由「…」菜单朗读。
- 「想和现在的 TA 聊天吗？」位于「内容 → 互动」最下面，内容目录不再展示镜译设置卡片。
- 「内容 → 生活 → 手帐」为独立模块；「两个人的日历」保留原入口、原数据。
- 手帐支持选择模块整页收录、手动勾选已有条目、跨模块挑选，也可收录当前聊天最后一条角色回复。HEART 的基础语言、日常一格、萤火虫与后日谈分别列为来源。
- 预览后新增手帐页，保留正文、换行、说话人、CG 图片引用与随信代码插画；不改原档案，不调用文本或图片生成。
- 本机 IndexedDB 独立保存，按角色与聊天隔离；支持 JSON 备份导出、追加导入。同 ID 不同内容拒绝覆盖；坏数据、写入失败不会清空旧页。
- 修复复核发现的预览与已存小画 SVG 标识重复，以及保存中编辑标题可能未保存的问题。保存期间临时禁用编辑，结束后恢复。

## 新鲜验证

- `node --experimental-vm-modules --test tools/test-*.mjs`：202/202 通过，0 失败、取消、跳过。
- `node tools/check-r8437.mjs`：通过。
- `node tools/build-runtime-bundle.mjs`：174 模块，source SHA-256 `f414e007140b77db90991ee173094389721000d20dd12fca52034d678759a813`。
- 全新进程导入生产 bundle：通过。
- 248 个 JS/MJS 文件语法检查：全部通过。
- 本地浏览器使用 `tools/repair-r8459-interactive.html`，实际操作生产模块与真实 IndexedDB：6 封信整页保存、只选 2 封另存、刷新重开后两页及 8 幅插画保留；备份导入后已有两页仍保留。
- 导入原文中的空格、换行、字面 `<script>` 原样保留，手帐 DOM 中无嵌入脚本。末条回复整页预览内容一致。
- 320 像素窄屏：正文容器 clientWidth/scrollWidth 为 287/287，手帐为 263/263，两个已存页面均为 237/237；镜译设置往返后仍只有一张卡和一个挂载面板，设置页宽度 287/287。
- 独立复核覆盖保存、来源选择、聊天隔离、转义和设置控件生命周期；相关发现已修复。

## 验证边界与保存说明

本轮浏览器验收在隔离样本运行，没有操作真实酒馆存档，也没有调用付费服务；真实手机麦克风、镜译在线服务仍未进行本轮端到端通话测试。

手帐保存在当前浏览器设备，不会自动同步到其他设备。请用手帐内的导出备份迁移。CG 备份保存的是图片引用，不内嵌图片文件；更换服务器后需保持图片地址可访问。代码绘制的随信插画数据包含在备份中。回退版本不会删除独立手帐库。

本轮未更改原有生成配额、每日收信规则、API 输出上限或提示词合同。仅手帐导入做数据格式与图片地址安全校验，保存中临时锁定编辑防止丢失改动。
