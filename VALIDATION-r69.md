# 心迹回廊 0.8.64 / r69.0 — D款静态悬浮球

## 本轮范围

唯一运行基线：本次对话已交付的 `Hearttrace-0.8.63-r68.0-INSTALL.zip`。
SHA-256：`ce5490074275d686152dc3c81c02ef72f1a3c739b11321cd774c456c411a88e7`。
对应 r68 SOURCE 的安装文件逐字节核对一致；只在独立副本修改。r68及用户原包均保留。
本轮已完整读取上传的项目CURRENT以及仓库同名CURRENT。两者版本记录落后于本对话；按用户后续明确需求和本对话r68实物核对，不以旧版需求替换当前代码，也没有修改远端CURRENT。

## 实现

- 从用户选定概念图的大号D圆球裁出透明PNG；不是重新生成另一款。安装资源144×144，8,351字节，默认显示48×48 CSS像素。
- 完全静态：无呼吸、旋转、闪烁、悬停动画、自动吸附动画或红点轮询。
- 手指/鼠标可自由拖动；达到6px位移判为拖动，松开不误触打开。位置有界，松开才写小偏好；不逐帧写存储。
- 点击走既有requestArchiveOpen → safeShowArchiveLibrary入口，保留r64以来的上次页面恢复。没有新开一套档案/路由。
- 悬浮球坐标与显示开关在当前设备本站点本地保存；刷新后仍能记住。它不改变原阅读书签仅限网页运行期的行为。
- 插件轻量设置可关闭悬浮球；首页 → 界面主题也可开关并重置位置。档案室打开时隐藏球，关闭后按用户开关恢复。
- 原生按钮键盘激活，方向键移动；窗口旋转/可视区域变化会重新夹限位置。停用/清理会移除球、样式和全部监听器。
- 图片缺失保留静态心形兜底与可访问按钮；不会回退到外部图片或触发模型。

## 未修改

隐藏心跳、模型提示词、提示词生成规则输入、柏宝绘公开调用、图文生成与确认、原图查看、导航书签逻辑、正式档案及派生结构、证据锚点、archiveRevision/CAS、删除栅栏、12MB上限、备份、只读和请求并发/超时规则均未修改。
`src/core` / `src/archive` / `src/generation` / `src/modes` 及导航/原图/提示词编辑器等共69个文件与基线字节一致。

## 已执行验证

- 新悬浮球定向测试：35/35通过，涵盖尺寸、坐标、取消/捕获、单双击、存储、开关/重置、清理与惰性启动静态契约。
- 本轮+bootstrap+r63/r64/r65/r66/r68定向回归：202/202通过。
- 完整可用测试集：596项，564通过、32失败；r68对照561项，529通过、相同32项失败。没有新增失败或跳过。仅将r68版本身份测试的硬编码预期更新到r69；未改其他历史失败断言。
- 全部JS/MJS语法：137/137通过，包含根级悬浮入口模块、runtime、测试与工具。
- 悬浮球离线Chromium检查：38/38通过。实际PNG、生产按钮/拖动处理、CDP触摸、鼠标、键盘、模拟重载、viewport变化、宿主样式干扰、实际runtime懒打开及既有书签恢复都覆盖。
- 既有图片设置/两阶段生成离线Chromium回归：43/43通过。合计81/81。
- runtime保持93模块，最终源码连续两次确定性重建字节一致。
- runtime SHA-256：`579e1d0daeebf19232686e7ce0c54e93ab2c1c47341405b7eab5e804d7caaf3b`。
- 打包后的文件清单、CRC、SHA与fresh-install复核附于本轮交付报告。

## 浏览器与正式验收边界

本环境Chromium策略阻断HTTP页面导航，因此浏览器在about:blank中注入生产函数体，模块加载/图片资源输送/localStorage使用离线适配，不改变按钮业务函数。未修改浏览器策略。生产触摸事件由Chromium CDP驱动，不是iPhone真机触摸。

真实iPhone/TT、原生WebView、实际酒馆主题、站点localStorage跨重启、外部网络/模型/生图服务未执行。需要真机确认拖动手感、软键盘/安全区与其他插件悬浮球叠放。
完整测试仍有32项基线失败，尚未逐项证明均无关；本版只作静态悬浮球测试候选，不作正式全量放行。

## 安全与远端

本地差异安全自查未发现本轮新增且未处理的High/Medium。独立Codex Security回执未取得，不宣称独立审查通过。
GitHub仅只读取得CURRENT，未commit、push、建分支/PR/Release，未修改第三方。

## 安装

先备份现有插件目录。将INSTALL里整个tokimemo-main的内容覆盖到原插件目录，再刷新酒馆。
本版新增根级floatingLauncher.js及assets/firefly-heart.png，请勿仅替换manifest/index/bundle而漏掉新文件。
不需要清站点数据，也不要完全重建档案。
