# 0.8.68 / r73.0 — TT 图片修复候选

基线：`Zaiyebuzuoyouqingdetiangou/tokimemo` 的 `71489e69550b5d6f053be07c73d59b5b080123b7`（原标识 0.8.58 / r62）。未提交、推送或发布至 GitHub。

## 变更与验收

| 用户可见行为 | 实现与证据 |
| --- | --- |
| TT 出图后替换抽象 CG | 接收、图片记录、逐条写回和显示共用同宿主地址检查；模拟 HTTPS、HTTP、tauri 三种宿主，柏宝绘返回的已保存路径均能写回并在重新打开后显示；原会话不被原地覆盖，相邻条目保留 |
| 事件画面优先 | 初始可编辑草稿保留 cgDesc/desc，imagePrompt 作为有预算的补充；删除新加的自动角色卡外貌提取；长补充词无法挤掉事件，已成功保存或用户确认的重绘提示沿用原路径 |
| 大图在插件内查看 | 两个原图入口取消新窗口导航，使用现有档案室内查看器；适屏/原尺寸、加载失败、Tab/Escape/cancel、返回、焦点还原和清理均由模拟 DOM 验证 |
| 保持安全和收费边界 | 不读取第三方私有数据，不增加请求或重试；保留角色、聊天、档案版本与写入栅栏；跨宿主、危险协议、凭据、端口伪装、非法保存路径和双斜杠重解析被拒绝 |

## 本地验证结果

- `node --test tests/*.test.mjs`：**16/16 通过，0 失败、0 取消**。这是代码与模拟宿主/DOM 验证，不是真实出图或浏览器布局验证。
- 修改前的路径/提示专项检查 8 项中 6 项失败，修复后全部通过。另补充双斜杠跨宿主重解析负例。
- JS/MJS 语法检查：**100/100 通过**；最终路径安全修改及重建 bundle 再次检查通过。
- runtime bundle：**87 模块**，连续重建字节一致，集成 bundle 可导入并导出 init/destroy。
- source SHA-256：`b722519fe05fc290b0da31f5ce7d187a03fe5a8ee0a8b0fd3ac8d0aa18ce7e9d`
- bundle SHA-256：`be2fb256b9c18eb7e72c69b05c67ccf93d66726795260ca4306520026b35b830`
- manifest/index 版本与缓存标识一致：`0.8.68` / `0.8.68-tt-cg-r73.0`。
- 独立 Codex Security 差异复核：发现的双斜杠重解析 Medium 已修复并独立复验；**无未解决的 High/Medium**。
- 打包使用 `tools/package-release.py`，强制验证单根目录、CRC、每个文件 SHA-256 与全新解压字节一致；最终解压包再次执行包内专项测试。

## 历史全套测试的实际限制

当前基线已经删除原 tests。为保留对照证据，从历史提交 `42e91cdd1a883609bb5108c81de858793ae34d69` 恢复同一组历史测试到临时验证目录，没有将旧生产代码混入候选。

命令：`node --test --test-timeout=5000 --test-reporter=tap tests/*.test.mjs`。单项上限 5 秒，以限制历史测试中的未结束请求。

| 对照 | 总计 | 通过 | 失败 | 取消 |
| --- | ---: | ---: | ---: | ---: |
| 修改前仓库基线 + 历史测试 | 396 | 357 | 36 | 3 |
| 最终候选 + 同组历史测试 + 本轮专项 | 412 | 373 | 36 | 3 |

失败/取消的用例名称集合完全相同，本次无新增失败。历史测试中包含已经不适用的早期版本号、房间宠物和蝴蝶规则等断言，也有超时取消；没有把全部失败自动认定为过时测试，没有修改断言掩盖结果。因此本候选**不能宣称历史全套测试全部通过**，本轮不修复无关模块。

## 安装和实机确认

将 ZIP 内 `tokimemo-main` 的内容覆盖现有扩展目录，manifest.json 保持同层；完整重启 TT 或刷新云酒馆，确认版本为 0.8.68。不要安装第二份插件或清除站点数据。

1. 对一条回忆打开“图片设置”，确认画面草稿包含事件动作与场景，然后由用户决定是否绘图；检查生成后相簿是否自动显示实图，关闭再打开是否保留。
2. 点“查看完整原图”，确认留在插件内；切换原尺寸、滚动长图、返回，原来的阅读位置和内容仍在。
3. 云酒馆执行同样的查看操作；实际生成、保存、后台切聊和档案保留仍需真实 TT/酒馆验证。

未执行 iPhone/TT 真机、真实浏览器视觉布局、真实柏宝绘或付费生图；画面是否遵从人物/动作仍由实际提示与模型决定。升级不会扫描或自动匹配此前只存在于柏宝绘图库、尚未写入相簿的图片，也不会自动重画已有图。
