# 心跳回忆／心迹回廊项目需求与开发规则（CURRENT）

最后整理：2026-09-22（r84.43 后台任务留在顶端，任务中心一项一行）  
适用项目：心跳回忆插件（显示名：心迹回廊）  
现行需求：有任务时切聊先确认并中止当前聊天任务；多人房间同一来源正文只发送一次；预算与裁剪一次提示；同一恢复草稿一套按钮；顶部任务中心跟随主题色，可查看阶段和落盘，并可单项或按当前聊天取消。档案整理完成后内容页都可以勾选，按目录顺序一次生成一项。有未完成草稿时任务图标变红并显示数量。热位 240、冷归档 100、6 万默认输入预算、身份／CAS／删除栅栏保持不变。自动重试仍默认关闭。  
当前候选：`0.97 / 0.97-tt-cg-r84.43-task-strip`。
状态：**0.97 把当前后台任务固定在顶栏下，失败显示在名称旁边；主界面不再重复未完成草稿。任务中心一项一行，未完成在上、串行完成在下，已完成可清空；打开结果会进入对应内容页。反复重试的优化方案只写在说明里，没有改提示词。桌面酒馆、Tauri/TT、同角色双聊天和 19 人房间现场未验收。**

## 0.1 本轮合同：r84.37

- 常规切聊在当前聊天有任务时先确认。确认后中止这些任务并关闭回廊；取消则留在原聊天。程序化切聊无法提前确认，切换后中止原聊天任务并提示一次。已落盘成果保留，不自动重试，不自动切回。
- 多人房间来源按 `(世界书, uid)` 去重。人物提示只带索引；完整来源留在本次冻结证据里供本地校验。超出资料袋的来源整条不送。
- 生成前先算完整输入账本，再决定一次提示。当前页用内联说明，离开页面才用 toast。
- 恢复按钮从错误文本里拆出，按草稿、页面和模式去重。超限草稿没有继续按钮。
- 统一任务中心在 r84.38：顶部入口与切聊确认共用一份运行名单；单项取消不影响同聊天其他任务；全部取消只作用于当前聊天。诊断字段扩展和真机验收仍未做。本文不把静态检查写成真机通过。

历史记录从下方原文章节起，不改写。

> 本轮生产基线为实际 r84.29 候选完整包（manifest `0.8.105`，build `0.8.105-tt-cg-r84.29-monotonic-recovery`，内部单根`tokimemo-main/`），其上一环为实际上传包 `hearttrace-upload.zip`（r84.28 / 0.8.104）。原包均保留。用户TT诊断为邮箱104–115ms内初始化拒绝、零请求；没有该设备旧journal原件，不将合成的缺字段样本冒充已确认现场唯一原因。GitHub只读，不上传或发布。

## 0. 当前任务与交接结论

### 本轮实施：r84.33 建档草稿身份误判修复与满档案快照适配

**背景与授权。**用户真机转报（coco，0.8.108，诊断3 + 截图）：(A) 未更换角色/角色卡/API，点击「继续这份原建档草稿」却报"角色身份或角色卡与原任务不一致"；(B) 档案 240 条（计数上限）时 album×3/adv×1 在约340-412ms、providerRequests=0 处全部以 RMT_RECOVERY_SNAPSHOT_TOO_LARGE 失败；建档面板 1939 来源/1846 已保存/93 未完成自洽，待入档 3 条为计数容量边界既有设计，非异常膨胀。用户授权：不放宽既有上限、不删除成果、不误标完成、不自动重复调模型；在安全边界内让原任务可继续；明确禁止以提高/删除容量上限掩盖问题，禁止取消角色一致性保护绕过。

**根因（A）。**草稿可见即证明 draftKey（characterId+头像+chatId+操作）四项匹配；续写身份里的 characterKey 内嵌卡片内容指纹（卡文本七字段哈希），草稿创建后任何卡文本编辑（含宿主保存时的不可见规范化）都会漂移指纹并被误判为换角色。档案分组层（groups.js）与延迟提交层（deferredCommitOriginMatchesContext）早已把"同槽位+同头像的卡编辑"视为同人，恢复身份层口径不一致。

**范围（A：同人判定对齐，保护不取消）。**新增 `taskOriginCharacterMatches`（runtimeKey 相等，或槽位 id+非空头像一致即同人）与 `isCurrentTaskRunOrigin`（任务运行门：epoch+同人+同聊天）；`createGenerationRecovery` 续写比对在 characterId+头像+chatId 三者一致时仅容忍纯指纹漂移，journal 保留原身份作证据；真换槽位/头像/聊天照旧硬阻断、报错文案与分类不变。档案导入运行闸（repository assertCurrent）换用运行门；读取/重绘路径（hydrate/导出/丢弃等）保持原严格 `isCurrentTaskOrigin` 不变——卡编辑中途到达的迟到读取仍不得重绑视图（r8426 合同原样保留）。

**根因（B）。**`captureGenerationContent` 把完整档案库嵌进每个新派生任务的恢复快照；240 条合法满档案序列化约超 120 万字符快照预算，派生任务在首个请求前全部失败——计数上限与字符预算在区间顶端结构性冲突。消费审计：恢复校验只读 id/title/anchors（证据锚点）与库级字段；提示词经 memoryPayload（summary 700 字符口径）构建。

**范围（B：快照适配，上限不动）。`GENERATION_RECOVERY_LIMITS` 与 `MAX_MEMORY_ITEMS` 数值一律不变。**快照定稿点新增 `fitGenerationContentSnapshot`：预算内字节级原样（小档案零变化）；超预算时按两档蒸馏 memoryBank（summary 先 1200、不够再 700——即既有 memoryPayload 提示词口径），id/title/anchors/participants/messageStart-End/sourceKind 及全部库级字段字节不变，附全量 digest 与蒸馏标记；蒸馏后仍超预算则由既有 RMT_RECOVERY_SNAPSHOT_TOO_LARGE 如实拒绝（兜底不变）。续写既有草稿仍读取原快照（老草稿内容零变化）；只有"今天必然全败的新任务"走蒸馏路径。

**一项有意的合同更新（如实告知）。**r8428 既有参数化用例"required mismatch is not aliased: characterKey"原断言"任何 characterKey 差异=换角色"；按本轮授权改为新合同：纯指纹漂移可续写且原身份保留，叠加头像变化仍判 character 拒绝。测试名与计数不变，同 r8429 先例。

**验证与限制。**新增10项测试全过（同人卡编辑可续/真换角色卡聊天仍阻断/空头像不容忍/运行门与视图门分层/预算内字节不变/满240条蒸馏后达标且证据字段字节一致/蒸馏库证据校验结果与全量库逐项相同/蒸馏仍超限时兜底原码拒绝/全部上限数值锁定不变）；r8426 迟到读取七场景（含 card）原样通过；全量 tests+tools 共2530项与 r84.32 基线160项失败完全同批（P2F=0、F2P=0、无消失用例，新增10项全过）；34/34 prompt 哈希不变；16/16 功能突变仍被捕获；全文件361项语法通过；确定性重建两次字节一致（bundle 5cde2f3b…）；冻结闸门按既有精确逆块惯例登记（r8433ApprovedDelta 链至 r8432），拒绝未知改动语义不变。真实 iPhone/TT/云酒馆/原生 IndexedDB 未执行，不以模拟结果冒充真机验收。

### 历史已交付：r84.32 超大草稿出口与快照超限分类

**背景与授权。**用户真机转报（旧包0.8.104，诊断4）：相簿×4+ADV×1 全部约400ms内 `RMT_RECOVERY_LIMIT`、零请求、无草稿卡。复核确认两个机制：(1) 角色档案/卡片资料本身超过120万字符续写保护范围时，创建草稿在首个请求前被容量哨兵拦截，草稿从未创建，故无卡可显示——但与"分段写入中途容量满"同码同文案，无法区分；(2) 任何总打包超180万字符的journal（旧版本或历史路径遗留）在读取路径被同一总量复核拒绝，摘要为null，恢复卡不渲染、导出直接抛错、放弃按钮找不到记录——可读出口全部堵死。用户明确授权窄修1+2，快照瘦身（3）不做。

**范围（1：错误分类）。**仅 `createGenerationRecovery` 的 contentSnapshot 挂载点捕获 `RMT_RECOVERY_LIMIT` 并重抛为新码 `RMT_RECOVERY_SNAPSHOT_TOO_LARGE`（文案如实说明：档案资料本身超限、未发送请求、未产生草稿、请精简档案）；新码登记 `taskTrace` 白名单与 `text.js` 文案表。所有容量数值、校验、其他 `RMT_RECOVERY_LIMIT` 触发点一律不动。

**范围（2：超大草稿出口）。**读取路径增加宽松结构解析（仅豁免 journal 总量复核，单段/请求/结构校验全部保留）：`generationRecoverySummary` 对"可读但严格校验失败"的journal返回带 `oversized:true` 的摘要，`canContinue/canRetry` 恒为false；`exportGenerationRecovery` 可导出并在导出包顶层标记 `oversized:true`；`createGenerationRecovery` 续写此类journal在首个请求前抛新码 `RMT_RECOVERY_OVERSIZED`（不冒充身份不匹配）；恢复卡（ui与modes两处视图、pool与legacy两分支）对超大草稿渲染"导出+放弃"、不渲染继续按钮，modes覆盖层补上导出按钮的既有同款处理器。写入路径全部保持严格上限；放弃路径（`discardSavedGeneration`→`saveGenerationRecovery(null)`）不经过journal校验，超大草稿可正常放弃。

**明确不做（3：快照瘦身）。**快照按原约定字节级保留原始输入供续写复验；仅存哈希无法续写；按模式裁剪引用子集属跨模块大改，会触碰Prompt不变性、冻结闸门与续写合同。本案例已由1+2闭环（如实诊断+保证导出/放弃出口）。

**一项有意的行为变化（如实告知）。**缓存规范化此前会把超大legacy journal当作"摘要无效"直接删除；现保留为可读、可导出、可放弃的可见草稿。这符合"不清除用户数据"原则，是本修复的目的之一。

**验证与限制。**新增9项测试全过；r8429单调套件中一条断言"超大journal摘要为null"的旧合同按授权改为新合同（超大→摘要非null+oversized标记+不可续写），测试名与计数不变；全量 tests+tools 共2520项与原160项失败完全同批（PASS→FAIL=0、FAIL→PASS=0、无消失用例，新增9项全过）；34/34 prompt哈希不变；16/16功能突变仍被捕获；全文件358项语法通过；确定性重建两次字节一致；冻结闸门按既有精确逆块惯例登记（r8432ApprovedDelta链至r8431），六个历史闸门失败集与基线逐一相同、拒绝未知改动语义不变。真实TT/手机/真实酒馆/原生IndexedDB未执行，不以模拟结果冒充真机验收。

### 历史已交付：r84.31 输入预算开放为用户设置

**背景与授权。**真机（旧包0.8.104）「他的房间」输入34,282 tokens超过写死的32,000安全预算被发送前拦截（0请求0费用）；用户把「最大输出」调到120,000无效后，明确选择把输入预算开放为用户设置。

**范围。**`MAX_GENERATION_INPUT_TOKENS=32000`保留为默认值；新增设置项「输入预算」，可调范围8,000–128,000，越界或非整数拒绝保存并回填原值，留空/非法持久值回落默认32,000；面板附「输入越大单次请求费用越高」风险提示。拦截文案补可行动建议（当前tokens、有效预算、精简档案/减少世界书/设置中提高预算）。96,000字符上限、token计数、超时、存储、CAS、Prompt构造一律不动；「最大输出」仍是独立设置，与输入预算无关。

**验证与限制。**新增8项测试全过；全量2511项中160项失败与r84.30基线完全同批（PASS→FAIL=0）；34/34 prompt哈希不变；语法全过；双重建字节一致；冻结闸门按既有精确逆块惯例登记（r8431ApprovedDelta链至r8430），闸门语义不变。真机/原生IndexedDB未执行。

### 历史已交付：r84.30 容量出口、建档保存分类与生成准备补码

**范围。**用户复核 r84.29 后明确指出：r84.29 只修了“续写覆盖旧进度”，尚未修“建档、邮箱根本不能生成”。本轮按用户授权窄修三处，不改 Prompt、不放宽校验、不清除用户数据、不调大上限、不删除旧草稿、不取消既有保存-before-request 保护。

**容量满时新成果保全出口（已复现项）。**草稿恢复容量满时，刚收到的新成果不再既进不了页面也进不了保存记录：仅对 `RMT_RECOVERY_LIMIT` 容量拒绝，将本次新收到的原始成果按身份有界暂存在当前页面（每身份8条、16身份、单条不超原分段上限），并在报错文案中指明可经恢复区“导出未提交草稿”一并导出；导出文件以 `unsavedReplies` 原样带出，显式放弃时随草稿一并清除。无安全保存条件仍停止后续请求，不自动重复生成；单条超限等不属实场景不附加该提示。

**建档请求前失败分类（故障确认项）。**建档在首个模型请求前报 `RMT_ARCHIVE_DRAFT_STORAGE` 的路径改为按真实原因分类：序列化超过12MB安全上限报新码 `RMT_ARCHIVE_DRAFT_CAPACITY`（未请求模型、原记录保留，并在建档/简介两处入口做同口径预检）；本机存储配额与CAS写入冲突分别给出可行动文案；保存回执不符单独成码。草稿序列化不再重复存放已校验一致的 `taskInputV1` 快照（旧双份记录读取结果不变）。保存-before-request 保护原样保留。

**生成准备补码（故障确认项）。**邮箱／相簿点击秒退的 `RMT_UNCODED` 实为追踪白名单外的兜底标签。本轮将确认的准备环节故障各自补码：canonical派生缓存损坏且镜像不可用报 `RMT_CACHE_BACKUP_CORRUPT`（不删记录、不带外覆盖，镜像可证明同聊天同revision时才以其为提交起点，且不复活仅存于canonical的草稿/版本/名册）；CAS前接管预检报 `RMT_CACHE_CAS_CONFLICT`；提交载荷超限先按既有存根策略驱逐终态草稿journal负载再报 `RMT_ARCHIVE_RESULT_CAPACITY`；角色卡字段无法完整快照报 `RMT_RECOVERY_SOURCE_SNAPSHOT_MISSING`；镜像调度失败仅claim路径按仓库既有 `keepCommittedOnMirrorFailure` 模式告警不回滚。已知限制：宿主持续抛错时deferred尾行仍可能记一次UNCODED；CAS内瞬时翻转保留既有无码哨兵。

**验证与限制。**新增31项测试全过；全量 tests+tools 共2503项与原160项失败完全同批（PASS→FAIL=0、FAIL→PASS=0、无消失用例）；34/34 prompt哈希不变；16/16功能突变仍被捕获；全文件语法通过；确定性重建两次字节一致；六个历史冻结闸门仅按既有“精确逆块白名单”惯例登记本轮已审变更，闸门文件本身逐字节未动、拒绝未知改动的语义不变。真实TT/手机/真实酒馆/原生IndexedDB未执行，不以模拟结果冒充真机验收。

### 历史已交付：r84.29 单调恢复（Kimi 独立回执已取得）

**范围与结论。**r84.29（0.8.105）修复续写覆盖旧进度：成功片段以 `retainedPartials` 单调保留，代码自有schema合并，冲突报 `RMT_RECOVERY_MERGE_CONFLICT`。本轮仅修该边界，不包含建档/邮箱零请求问题。

**独立回执。**2026-09-20 已取得 Kimi 独立审查回执（包外 `Hearttrace-r84.29-independent-review.md`）：以两个实际ZIP差异与独立复跑为准，397/404共有文件逐字节一致、7改8新增与声明一致、基线2394项对候选2472项无PASS→FAIL、34/34 prompt哈希一致、16/16突变捕获、双重建字节一致。回执结论：续写修复在Node模拟边界内成立；因160项既有失败与无真机验收，不放行公开下载。

**降级风险（如实告知）。**r84.29 起journal可能含 `retainedPartials` 等新字段；用 ≤r84.28 旧包打开含这些字段的未完成任务时，旧版本不认识新字段，可能无法续接该任务。已有数据不丢失——换回 r84.29+ 后记录仍在；但降级期间不要指望旧包续写本轮新产生的未完成任务。建议升级后不要回退版本。

**diff 行数口径（统一）。**逐文件净增/净减与补丁重放为唯一口径：r84.28→r84.29 全量 diff 经补丁重放可逐字节复现全部412个文件，净 +519 行一致；各 diff 算法的毛增/毛减总计（+9427/−8908、+10849/−10330、+10217/−9698）因算法不同而不可互相复现，不作为证据使用。后续轮次一律报“净行数 + 逐文件补丁可重放”，不再引用毛计数。

### 历史已交付：r84.28 共用任务兼容与恢复

**范围。**用户明确要求所有任务的共用兼容与恢复入口，不仅邮箱。以已交付r84.27为生产基线，不恢复旧私有接口、不改其他创作指令、请求渠道、输出额度、标签/世界书/生图与Key逻辑。档案整理独立草稿继续遵守r84.27保存-before-request合同。

**旧标识安全补全。**V1 journal允许空内部archiveTargetEntryId；仅canonical读取边界已逐项确认角色、聊天、档案revision、模式、目标索引、清除标记及写入栅栏时，对返回副本补全该派生标识。原持久记录读取时不改，明确非空不匹配不替换。禁止在通用身份函数中把缺失的必需角色/聊天/revision全部当兼容。

**原任务原成果。**主模式与ADV单/批/补全、HEART分部/四季/萤火虫、房间日常及内容项使用同一恢复层。原operation、日期/计划/格式偏好、槽位与requestHash维持；成功段仍交原领域验证器复验，未完成段才请求。原已有的有界legacyPrompt兼容合同保留，不增加通用hash绕过。

**旧背景不能被失败重试污染。**旧journal缺少已验证输入快照时，新读取背景与来源策略仅暂存在当前handle；匹配对应原配方后才保存。取消/不一致不把新背景写成旧任务最初背景。增加可选inputSnapshotVersion=1标记，不改变档案schema、DB/缓存键或原容量。旧无成功/无截断失败记录（包括旧版本过早写入无标记背景的记录）仅经明确确认可按当前已读取配方另试，并保留旧失败配方；取消不发送，完整和截断结果不可走此重启路径。所有边界仍有原有受限JSON/CAS/超时/取消保护。

**真实变化与出口。**共用生成设置Hash与旧序列化逐字节一致，只把比较提前到写入栅栏变化之前。来源、角色、Persona、配置、档案版本真实差异仍阻止错配；提示及脱敏诊断区分固定phase/category，不存原文、散列、连接或Key。恢复区新增显式“导出未提交草稿”，文件含原背景与草稿，仅用户点击时下载，不作为脱敏日志公开。此模块保全文件本轮未加入导入/跨聊天迁移功能；与现有档案整理导入格式不同。此前模型结果已丢且无副本的情况不承诺还原。

**验证与限制。**新增矩阵覆盖所有17个主模式标识与8种子操作的共用生产恢复/请求/缓存链；矩阵使用合成分段验证器，不冒充每种领域正文或每个按钮都现场生成。真实旧bundle经邮箱/基础语言/四季按钮建立失败或半完成任务，新bundle新页面续接；另测明确标注的旧可选字段缺失样本、真正来源变化、导出、原2000楼63段41成功的升级流程。旧测试不修改、不删不跳过；三轮定向/全量/全语法/双build/范围/浏览器，第三轮必须最终ZIP重解压。真实TT/手机/原生IndexedDB和模型渠道与模拟明确分开。专用安全扫描和自复核分别记录。

### 历史已交付：r84.27 TT 保存确认与读取失败

**已证实的代码缺口，不冒充现场定因。**r84.26在公共IndexedDB接口缺失或不可访问时，会把整理任务置为pageOnly，继续请求模型但只保留内存；这使更新/刷新可能失去已付费成果。另有已读空作用域被缓存、显式重读仍不读真实记录的问题。TT现场是否正好由该分支引起，仍需现场证据。

**每段保存承诺。**新建/续接首先确认任务输入已持久保存，才允许首个模型请求。成功分段仅在真实CAS事务完成回执匹配后算本机已存；缺失API、拒绝访问、配额/事务失败或无效回执不再静默降成页面模式。失败停止后续分段请求，保留当前页结果和上一份持久记录；不自动补发模型、不自动换库、不要求清数据或重建。保存-only/导出入口仍可使用；存储恢复后只保存未落盘结果，不重复生成。界面同时显示本机已确认数量与当前页数量，不把页面42段都说成本机已保存。原正式档案已保存内容及其他模块阅读不被锁住。

**读取与并发。**公共接口不可用、读取被拒或记录包不合法，明确“读取未完成，不能认定没有记录”；不显示“未找到”。用户点击重新读取可越过旧的空缓存。与本页未完成写入并行时不采用旧读覆盖新成功；遇到其他写入者/删除栅栏revision变化保留双方并提示冲突，不能借重读把revision改成最新后强写。复用原DB名字/版本/键/格式、CAS和null-payload删除栅栏，没有另建TT私有库或扫描其他来源。draft写事务尽力使用标准strict durability选项；仅旧引擎明确TypeError不支持该字典时退回原标准原子事务。凭据事务及Key逻辑原文不改。

**真实UI补口。**失败可能发生在初始任务写入、尚未取得ticket时。失败后仅刷新仍可见且身份匹配的恢复区域，及时提供保存/导出与正确数量；不导航到其他页、不改来源/滚动、不影响关闭后任务生命周期。

**范围与验收。**不修标签/NAI/生图/高级API/模型；94个旧命名Prompt函数和旧正则必须保持逐字节不变。2000楼/63段/41段旧包保存后新包实际页面恢复22段；另测第42段结果保存失败、保存-only42、重开后仅余21段。缺失/拒绝API不得收费且能重读恢复，旧原格式导出可导入。三轮最后必须来自最终ZIP新解压。原生IndexedDB、真实TT/酒馆/手机/网络模型与模拟分开记录。

**测试维护披露。**旧Node回归夹具没有持久层会在新保存前置处停下：仅为相关旧夹具补同项目已有的修订号/CAS模拟存储，保留旧行为断言；放弃草稿断言等待既有异步durable接口。两项页面-only出口测试改由真实旧草稿导入后保存失败建立页面成果，不再通过已禁止的“无存储仍付费生成”来造夹具。新增TT/事务反向测试，不删除/跳过旧测试，不修改历史冻结哈希凑绿。最终失败清单、逐项归因与测试数见本轮包外证据。

**恢复边界。**存在原本机记录/有效导出/仍活着的旧页面成果才有恢复基础；从未落盘且页面已销毁、没有副本的41次原模型输出不能被ZIP凭空重建。安装不删除原库，也不保证修复已丢失的数据。

### 历史已交付：r84.26 已保存草稿可见与升级恢复

> 以下为r84.26历史实现范围；当时遗漏的无IndexedDB页面-only退路及显式重读空缓存，以上述r84.27合同修正。旧测试通过不等于已确认每个TT环境落盘。

**恢复可见性。**用户打开原聊天的“当前档案”时，允许按需读取该作用域本机整理/简介草稿，显示成功数与继续入口；不是等到再发起建档才读取。新增显式“读取已保存草稿（不生成）”，不调用模型、不读取世界书、不开其他聊天、不修改正文或正式 Mxxx。未完成模块仍沿用原规范化缓存/恢复记录，不重写其 Prompt。

**升级验收。**必须用真实旧包建立恢复记录/导出文件，销毁旧运行环境，再加载新包并走实际页面和按钮验收；不能只在新包中构造41段、测试函数主动hydrate后就宣称旧版升级通过。重点合成场景为2000楼、63段、前41段成功，升级后打开即可看见、只请求剩余22段。

**保存与导出。**导出先读取本机真实草稿，不生成虚假的空文件；存储读取失败但当前页有成果时仍允许导出。无关简介草稿损坏不能剥夺有效档案草稿的导出/保存出口。新增页面草稿显式落盘入口，未成功确认存储不写“已保存到本机”，失败保留页面成果与旧持久记录。并行读取共享一次，迟到完成仅更新原档案恢复区域，不把用户切到的设置/内容页换回来。

**不放宽边界。**不改变原本机数据库名/版本/记录格式、4草稿槽、240条正式记忆、12MB、请求/来源身份、CAS、删除栅栏、只读/备份、超时取消或收费重试；不改API、模型、用户输出额度、原Prompt、正则及其他功能。普通启动、普通消息仍不打开IndexedDB，只有用户明确打开当前档案/读取/导入/保存/整理才进入草稿读写。

**不能伪造恢复。**r84.25已落盘记录或有效旧导出可以按原校验恢复。旧r84.24仅页面内存且已经销毁、没有导出或其他副本的结果不能靠安装包还原。不得让用户先清数据或从头重复付费来验证“有没有草稿”。用户设备中这41段的实际副本状况未取得，合成测试不是已找回用户原件的证明。

**回执。**本轮三轮分别为工作目录、独立副本、最终ZIP重新解压；最终数字、CRC和SHA只以实际生成的包外报告和日志为准。原生IndexedDB、实际ST/TT/手机/模型与模拟宿主浏览器测试分别记录，不以截图或旧数字代替真实环境验收。

### 历史已交付：r84.25 恢复与模型适配

**真实续接。**基础语言和其他共用生成任务冻结首次背景、受限证据与人设资料，保存顺序与原Prompt保持一致；临时读取失败/顺序变化不应冒充用户修改。角色卡、Persona、选择、聊天和档案版本的真实变化仍拒绝错误请求/写回，绝不删除身份/CAS校验。旧零成功且无截断内容草稿只有明确确认才另试，并保留旧失败记录；已有成功段仍精确匹配并重走原验证器。有效旧journal不因七天到期被忽略。

**本机恢复（r84.25历史范围；当前读取入口以上述r84.26为准）。**当时仅明确建档/续接动作懒加载独立本机库；分段结果及输入一起持久保存，失败不假报进度，不继续下一段收费。正式档案和草稿分离，Mxxx/240条/12MB/CAS/删除栅栏不变。稳定定位键只寻找原草稿，完整角色指纹继续授权验证；角色卡改变不能因查找键漂移而自动另发请求。新增v1/v2待入档成果导入，导入不请求模型、不覆盖旧草稿、不赋予来源权限。

**升级边界。**能保留已经正式保存或仍在有效恢复记录中的模块/分段。旧版仅存在页面中的成功段须先导出、再升级/导入；刷新前从未落盘且已丢失的数据不能被新版找回。不得把“本机保存成功”泛化成所有旧页面状态都自动恢复。

**手动连接。**地址/模型自动保存；用户输入Key本机AES-GCM保存，设置仅opaque ref，地址绑定，不回显/日志/导出/明文回退。只在授权请求瞬间取用；不读取宿主或第三方现成Key。当前浏览器/站点保存不等于跨设备同步，CryptoKey不导出不等于隔绝同源脚本权限；保存失败保留输入并如实提示。

**高级参数与国模。**默认关闭，推理强度/流式/采样排除/常用四项/取消/受限JSON/保存撤销重置；排除优先且是删除字段不是0。新编辑器8KiB只限配置文本，不限模型输入/输出。受保护的模型、消息、额度、地址、凭据和tools不能从高级JSON覆盖。非对应后端不静默忽略配置，不擅自切连接。

**响应。**兼容OpenAI格式、DeepSeek/Qwen样式、Gemini candidates、Anthropic文本块、Responses以及公开Profile累计流，同响应JSON/SSE和所有原保护保留。最终正文空与只有推理分开诊断；不能把reasoning中的JSON自动当最终结果，不自动重试/换模型。没有仅凭3.6/3.7/3.8名称猜参数的规则，也不能由一次截图概括所有中国模型失败的原因。

**最终证据。**三轮本地各245/245定向、295/295语法、57/57实际Chromium模拟宿主用例；完整1887项1726通过161失败，161=实际基线154+5历史冻结+2旧空回错误码断言。94个既有命名Prompt函数全部逐字节一致，原正则全部保留，原测试未改删跳过。原生IndexedDB被浏览器环境导航策略阻断；实际ST/TT/手机/真实模型未验收。专用Codex Security无动作可调，扫描未执行，自复核未发现未处理新增High/Medium，独立回执未取得。第三轮来自最终ZIP重解压，身份见§7.0及包外报告。

### 历史已交付：r84.24 用户设置窄修

**最大输出。**实际设置控件不再有60000的max；默认值改为60000，已保存的其他合法正整数保留。设置读取、建档完整请求计量、Profile实际发送和手动API的max_tokens共用纯数值规范化函数。合法用户填写值原样传递，包括大于60000或小于1024；旧功能内部建议值不得偷偷压低用户设置。不填采用默认，非正整数／不可精确表示的输入给出提示并保留原设置；该数值格式校验不是新的模型额度表。错误摘要显示本次实际额度，不再写“插件允许最高60000”。连接／模型／URL／stream与同响应JSON/SSE、重试不变；响应字符容量、12MB等保护与请求额度是不同边界，未擅自扩容。

**标签保存含义。**当前设置界面改为“按用户所选标签保存”：勾选标签块参与之后的整理，未选标签块略过，无标签正文保留。未选外层块包含内部，即使内部名字被勾选也不绕开原隔离语义。勾选／全选／反选先改草稿，用户点击“保存选择”才生效；撤销编辑复原已保存选择；零标签也是有效选择，不加最少数量或强制重建。旧排除偏好在首次明确保存前保持旧语义，不自动反转用户设置。

**标签根因与范围。**旧归一化最多保留32个名字、旧扫描最多展示100种并只看末500条／256k字符；这会造成后面的选择丢失或不可见。新明确扫描完整当前聊天，分时让出UI并核验聊天／Persona／生命周期，选择数量不再默默截到32或100。扫描仅由按钮触发，启动／普通消息不跑全聊天。标签名仍按原惰性文本及合法名称规则处理；DOM使用textContent和原生checkbox，不开放HTML/CSS/JS执行。

**隔离复用及证据。**原tagAt/symbolAt解析器与嵌套／编码／不完整块处理语义保留；新增保留集合转为实际来源中未选集合，仍调用原隔离器。为长外部来源提供只读保留区间回调，先按完整记录隔离，再投影到原片段位置；不让被排除块的中间片段漏入请求，也不重编号原externalId或Mxxx。原始来源账本、原聊天与旧正式档案不被清洗／删除。标签设置纳入读取与续接身份；配置变化保留旧成果并阻止错配续写，不删除草稿或清数据库。保存的是筛选后整理结果，不是修改原聊天或追溯抹除既有档案。

**世界书。**只给现行世界书选择弹窗增加独立可滚动正文和固定头部“完成／关闭”按钮；不改世界书排序、来源接口、历史／设定语义和既有容量常量。样式严格限定该弹窗，复用相同card class的档案分组管理器不受影响。主题沿用原变量，关闭仍走原真实按钮处理，选择保持原保存方式。

**不变合同。**其他创作Prompt、对白正则、CG继续编辑／还原／保存、NAI偏好、第三方公开调用和历史读取保留。请求输入因用户选择而减少不等于改写生成指令。94个旧Prompt命名函数中只涉及`filterJsonPromptStrings`分派到新保留策略；其余创作／构思函数原文保持。12MB、CAS、删除栅栏、240条、独立备份、只读、跨聊天、取消／超时及轻量启动未放宽。

**验证边界。**正式三轮分别为工作目录、独立物理副本和最终ZIP全新解压；执行同口径定向、完整tests＋tools、所有JS/MJS语法、两次确定性build、范围与标签隔离差分、实际Chromium UI回归。真实模型、原生IndexedDB事务、实际酒馆／TT／云酒馆及iPhone／Android真机不冒充已验。专用Codex Security本轮已尝试发现：已安装但无可调用工具；专用扫描未执行，差异自复核单独记录，独立回执未取得。完整测试的历史失败必须保留并逐项归因，不改冻结哈希或删旧测试凑绿。

### 历史已交付：r84.23 建档模型预算

**已确认根因。**用户2026-09-18现场诊断运行真实r84.22，完整输入64,004个UTF-16代码单元／63,992个Unicode字符／142,059个UTF-8字节，宿主估算输入47,030 token，请求最大输出60,000，模型上下文和最大输出能力均为null，providerRequests=0，错误`RMT_ARCHIVE_CONTEXT_BUDGET`。代码复现证实触发固定32,000输入token门槛，不是提供方返回、不是数据库拒绝，也不是已确认的模型上下文不足。不能以输入加输出107,030就断言超过未知模型容量。

**修复合同。**仅建档请求预算去掉固定32,000 token／96,000字符拒绝。仍记录完整请求的字符、UTF-8字节、宿主token估算和实际请求最大输出。模型容量未知就保持未知，正常进入原提供方；可靠且与实际请求匹配的已确认容量才能用于相应预算判断。当前生产链没有可信容量表，不虚构自动识别，也不强制用户另填上下文。不新增确认门槛、自动重试、自动切模型／URL或输出降档。

**旧草稿与性能。**新建、增量、简介和旧草稿实际发送统一用建档计量；只在发送处选择该计量路径，旧requestHash、已捕获Prompt、sourceIdentity、成功片段重放和旧结果normalizer不变。旧`skipTokenCount`仍被尊重，不给旧续接分块新增逐段tokenizer等待；未知计数不伪造token。保存失败仍只重试保存。

**不变范围。**其他模块的Prompt、对白正则、UI、生图编辑／还原／保存、NAI偏好、响应JSON／SSE分流、公开来源接口均不改。共享32k／96k常量没有调大或删除，其他非建档模块保持原规则，不把本次局部授权扩大到全部功能。来源256片段／240,000字符每批、正式240条及待入档出口、UTF-8 12MB、CAS、删除栅栏、只读／备份、跨聊天、并发、超时和取消继续保留。

**测试与交付。**两条直接把未知容量当成固定32k／96k的旧测试改为“显式合成模型容量”的预算测试，同时新增27项反向及链路测试；没有修改146项旧失败预期、删测试或跳过文件。实际Chromium加载候选bundle点真实按钮，用模拟宿主／provider／存储验证高估算token、60000输出、三批、销毁页面后恢复；原生IndexedDB尝试在导航阶段被环境策略拦截，未更改或绕过策略。专用Codex Security虽有安装记录，但本轮发现无可调用动作；差异安全自复核和专用扫描不能混称，独立回执未取得。最终文件及逐轮回执见§7.0与包外证据。

### 历史已交付：真实r84.22建档分批、保存、续接与请求预算

**授权和基线。**先解压并完整读取本轮START-HERE及同包CURRENT；唯一实际基线为r84.21（0.8.97，2,539,378字节、327文件、CRC正常，上述SHA一致）。原上传件和原安装包保留；在独立副本实施。GitHub未读写远端、未上传、未发布。新r84.22并非给旧包改名。

**来源与批次。**先取用户已选来源的完整快照，再按每批外部256片段／240,000个UTF-16字符规划；长单条保留尾部、顺序、原证据ID。实际完整请求过大时，仅在发送前继续本地拆分；不自动收费重试、不改URL或模型。每次点击只处理一批，批内各小分块不新增逐条确认。批次结果通过原校验后和检查点同一次正式保存；失败不推进。重新打开后从已正式保存的检查点继续；不要求全批完成才能阅读。

**240条的边界。**最终canonical档案上限仍为240条，已有Mxxx不覆盖、不重编号、不扩大全局常量。接近容量时先接纳能安全容纳的有效条目，未入档的有效结果留在有界的capacityPending中，可查看数量并显式导出；检查点停在当前批，不把待入档条目算完成。已满时不继续收费请求。导出不等于这些结果已成为可供其他模块引用的Mxxx，也不是实现了无限容量；要自动容纳更多正式记忆，仍须另获容量／schema方案授权。不是只给“完全重建”一个出口。

**状态字段与兼容。**仅在memoryBank内增加有界可选`archiveImportProgress`（内部version=1）：任务身份散列、来源账本固定revision及片段引用、批次游标、必要受控背景、已处理／已保存计量和容量待入档结果。schema仍为3；原Mxxx、外部证据ID、CAS、删除栅栏、跨聊天、只读、备份合同均保留。不把整份原聊天反复复制到每个检查点，所有新状态仍受UTF-8 12MB保护；已固定账本可读时不再复制完整摘要。无可用持久账本时沿原授权使用有界来源快照，不要求停用数据库。旧版本能读已有正式档案，但不知道新续批检查点；降级后不能用旧版继续本任务。升级／降级前应先保存或导出页面内未提交草稿，不要求清站点数据。

**真实入口与草稿。**普通“生成当前窗口档案”原本已有续接分流，本轮未把它错误归因为完全无续接按钮；修复从handleOverlayClick、准备、恢复、完整请求到提交的实际路径。同源同配置保留旧requestHash和成功分块；旧格式页面草稿使用原捕获请求完成当前段，原先未采用的尾部另列后续批次。真正聊天、角色、Persona、档案revision、读取范围、来源选择或生成设置变化分别提示，不泄露内容／密钥。零成功失败可明确按新配置另起任务，旧草稿保留，不自动删除。有保存结果的续任务按来源片段区间排除已保存部分；不因重新分块重复发送。已完成结果仅因角色显示名改动而待重试保存的旧兼容保留，仍核验物理角色、其余人设、Persona、聊天、配置和CAS。

**页面草稿寿命。**批内成功但尚未正式提交的片段仍是页面内草稿；刷新／退出可能丢失，不冒充永久存储。明确另起任务会保留旧页面草稿，占用原有最多4个页面草稿槽；有显式导出和用户确认后的清理动作，没有自动清空数据库。正式保存的批次检查点另受canonical保存保护。

**约8万字符反馈。**没有这次用户渠道完整诊断，不声称已查明现场根因，也不拿附件r84.18旧诊断代替。代码／合成测试证实旧全量摘要截取、单条/片段与单批边界、选定世界书在背景重复附加等本地路径；本轮按实际发送的完整指令、人设、世界书、旧档案背景、正文计量，并单列所请求输出。UTF-16长度、Unicode字符数、UTF-8字节、宿主tokenizer估算、请求输出和合计tokens分别记录；tokenizer不可用即未知，不用字符冒充token。生产没有可信的各渠道模型上下文／最大输出表，相关能力仍未知；本地量测通过不保证任意模型接受8万字。该历史实现当时仍有96,000字符／32,000估算输入token硬拦截，后来由上述现场诊断证实不合理，现已被r84.23建档预算合同替代；用户最大输出、响应容量、取消／超时和r84.21同响应解析仍保留。

本地三轮、失败逐项归因与未执行项见§7.0。原有创作Prompt和对白正则没有被本轮改写；生图编辑／还原／保存、NAI只作为构思偏好及千千结／柏宝书公开读取实现未变。

### 历史已交付：r84.21手动API同响应分流

用户明确授权：针对已经复现的“有效SSE经酒馆转发丢失Content-Type而误走JSON”的问题做窄修；不影响别的功能，复核三轮并提供一键上传名称。此次不包含模型列表、旧草稿新建/续写交互、标签过滤或第三方适配扩展。

只修改`src/core/independentApi.js`：同一响应最多检查前8192个UTF-8字节，再把已读字节原样交给既有JSON或SSE解析器；不改stream开关、接口协议、模型、地址或请求体，不二次请求、不自动重连/翻译。Content-Type缺失/错标时使用有限数据特征，明确HTML与不完整/畸形内容仍按保护处理。不从HTML里搜JSON、不把推理当最终正文。

三轮分别是工作目录、独立副本、最终ZIP新解压；新测试121/121、相关394/394，真实HTTP回环12/12、实际Chromium响应16/16、旧UI252组零差异及生图编辑流程保持。完整1693项1569通过124失败，新增两项为r84.20独立API旧哈希和0.8.96版本冻结断言。原测试预期未改，无新增行为失败。身份与未执行见§7.0。

### 历史已交付：r84.20自动构思与编辑功能隔离

用户本轮明确授权：所有生图入口共用视觉提炼规则；日常一格按实际格数自上而下；习惯性微笑、条件反应等不自动作为当前画面表现。同时特别要求不能连带改坏已有提示词继续编辑功能。唯一基线为真实r84.19完整包。

新增纯共用规则工具，首次请求可同时返回有界cgPromptDraft；三模式必要normalizer保存该可选字段。缺失/无效草稿不使原有合格剧情失败，不增加调用。重新构思复用同一核心规则并保持各模式画面类型。自动来源摘录保守剔除习惯/服装偏好/破碎语句，用户手改及已保存图的metadata优先。新字段加入图像目标签名防迟到覆盖，无该字段的旧签名与旧任务请求保持原文。

编辑器/UI/CSS、最终发送组装、用户手写外貌规范化函数原文未变。三轮实际DOM测试了继续手改、格式切换及还原、显式重构及还原、保存外貌与关闭；另起进程读回保存的用户提示和外貌。不能将模拟接口产生的示例视为真实模型画面质量验收。具体身份、失败与未执行见§7.0。

### 历史已交付：r84.19生图格式仅为构思偏好

用户明确要求取消所有生图链路的语言／句式硬拦截，不能只修日常一格，修后复核三轮并给实际新ZIP。本轮从真实r84.18开始，未使用上一轮不可核验的“71项通过”等记录。

本次覆盖：相簿／ADV／日常一格生成字段、重新构思与外貌、图片设置、发送预览、最终公共接口、漫画包装。只移除语言和句式拒绝层，不改变生成Prompt指令、领域证据、保存和费用保护。通用后端完整提示缺失时复用原场景＋具名人物组合，不增加翻译请求。

修改4生产文件，140个既有生产JS逐字节一致，0新增生产模块。两旧测试文件内8个行为案例按用户最新明确授权由“格式不同拒绝”改为“合法数据可发送且预览一致”；取消、领域校验、长度和保存保护断言保留，无关冻结预期未改。

实际三轮记录与身份见§7.0。三轮都是本地自复核，不是独立reviewer；存储和第三方接口为模拟，不能表述真机验收通过。本轮未写GitHub、未调用真实模型，原包保留。

### 历史已交付：r84.18简笔画与只读恢复

本轮用户明确要求“按上文要求窄修，修完复核三次”。唯一开发基线为实际r84.17 `hearttrace-upload.zip`，不是r84.16，也不是早期预览或源码反向还原。

授权仅限：
1. 移除明信片纯构图／写实性门槛，尤其boat不强制依赖water/shore；简单、重叠、比例奇怪和不写实不是无效设计。仍保留可解析结构、支持图元、有限数值、资源和执行权限边界。
2. 复现只读恢复问题后窄修：临时读取失败不宣称源聊天永久锁死；允许用户主动重新读取原目标，迟到的历史读取不抢回已经离开的页面。备份本体不可改，重新取回合法源档案不等于把备份解除只读。
3. 三轮复核新修复完整链路、其他Prompt／正则及跨模块回归，最后一轮从最终ZIP重解压；更新同一CURRENT，提供新版本完整ZIP。不写GitHub，不自动发布。

本次已复现并修复失败快照缓存阻挡源恢复、迟到历史读取覆盖当前页两条路径。正常当前档案入口的只读清理本来正确，保持原实现。未改相簿、ADV EVENT、HEART、歌曲、来源接口或其他创作规则。

r84.17独立复核阶段已经结束并收到用户转交的Claude报告，不再写“尚未收到该报告”。两项JSON／可选设计处理沿用：无效可选设计丢弃、有效信件保留；合法JSON标签字符串按普通文本转义而不执行。此授权不放开HTML网页响应、领域校验或存档保护。r84.18的新独立审查和真实环境验收未执行。

三轮具体身份和结果见§7.0；r84.17身份、原报告及独立复核记录在§7.1–7.2仅作为历史和本轮基线保留。

---

## 1. 项目目标与范围

「心跳回忆」是 SillyTavern 的角色关系档案与衍生内容插件，现有界面显示名为「心迹回廊」。

核心目标是由用户主动将当前聊天整理为独立、可追溯的档案；在明确区分真实经历、人物／世界设定、未来模拟和虚构番外的前提下，生成相簿、ADV EVENT、角色互动、房间、私人终端、出行路线、邮箱、日历、角色印象曲、ENDING等内容。

保持不同角色、不同聊天世界线、live数据与历史只读快照隔离；在iPhone、TT、云酒馆和长聊天下避免启动、发送和交互回归。

不把兔子镜、柏宝绘、构画或第三方记忆插件自身的产品需求混入本项目。提及第三方只用于本项目已经获准的公开接口兼容，不等于获得修改或接管第三方的权限。

---

## 2. 开发、权限、验证与交付规则

### 2.1 修改基线及最小范围

- 用户明确上传或指定ZIP时，该ZIP为本轮唯一修改基线。不得以GitHub main、旧报告或印象替代。
- 未另指定时，先核对§7的最新候选包、manifest、index及build/cache身份，再继续。发现实际文件身份不一致必须说明。
- 原上传包、上一候选和既有用户数据保留；在独立副本中修改，生成新身份的完整候选。
- 只改获准事项及实现它们不可缺少的连接。不能为UI改动重写其他功能Prompt；不能为修复加“小巧思”、新功能、数量门槛或更强的生成限制。
- 先找根因，避免复制实现、叠补丁和改错死文件。`src/ui/`与`src/modes/`有部分历史同名文件，必须以入口可达性确定真正运行路径，不能顺手清理全部重复文件。
- “检查／诊断／复核”不等于允许修复。只有用户明确要求修改、修复、制作新版才写生产代码。
- 报告中的“已实现”必须能在实际交付ZIP找到；不得把工作目录、未打包修改或历史日志当作已交付功能。

### 2.2 仓库、外部工具与发布

项目仓库记录：`Zaiyebuzuoyouqingdetiangou/tokimemo`，约定正式分支为`main`。`toto`、`tototest`不属于本项目。

GitHub默认只读。未经当前任务明确授权，不commit、push、建branch／PR／Release、修改远端文件或触发发布。写检查词、制作ZIP、修改CURRENT不等于取得远程写权限。

外部接口必须由作者正式公开、文档化并允许相应用途；读取公开文档或源码不能当作调用私有功能的许可。不得导入第三方私有模块、模拟点击、操作内部DOM／设置／metadata／数据库／密钥或绕过接口缺失。

专用安全工具或独立reviewer不可用时如实标记；不能虚构工具已执行、独立回执或网络状态。发现High／Medium不得直接放行，应修复或明确阻断。

### 2.3 复核方法

每轮修改至少执行：本轮定向回归、相同口径的完整测试、全部JS/MJS语法检查、runtime连续确定性重建、ZIP清单与CRC、manifest／index／build身份和必要SHA-256核对。

用户明确要求两轮／三轮时，必须按该轮次数完成有记录的检查，最后一轮从最终ZIP全新解压验证。三份相同结论或复制日志不算三轮。自复核不能改称独立审查。

同时比较基线与候选，重点包括：新修复完整调用链、旧功能、既有Prompt模板、正则、实际可达模块及打包结果。只变资料输入与修改创作要求必须分开说明；不能先还原候选源码再用哈希证明“未改”。

保留真实失败记录。不能删除测试、跳过文件、改预期值或放宽领域校验来凑全绿。因获准行为变化需要更新旧行为测试时，必须申报旧／新断言及理由，并补反向与安全测试。旧版本全文哈希失败可以归类，但不自动等于无害，仍要核对差异与行为。

实际Chromium DOM检查、模拟宿主／接口／存储、原生IndexedDB、真实模型、真实共装酒馆、手机真机是不同测试环境。没有执行的明确写“未执行”，不以自动化数字替代真机验证。

### 2.4 交付

新候选使用新的manifest／build／cache身份；交付完整ZIP，不只交源码或旧bundle。不自动删除旧版本、发布或上传GitHub。

本项目当前一键导入文件名是`hearttrace-upload.zip`，包内为单根`tokimemo-main/`，其中`index.js`、`manifest.json`、`src/`、`dist/`等同层。命名本身不是GitHub功能，必须有接收分支上的对应导入工作流；每次涉及发布时只读核对，不从旧记录假定远端仍未改变。

固定上传名的文件需记录build和SHA-256，避免同名新旧包混淆；保留可追溯原版。中文文件名下载不便时提供英文名备用。

最终报告简要给出修复、不变范围、测试与语法、版本／CRC／SHA-256、安全复核、GitHub写入状态和待真机事项。

---

## 3. 档案、来源与事实边界

### 3.1 每个聊天独立建档

- 每个聊天窗口独立档案和世界线。首次建档由用户主动触发；默认手动增量更新。用户明确开启按楼层自动同步时，沿用既有到期门槛、锁和作用域，不因打开页面自动建档。
- 普通新增聊天不会无故让旧档案失效或重做旧收藏；未归档新正文不能由派生模块偷偷作为已发生历史读取。
- 已归档正文编辑、删除、重排或来源实质变化时，按现有指纹／版本规则停止不安全的增量提交。只有用户明确完整重建时才处理相应重编号和不兼容派生数据。
- 普通更新保留既有Mxxx、有效派生内容、评论及实图，不因UI更新批量迁移或重生成。

### 3.2 三类资料分层

**真实历史**：声称两人确实经历过的事件、历史CG、告白、关系变化、痕迹、终端历史、成就及已发生日历事项，必须有当前档案的真实`sourceMemoryIds`与匹配`sourceMemoryAnchor`。不得伪造锚点或凭背景设定直接证明共同经历。

**明确初始设定**：角色卡、Persona及受控世界书明确说明的身份、关系和可见外貌可以使用。例如开局已明确为夫妻，亲密称呼不能只因为Mxxx尚未记录夫妻关系就一律拒绝。设定依据与后来剧情关系变化分开保存。

**人设推演／未来模拟／番外**：春夏秋冬、后日谈、日常一格、时空回响、蝴蝶外延世界，以及已获准的人设生活扩展，不需要假造一段曾经说过的真实台词，但不能伪装成正史或反写新Mxxx。已有证据的事实与推演标记不可混淆。

“允许按人设”不等于取消所有字段完整度、语义、同意／关系或安全校验。只取消用户明确指出的不合理真实依据门槛，不擅自重写共用Prompt。虚构双方对白不应修改主聊天；各模块原有的对白授权范围保持。

### 3.3 角色主档案与世界线

同一角色不同聊天可共享角色卡明确支持的稳定资料；生日、年龄、身高、血型、职业／学校等没有依据时保持未知，不猜值。后来在某聊天才得知的事实留在该世界线，不能自动提升为所有聊天公认事实。

角色档案默认折叠，保留头像、姓名、读取数量和必要操作。不要堆叠重复静态玩法说明。

### 3.4 历史只读、删除与备份

历史档案默认只读，不自动切换当前酒馆聊天。解除界面只读状态不等于通过写权限；真实写入仍检查live角色、chatId、档案身份与`archiveRevision`。

档案删除、覆盖等破坏性操作沿用既有双确认。仅移除档案室索引不删除源聊天或独立备份；明确删除档案后保留无内容的删除栅栏，阻止迟到结果或旧metadata复活数据。

禁止调用SillyTavern聊天删除接口、替换`context.chat`、splice原生聊天正文。档案删除只操作本插件自己的数据。

runtime按需加载后可维护本机独立备份；普通启动不得为备份打开IndexedDB。源聊天暂不可读时只展示身份匹配的只读备份，不能据一次读取失败宣称源聊天已删除或永久锁死。备份副本始终不可写，不能假称恢复原聊天正文或重新绑定到另一聊天。清站点数据、换设备／浏览器不会自动同步备份。

用户主动重试读取源聊天时，失败产生的backupOnly缓存不能遮住已经恢复的源数据；重新执行原目标身份、角色、档案版本、生命周期和删除栅栏校验。成功后得到新的合法源快照并仍默认只读；不得把旧backupOnly清除后硬写、把备份覆盖回源聊天或自动取消用户主动只读。源仍不可读／身份不匹配／已明确删除时保持拒绝不安全写入。

历史读取等待期间用户返回当前档案、其他页面、关闭档案室或改变聊天／角色／Persona，迟到成功和失败不能强行显示历史只读页或错误页。正常当前聊天建档不因以前查看备份而永久锁住；相关正常入口保留原实现。这里限制的是读结果的页面提交，不新增取消所有后台生成的规则。

### 3.5 记忆来源：四条通路不能混为一个开关

1. **当前聊天正文**：按原始楼号选择；隐藏的普通聊天楼层能否纳入由明确选项控制，隐藏不重编号。范围只控制正文，不代表外部摘要也“全部读取”。
2. **用户手动选择的历史世界书**：通过宿主公开读取，支持整本或指定条目并标记“作为历史摘要”。不依赖第三方记忆接口，也不依赖关键词激活；关闭自动外部摘要不能把这条通路一起关闭。
3. **自动外部摘要**：只读取获准且明确属于当前聊天的公开接口。千千结、柏宝书、自带摘要分别显示就绪状态、来源和覆盖，不能把接口缺失一律叫“0条”。
4. **自动激活的世界书背景**：普通前／后置及宿主公开返回的深度注入文字用于建档背景，不自动转成历史事实。普通设定不得因被扫描到就升级成Mxxx依据。

标为历史的世界书或公开摘要先进入本插件来源账本，经原外部摘要抽取、来源引用与锚点流程归档；不是直接把整个世界书当作已经验证的Mxxx。未选择的书／条目不得借修复扩大读取。

### 3.6 完整性、预算和来源变化

必须区分**来源返回量、本地保存量、本次请求采用量、最终归档量**。完整保存不等于模型已经消费全部，摘要完整也不等于原聊天每楼都有摘要。未知总数保持未知，不把`null`变成0。

长记录完整分片并保留尾部、来源ID与次序；无法安全保存或超限时保旧数据并明确提示，不能截掉后半条却声称完整。

每次主动扫描／建档可读取已选来源；普通启动、普通消息和纯UI切换不能偷偷触发大扫描。连续扫描合并同一在途操作；切换聊天、角色、Persona、千千结内部聊天身份、接口对象或来源选择后，旧读取结果不得污染新的来源账本。

当前实现的数值上限属于§7技术现状，不是永久产品目标。建档的固定32k／96k输入硬拦截已由r84.23移除，不能把来源分批／存储资源保护与模型容量混为一谈。本轮START-HERE已明确授权在既有单批、单请求和存储保护内修复完整来源分批；256片段／240,000字符由全任务截取改为每批采用边界，余量有明确后续批次。不能扩大全局容量、自动连续收费或把未入档结果算完成。正式记忆240条和UTF-8 12MB保护不变；确需扩容／改schema仍须另获授权。

### 3.7 已确认的公开摘要接口约定

千千结：仓库为`atonal519/ST-MyriadKnots`；`ST-BaiNiaoData`是相关数据后端，不等于前端当前聊天摘要接口。本候选采用V1 `qqj_v3_public_bridge_v1.getStatus()`及`getSnapshot().memory.floors`，仅取已保存摘要原文与楼号，分别检查顶层身份和摘要分区就绪状态。

`messageIndex`与`assistantSeq`不是同一编号，不得混用。最近一次召回不是全部历史；文本投影不能冒充完整摘要。未加载／未生成的数据不能借用其他聊天、直接扫私有后端或自动替第三方生成。当前适配不导入CSE／people为历史，也不整份序列化其快照。

柏宝书：沿用`STBaiBaiBook`正式公开读取接口及其`apiVersion`、chat、revision和coverage语义。摘要节点可能是已压缩历史，不等同正文楼数；真实缺口保留，未知`total`不造0，不假称所有接口都提供相同计数。

SP·数据库：此前核对发现未直接接其表格读取来源，同时世界书深度背景与本插件响应识别有兼容缺口。不得把“无直接适配”说成“对方完全没有API”或“必定不能共存”。其私有库和内部函数不在获准读取面；需要直接新增适配时另核实官方公开合同与本轮授权。

---

## 4. 现行产品与交互

### 4.1 窗口、首页与层级

在原有配色、卡片风格上优化，不另做无关皮肤。一级导航顺序为**设置／当前档案／内容**。

设置包含连接、模型、生成参数、来源和显示偏好；当前档案突出来源选择、扫描、建档／增量、简介和两个人的日历；内容页集中功能目录，支持卡片／列表展示，避免所有模块堆在同一长页。

首次打开可浏览入口与空状态，不以先完成生成为条件强迫用户付费请求。进入功能和点击明确生成必须区分。

保留跟随酒馆主题与自定义主题；深色时卡片、输入框、边框、图标和正文成对变色，不能只换外层背景。桌面不要默认铺满到难操作；手机保留正常字号和触摸目标，不靠缩小全部文字塞内容，保留窗口／全屏和TT安全区偏好。

“窗口与导航”始终只挂载一张；切页不丢未保存模型ID等输入。通用按钮美化不得强行覆盖各模块原有`display:none`、禁用、焦点及事件。不能再引入导致裸页的外置CSS／link依赖。

### 4.2 回忆相簿、共同回忆与私密分类

相簿基于真实档案的重要节点增量收藏，不为数量凑内容。普通增量保留实图、原条目与旧评论。

共同回忆是现在的`{{char}}`陪现在的`{{user}}`回看过去，讨论细节、当时未觉察的情绪及如今感受；不是长ADV独白，不频繁出现插件、档案、Mxxx术语。共同回忆原目标约6～8段，服从内容质量及既有校验，不机械凑数；不因UI改动重写创作要求。

分类保留全部、日常、约会、私密、结局等既有类别，**私密排在结局前**。私密只是可以正常打开查看的独立分类；相簿中的成人向内容归入私密。不得沿用早期预览的遮挡墙、再确认才看、伪密码锁或宣称加密，不凭此添加新的生成内容要求。

### 4.3 ADV EVENT

- 以`{{char}}`第一人称、性格、动机和心情补完为核心。
- 每篇实质覆盖四类内容中的至少两类：与事件相关的更早经历／心结／习惯来源，事件前后日常，共同经历时的当时心情，之后的回味与小改变。不能只写类别名或一句标签冒充覆盖；沿用原段数、总字数与第一人称密度等单篇质量校验。单篇和批量同一合同，UI和记忆来源窄修不重写该Prompt。
- **默认阅读为CG＋事件标题／日期＋ADV正文＋翻段按钮一起显示**。只看CG是额外切换；不能默认隐藏图或每次逼用户从CG切回文字。
- 正在阅读时不误显示灰色“阅读ADV”；图片设置、翻页和阅读位置继续可用。
- 旧ADV继续可读，不因新版本自动重写；不合格生成保留已有合格作品。

### 4.4 “生成多少显示多少”

这是已明确批准的体验，不是把未完成JSON当成完整内容。

正常返回较少但完整、校验通过且保存成功的作品，就显示实际已有数量；不为未凑齐建议数量封锁阅读，也不自动伪造缺项。真正请求失败、校验失败、截断或保存失败仍如实提示。

两个成功、一个失败时保留已成功部分，只让明确重试处理未完成部分。没有有效内容或没有保存不能显示成功。原单篇字段、安全、历史证据和最大容量要求不因此取消。

### 4.5 HEART、基础语言、四季和独立阅读状态

角色互动保持独立入口，位于成就库之前。HEART基础语言与初始化解耦，可以独立生成／重生成；缺少某类语言或局部失败不得挡住进入角色互动或其他HEART子模块。

基础语言不与春夏秋冬绑在一次生成里。有哪类就用哪类；同类多句不应反复固定第一句。**生日祝福不作必须生成项，默认不补；用户单独选择生日类时才生成。**不能因没有曾经说过的真实生日台词而阻塞人设对话。

四季、后日谈分别生成；每季Voice与Scenario分开保存，一半失败不拖垮另一半。每篇逐篇翻阅，不全铺；按季节和角色人设变化环境，不只复述旧Mxxx。保持现有正文内容要求，不借UI重做Prompt。

对白与旁白正确分开；`{{char}}`和`{{user}}`各自台词使用对应头像、姓名、气泡，不把user台词默认归char，不拆坏原段落。

独立后日谈、日常一格、萤火虫页面只影响当前阅读路由，不覆盖普通HEART的标签、季节和篇章选择。生成、合并、保存、延期写回完成后也不得串页或抢回用户已离开的页面。

### 4.6 萤火虫栖息地与日常一格

r84.20明确：漫画以实际panels数组数量/顺序为准，1格单幅，多格单列纵向排列，4格即上下四格；不使用左右两列或2×2替代。格内人物左右位置不受影响。现有合法分镜数量范围不扩展，不重写原panels台词/动作，不自动纠正用户已手改的旧画面提示。

一颗光是一段当场展开的双方会话，`{{char}}`不经意流露本音；`{{user}}`仅在该模块既有许可范围作极短、中性、非正史回应。不能变成整段单向独白。

五色语义保持：粉心恋爱、蓝心恋爱烦恼、黄心朋友、白心个性／お楽しみ、红心对`{{user}}`的直白渴望；黄色／白色要承载角色工作、兴趣、朋友和生活，不全部只谈user。原有内容边界保留。

旧光累积分页，不为新增淘汰旧光；原批次5～6颗的目标与单页最多6颗保留，但采用§4.4的有效成果可读原则，不因少于建议数封锁已保存会话；达到安全上限停止追加。旧式一句话光点兼容，升级由用户主动发起。

日常一格独立生成、独立访问；图像和文字应对应。图像prompt不带对白框、水印和文字，台词由本地显示，单图／分格含义不得偷换。

### 4.7 房间、物品与私人终端

房间为本地SVG＋CSS的内装和人物呈现；结合世界书明确设定、角色卡、时代、身份、职业与生活方式变化。房中char形象不固定，不用档案头像替代，不铺绿幕，不让所有char共用一种外形。未知明确客观值不猜。

房间分类、房间介绍、物品介绍、char的话、可观察与收纳等现有层级清楚可见，不为美化隐藏功能。

打开房间读取已有内容；“今日生活”的新生成由原手动按钮明确触发，不因进房间自动请求。已有节点的本地时间展示与日历剧情日期是两件事，不把日历修复顺带改成所有模块同一时钟。

房间／物品／出行的后续追加区分“同步新历史”和“按人设扩展生活内容”。后者允许合理新物件、地点或重访当下对白，不再只接受记忆来源；但不能把推演写成两人已经共同经历过。

私人终端按人物世界观展示设备外壳、状态栏、壁纸、App及层级，不强套同一现代手机或真实商业App。普通笔记、未发送草稿、合理联系人等可作人设推演；不冒充真实历史。一条无效不因数量不齐让整个App有效内容消失。此许可不等于关闭其他字段校验或重复生成日历的约定／To-Do。

### 4.8 出行路线、明信片、纪念页与邮箱

远方明信片／纪念页是**模型写信与结构化设计、本地安全绘制**。正文不模板化；插画不能只把自然语言说明正则匹配回几张固定景观后称为自由设计。

模型可以在局部明信片原请求内选择受限图元、层次、位置、大小、数量和色板枚举；不提供任意HTML／SVG字符串、path、CSS、URL、class、字体地址或执行代码。

按实际批准的名单有24种（以前称25种是计数错误，不擅自补第25种）：
`peak hill shore water field tree grove reed flower leaf pavilion cabin tower bridge gate wall boat orb cloud bird rain snow star lantern`。

最多20条设计元素、8 KiB UTF-8设计数据，固定三层；渲染预算按复合／重复展开后的真实节点计算，当前交付规则最多90个SVG子节点并纳入背景成本。超限整份回退，不中途截成半张图。正文通过原校验则保留，不自动追加第二次收费请求。

已有山、地面物、桥、水与船的承托、衔接和定位能力保留，但不以写实或美术质量拒绝可安全显示的设计。简笔画、单图元、重叠、不对称、比例奇怪都可以显示；boat没有同层或远层water/shore时也允许，使用既有有限层基线，不替模型补水。

这次不增加图元、人物画法或重做插画风格。没有设计的亭子、船、水或天气不擅自添加。深色可辨，正式样式和图注不遮挡要求属于程序正确显示，不是对{{char}}的美术评分。结构／支持能力／资源不满足才回退可选画面，合格信件正文仍保留。

新设计随信保存，重开一致；邮箱冻结同一份正文和设计，路线后来变化不改已收藏快照。`keepsake`／`postcard`并存时使用同一有效来源。

相同完整信件换ID／标题／设计不重复收录；同地点真正不同正文、落款或历史依据保留。不模糊删除“意思相近”的旧信；已有重复记录不因此批量删除。

没有新设计字段的旧信走其原绘制路径，包括原`picturePlan`、随机种子、文字和图像。不批量重画、迁移、删除旧模板或叠两套完整实现。新设计失败可以旧简洁版兜底。当前不因本轮文档整理新增“重画旧信”按钮；今后若做必须明确请求、保旧稿、预览后确认替换。

### 4.9 两个人的日历、页角随笔和节日贺卡

只收录角色真正会圈出的约会、接人、出行、生日／纪念日、明确安排、承诺兑现等，不把每段带日期剧情变成事件大纲。区分已发生、约定未发生、世界设定及未定日期。

**默认日期按剧情，不按设备显示时间、生成时间或用户翻看的月份。**没有可核对剧情日期保持待定；只有月日不猜年份。手动翻月只改变阅读，不改变剧情日期。绑定真实事件的随笔保留对应事件日期。

每个日期、无年份纪念日及待定页的草稿、备注、To-Do、完成状态和页角内容独立；不能全部日期共用一个`dayPages`内容，也不把旧共用稿复制到每一天。

页角“此刻心情”可以按人设自由书写，不要求先找到Mxxx完整锚点，也不强制通过后仍只用本地词库拼句；引用真实过去的部分继续要求依据。不能借此升级关系或伪造承诺。

日历贺卡的既有受控图文、纯文字、纯画、纯书写／书法和极简形态保留；不能把纯画／纯书写当缺字段的次等兜底。具体未在本轮核对的实现不凭印象宣称已完成；来源窄修不改贺卡Prompt。

### 4.10 ENDING、告白、关系与成就

真实告白回看必须有档案证据，角色头像与逐句播放器保持；未来ENDING不能冒充已发生告白。未来终章、后日谈与历史告白分开。

**ENDING和后日谈页面不再显示角色互动相通／跳转模块**。不要因移除入口删除HEART共用能力或损坏后日谈独立保存。

明确初始关系可支持相应亲密称呼；未有依据的关系升级不得通过。成就解锁继续要求真实memory ID与anchor，未解锁只显示原条件／提示。虚构作品不能自动解锁历史成就。

### 4.11 蝴蝶效应、前世今生和时空回响

蝴蝶效应是明确平行世界模拟，主轴有真实档案基础，外延不反写Mxxx。保留用户认可的第一人称人生独白、现世对照、自省和末端汇合的叙事方向，不能把新建流程误走旧恢复而直接MAIN→终点跳过普通分歧。

UI、颜色、布局、动画和既有Ω页面不因内容修复重做；旧成功计划／提示槽保持精确续写，不通过强制补节点偷偷重复请求。不要为char凭空新增前任、与别人恋爱／婚姻／组建家庭的情节。前世今生保留原独立模式，不借本次文档整理调整其Prompt。

**时空回响保留原来的UI**，入口在私人终端，终端未生成也可独立进入。它是世界观适配的双端时间与媒介番外，不硬添现代电话或魔法，不强制婚姻、悲剧、固定篇幅或章节数；允许既有许可的双方虚构对白，不写回真实历史。追加不覆盖旧篇。

`timeJourney`（错位相逢／错时相逢／时空旅行者的妻子）已撤回活动模块；不恢复入口、Prompt、专属UI或书签。旧数据不活跃保留，不主动清除，也不迁成时空回响或蝴蝶效应。相关退役测试失败不意味着应恢复被用户删除的功能。

多人卡适配此前暂停；未获得本轮继续授权，不顺带开发。

### 4.12 角色印象曲

入口保持内容页既有角色印象曲功能，可为角色或用户选定的真实档案事件创作。输出歌名、演唱者／声部、人声说明、曲风和完整原创歌词；提供分段标记及可分别复制的曲风／歌词，便于外部音乐工具使用，但不在界面／导出中提特定音乐平台名。

**阅读模式／创作格式**两个视图：阅读强调歌名、演唱者、作者信息与歌词；创作格式保留英文风格提示、分段标记、复制及导出。切换不请求模型、不改正文、不写档案、不丢临时创作方向。没有作者信息不擅自把char当词作者。

语言：中文、日语、英语、韩语、自定义。自定义由用户输入西班牙语、粤语等，不附额外说明性注释。演唱：角色独唱、双人合唱、旁观者演唱、群像；群像允许已有设定人物轮唱／应答／合唱，不凭空发明固定重要人物。

明确操作每次创作一首并追加，失败保旧歌，不自动多请求。生成的是歌曲文本而不是音频；歌词不反写为已发生Mxxx。原三种演唱方式及原有语言Prompt不能因新增群像／语言被随意重写。

### 4.13 图片设置、NAI格式与实图回填

**r84.20用户新增明确规则：**不是只修日常一格。相簿、ADV EVENT、日常一格的首次生图字段生成与主动重新构思，共用同一视觉提炼核心；模块只补自己的单幅CG／漫画类型规则，不重写其他剧情Prompt。稳定外貌仅提取明确可见的固定特征；当前衣着、动作、表情来自当前事件/分镜，不从性格、习惯、条件反应、口癖和关系履历自动推导。“平时总带着笑”“被调侃会红耳朵”“笑起来眼睛弯成月牙”等不作为默认生图指令。当前画面没笑就不补笑；当前画面确实写明的笑/哭/脸红只在该场景描述，不混作稳定外貌。

首次请求可返回可选的受限视觉草稿；它不是第二次收费抽取，更不是新的剧情必填字段。缺失/无效时仍保留原领域校验通过的内容。程序没有新绘图结果前不覆盖原图；已保存实图及用户手写内容优先。不能以改善自动构思为名重写用户编辑、重开还原、保存外貌、切换格式或确认发送等操作；旧手填文字即使包含情绪也仍走原有用户确认合同，不应用自动摘录规则。

相簿／ADV EVENT／日常一格的图片设置保留现有操作：查看对应回忆、可编辑场景、双方外貌／场景标签、发送预览、重新构思／提取外貌、确认绘图与关闭。不能为了UI预览简化而丢掉真实控件或改变动作含义。

CG主角是这条回忆中人物动作、场景和环境，同时保持char与user的明确可见外貌，不能退化成只有外貌肖像。外貌从受控人设提取，不把性格、关系履历和行为习惯整段当稳定外貌；未知不猜，先给用户编辑确认。

**NAI 5／4.5是提示词生成写法的选择，不是实际生图模型选择，也不是用户能否绘图的许可。**用户确认发送时，不能仅因中文、自然语言、Tag列表或混合写法与当前选择不一致而拦截。

NAI 5生成目标：主动生成／重新构思时采用原先认可的连贯自然场景描述，允许自然中文，不额外强制英文优先；完整英文句子也属于自然语言。模型未按目标写成自然语言属于生成格式未达预期，不因此拒绝用户后续主动提交可用文字。

NAI 4.5生成目标：主动生成／重新构思时要求场景、人物可见外貌及对应完整提示写成英文Danbooru式标签，不将中文人设整段照抄当成已完成的英文Tag。这是对自动构思产物的要求，**不禁止用户编辑、保留或发送中文外貌／自然语言场景**；也不能强迫用户再次付费转换才能绘图。

格式检查若保留，只作轻量、非阻断提示；不得以格式不符使图片设置、发送预览或最终绘图请求失败，不新增必须通过的第二次格式确认。用户明确确认绘图后，按既有安全的字段组装发送与预览一致的文本，不静默删中文、换内容、遗漏人物或改实际后端模型。通用提示缺失时不能仅以“不是完整英文Tag”为由封锁；应沿既有合法数据合同组装已确认的场景与人物材料。

模型未按所选格式生成时，可保留能安全使用的草稿并提示；不能仅因生图字段写法不符，让相簿、ADV EVENT或日常一格的其余有效内容整批失败。不会自动追加收费转换或重试。图像效果及下游真实报错与本地格式提示分别记录，不保证任何引擎对任何语言都有同样效果。

切换格式不自动转换、不发请求、不只改格式标签就称转换完成。明确构思／提取沿用获准文本请求，结果为可核对草稿；转换失败保留当前稿，提供原有还原草稿能力，未确认不覆盖旧提示和实图。

仍保留必要的数据类型与非空检查、既有长度／资源上限、文本清洗与转义、公开接口能力与配置、并发／取消／费用确认，以及图片结果和写入目标保护；不能把取消语言／句式门槛扩大成接受任意网页代码、外链、执行指令或跨聊天写入。

云酒馆／TT实图成功后写回对应原条目并替换抽象CG，重开保留，不能只进第三方图库。大图在插件内查看，保留适屏、原尺寸与返回；失败不自动再收费，不通过扫描图库猜匹配来补救。

### 4.14 悬浮入口及诊断

悬浮头像默认char，可选user或关闭；显示正在看的档案归属头像，历史档案缺user头像不借当前其他Persona。沿用既有关闭／重开书签。

插件主界面有默认折叠的故障排查，用户主动查看／导出才执行；runtime加载失败保留后备入口。诊断只给安全计数、阶段、错误码、版本和能力，不泄漏资料正文或密钥。性能诊断保持零解压，读取摘要0来源不等于档案0条。

---

## 5. 生成、连接、性能和写回

### 5.1 请求与费用

使用用户明确选择的副API／Connection Manager配置，不偷偷跟随主聊天切模型。保留手动API入口、模型拉取与手动填写、流式开关及实际最大输出设置；桌面模型列表和手机选项必须在可见区域。

不读取、复制宿主或第三方的现成API Key；不回显、记录或导出用户Key。用户本轮明确授权手动连接默认保存：自行输入的Key只在插件独立本机库AES-GCM保存，普通设置只存opaque引用，授权传输时临时取用；不退回明文落盘。诊断仅显示是否存在，不含值。

建档模型容量未知时不得以共享32,000输入token或96,000字符拒绝请求。保留完整实际发送文本的计量；宿主tokenizer只是估算，不当作副API的精确分词器。当前不新增上下文字段或强制用户填写；未知能力由用户选定的提供方实际判定，错误按原层次脱敏显示，不自动再收费。来源、响应、存储等独立资源边界不因此取消。

最大输出未填写时默认60,000 tokens，不是插件上限；合法用户填写值在设置、预算与最终请求中保持原值，不偷偷降到3k／3.8k或60000。既有合法保存值不被升级默认覆盖。错误区分配置额度、本次实际请求与提供方能力；未知模型能力保持未知，不保证所有模型接受用户额度。响应容量、12MB存储和取消／超时等保护独立保留。

provider名额上限2，原限流策略可降1；逻辑主任务上限5。准备、token计数、解析、校验和保存不占provider位；直接建档和内部子请求不得绕过协调器。成功、失败、取消和超时均恰好释放名额。

公共文本分段默认不自动重试。仅代码显式许可的原有可重试路径最多一次；认证、配置、无效请求、禁用词、取消、未知连接和本地超时不能自动重试。不同正常分段仍各自请求，不能用“单次生成”掩盖实际次数。

请求必须有超时和取消保护。**旧CURRENT与代码的超时数值冲突见§7.4，不能用文档整理擅自更改运行参数。**不凭总耗时断言代理两分钟切断；排队、节流与发送耗时分开。

### 5.2 页面离开、迟到结果与原子写入

关闭档案室、切换普通阅读页或切换酒馆聊天不拦截宿主导航，也不无条件撤销已授权后台任务。生成不能锁住聊天窗口。

任务固定发起时的完整角色作用域、聊天和档案版本，不重新绑定新窗口。临时切聊的结果只能按原不可变快照／队列等待回原作用域验证；目标正文、版本或归属实质变化时拒绝写回。不以“允许后台”解释为可跨聊天写入。

正式提交重读状态并通过`archiveRevision` CAS、origin/revision fence、必要聊天指纹和消息范围检查；copy-on-write不污染原有效数据。同聊天同模式旧结果不能覆盖新档案、新作品或用户修改。

延迟队列按作用域single-flight，较新结果优先；临时存储失败按原有有界策略处理，永久stale丢弃。Phone草稿和CG／日常一格保持目标条目级身份／旧图指纹，不覆盖整份旧缓存。

来源读取与后台生成的生命周期不同：**正在扫描外部来源时改变聊天／角色／Persona／来源，应丢弃旧扫描结果**；不拿后台结果允许暂存的规则来放松来源归属。

在途请求不是跨刷新后台服务。新版本只对已经成功确认本机保存的草稿提供恢复；未保存或仍在飞行中的数据仍可能因刷新/关闭App而丢失。旧版仅本页草稿须先导出再升级；不能宣称能找回从未落盘的旧数据，不要求清站点数据。

### 5.3 同任务重试与错误

同一次建档使用开始时捕获的已选来源和受控背景；后台数据库／世界书更新不改变本次已确认输入，也不重做成功块。真实修改正文、读取范围、来源选择或生成配置仍按原校验停止不匹配续写。

旧草稿没有新材料快照时，不把它自动宽松升级；保持旧请求精确匹配。r84.22为真实旧格式草稿保留原局部成功请求的精确匹配；完成旧当前批后才规划原先未采用尾部。不能从普通按钮绕开身份校验。普通入口同任务继续；零成功或真实配置变化可明确另起新任务并保留旧草稿，已正式保存的来源不重发。正常局部成功不伪装成正式档案提交成功；失败横幅保留安全原因、成功数和手动重试入口。

合法JSON字符串内的`<span>`、`<tableEdit>`等是数据，不应整次误报HTML；实际HTML网页、HTML包裹JSON和明确HTML响应仍拒绝。保留原结构、领域校验、转义、输出容量及脱敏保护，不用删除HTML防护换成功率。

授权说明（本轮用户采纳前文窄修要求）：无效的可选明信片设计不牵连通过原校验的正文；合法JSON内歌曲等文字字段的标签字符继续作为惰性文本存取和转义，不获得执行／外部资源权限。本轮未修改歌曲或HTML响应识别代码。

r84.18只为新travel-map视觉要求新增独立版本合同；旧r84.17草稿仍用原局部视觉指令和精确请求身份，新结果也通过现行可解析／资源校验。不是给所有模式放宽requestHash，不因升级重复已成功的收费片段。


**r84.21手动API响应分流授权与实现：**请求设置不等于服务实际返回类型，不能只靠`stream=true && Content-Type包含event-stream`判断。仅在同一响应内做8192字节有界前缀识别并原样回放；普通JSON和受支持SSE复用原解析器，未知数据不无限搜索、不忽略畸形事件，HTML、结束状态、长度、取消、超时、归档证据与写回保护不放宽。不得为选择解析器再发收费请求，不改用户的stream设置。此修复不取消`RMT_RECOVERY_INPUT_CHANGED`身份保护、不保证列表端点可用。

### 5.4 启动与长聊天

普通启动只运行轻量bootstrap，不下载／解析完整runtime。用户主动打开档案室／完整设置，或明确启用且真正到期的原自动同步任务，才按现有边界加载runtime。

普通消息不能扫描长聊天、读取世界书、解压大缓存、枚举连接、打开IndexedDB或调用模型。自动同步仅在原轻量门槛／锁／检查点后进入runtime，不因保存开关而抢冷启动。

零解压诊断只读O(1)标量、版本、字符串长度，不Base64解码、遍历正文或序列化大缓存。前台操作避免同步大扫描或不必要重复读取。

12MB缓存上限按UTF-8字节覆盖压缩、无压缩、解压输出、销毁及延期写回路径；超限保留最后有效缓存。来源账本的独立限制不能混成12MB整个应用大小。

手机320／375／390／430px不横溢，主要触摸目标约44～46px；TT安全区默认关闭时保留用户既有edge-to-edge偏好，开启时适配顶部安全区和WebView异常。桌面不以巨型满屏卡片替代信息层级。

---

## 6. 安全、隐私及公开第三方边界

模型输出只作为受限结构化数据；动态文本转义或`textContent`。模型不获得HTML／CSS／JS执行权限、任意URL、class、缓存键、对象路径、模式路由、目标聊天或写入权限。

明信片的受限几何枚举／数值属于§4.8已批准的数据合同例外，不是开放原始SVG路径或代码执行。页面样式、节点、资源来源和写入位置继续由本地代码控制。

错误、日志、toast、报告不泄漏Prompt、聊天正文、角色卡、Persona、世界书、档案、Authorization、Cookie、密钥或响应正文；只使用安全固定文案及计数。发现API返回报错文本也不能直接显示其可能包含的秘密。

第三方只读来源主动调用、身份绑定、版本门禁、超时／取消及失败保旧数据必须保留；不以DOM按钮、私有指令或后端枚举绕过缺失公开接口。不放宽归属以解决0来源。

CG公共接口：9月14日CURRENT已记录作者正式公开的`STBaiBaiImage` API v1及用户许可。这个记录替代r42.7里“没有公开生图API、禁止任何自动调用”的当时状态；**禁止私有桥接仍永久有效**。调用前核对正式API版本、方法和能力；不通过本次文档维护重新授权任何旧私有桥接。

只发送用户已确认的场景、双方可见外貌及原受控图库分组。已获准的公开角色库只在API v1与`characterLibrary`能力成立时使用`getCharacters()`，不得读取私有角色库或密钥。

CG结果仅接收已获准的同宿主本地图片引用；HTTP(S)严格同源，TT按既有精确`tauri://localhost`和`/user/images/`路径限制。不能因为自定义协议放开任意外部主机／端口／凭据。保存失败不自动重生图、不扫描图库迁图。

EverMind等既有远程接口保持HTTPS要求，HTTP仅允许经过解析确认的loopback。该条不新增或启用任何未授权连接。

---

## 7. 版本、实测记录和未决事项（不是永久产品规格）

### 7.0 最新候选：r84.31

- manifest `0.8.107`；build/cache `0.8.107-tt-cg-r84.31-input-budget-setting`。
- 单根 `tokimemo-main/`；不覆盖r84.30/r84.29/r84.28、不写GitHub。
- 本轮范围见§0；实际回执、失败清单、版本/CRC/SHA-256以包外本轮最终报告为准。
- 候选不是全量全绿或TT真机已验收；全量2511项中160项失败与r84.30基线完全同批，新增8项全过。
- 模型首次返回半截正文、timeEcho约38分钟超时仍为待查项，不在本轮修复范围。

### 7.0.30 历史候选：r84.30

- manifest `0.8.106`；build/cache `0.8.106-tt-cg-r84.30-overflow-outlet-preflight`。
- 单根 `tokimemo-main/`；不覆盖r84.29/r84.28、不写GitHub。
- 容量出口/建档保存分类/生成准备补码；全量2503项中160项失败与r84.29基线完全同批并已分类，新增31项全过。

### 7.0.29 历史候选：r84.29

- manifest `0.8.105`；build/cache `0.8.105-tt-cg-r84.29-monotonic-recovery`。
- 单根 `tokimemo-main/`；不覆盖r84.28、不写GitHub。
- 续写覆盖旧进度修复；Kimi 独立回执已取得（2026-09-20，包外 `Hearttrace-r84.29-independent-review.md`），结论为模拟边界内修复成立、不放行公开下载。
- 降级风险：含 `retainedPartials` 的journal在 ≤r84.28 旧包上不可续接（数据不丢失，换回新版可读）；升级后不建议回退。

### 7.0.28 历史候选：r84.28

- manifest `0.8.104`；build/cache `0.8.104-tt-cg-r84.28-task-recovery`。
- 上传包 `hearttrace-upload.zip`，单根 `tokimemo-main/`；版本归档 `Hearttrace-r84.28-task-recovery-full.zip`；不覆盖r84.27、不写GitHub。
- 范围见§0历史段；实际三轮回执、失败清单、版本/CRC/SHA-256以包外 `Hearttrace-r84.28-validation.md` / `Hearttrace-r84.28-review-evidence.zip` 为准。
- 候选不是全量全绿、独立审查通过或TT真机已验收；无原journal不确定用户现场具体比较项，不承诺所有未知旧格式无条件续写。

### 7.0.27 历史候选：r84.27

- manifest `0.8.103`；build/cache `0.8.103-tt-cg-r84.27-tt-draft-durability`。
- 上传包 `hearttrace-upload.zip`，单根 `tokimemo-main/`；版本归档 `Hearttrace-r84.27-tt-draft-durability-full.zip`。不覆盖r84.26、不自动远端写入。
- 核心修复和范围见§0；本轮实际测试、差异、完整失败清单、ZIP/CRC/SHA-256以包外 `Hearttrace-r84.27-validation.md` / `Hearttrace-r84.27-review-evidence.zip` 为准。
- 不能把模拟TT-shaped能力、模拟存储/模型和浏览器DOM用例标成TT真机；专用安全扫描与自复核分开记录。

### 7.0.26 历史候选：r84.26

- manifest `0.8.102`；build/cache `0.8.102-tt-cg-r84.26-draft-recovery`；上传名 `hearttrace-upload.zip`，内部单根 `tokimemo-main/`，版本归档包另存。
- 源码仅修改档案恢复库、repository恢复读写入口、档案页恢复区和恢复按钮；其余创作/请求/证据/存储/Key模块不改。原有测试文件不改、不删、不跳过。
- 本轮真实r84.25基线重新跑完整1887项：1726通过、161失败。最终新包三轮实际计数、完整失败对照、syntax、两次build、文件CRC与SHA见独立报告，不预先将未执行项目写成通过。
- 三轮必须实跑旧bundle创建41/63草稿、新bundle首次打开可见、剩余22段；旧r84.24导出到新包文件导入；读取失败重试、页面成果保存、导出和迟到页面保护。
- 新的本机恢复入口不等于已经找回用户丢失的原41段。原生IndexedDB/真实ST、TT、云酒馆/手机及真实渠道仍须单列实测状态。

#### r84.26 最终交付回执（安装ZIP封包后补入同一项目CURRENT）

- 三轮实际完成：定向每轮269/269；完整1911项1750通过161失败；全部JS/MJS每轮296/296；实际Chromium每轮76/76；连续两次build一致，第三轮来自最终安装ZIP重解压。
- 本轮重跑r84.25基线1887项1726通过161失败；新包新增失败0，原测试文件无改删跳过。94个既有命名Prompt函数和原正则全部保持。新export逃生出口的两处预检阻断已修复，不用删除旧测试放行。
- ZIP：2,717,309字节、353文件、单根tokimemo-main/，CRC全部通过。SHA-256 `016975a76c465e0a3d872c506a3d35995dd07c959dfbbfdd8d6777e26bdd3f5b`；bundle SHA-256 `98a1e406db19e15c10ca8f0e34e19f1ac65fa9681a042e08ddc5f9f2755796b7`。上传包与版本归档包字节相同。
- 包内CURRENT是封包时的r84.26需求快照，最终测试及ZIP哈希由本段与独立报告补录；没有为了补录ZIP自身哈希再修改已验收安装包。
- 原生IndexedDB事务未完成（测试站点导航被环境策略阻止），真实ST/TT/手机/模型渠道未执行；三轮浏览器使用真实bundle/DOM、合成宿主/模型/存储。专用Codex Security动作不可调，扫描未执行；差异自复核未见未处理新增High/Medium，独立回执未取得。
- 完整证据：Hearttrace-r84.26-review-evidence.zip；报告：Hearttrace-r84.26-validation.md。GitHub无写入/自动发布，用户原包与r84.25保留。

### 7.0.25 历史候选：r84.25

- manifest `0.8.101`；build/cache `0.8.101-tt-cg-r84.25-recovery-models`。
- 完整安装ZIP 2,699,680字节、351文件，单根`tokimemo-main/`；CRC全部通过。上传名`hearttrace-upload.zip`，另存版本归档，二者逐字节相同。
- ZIP SHA-256：`9da8811267252af41178f4522a46023adce881b030f1c4f1869e2a02b00132f6`。
- bundle SHA-256：`92555062e11ec19effc86f424ece120a3d5ed7dcdfe4808abe070d63009a99a1`。
- 三轮每轮定向245/245、完整1887项1726通过161失败、语法295/295、浏览器57/57、两次构建一致。第三轮从最终ZIP新解压，构建后全文件仍匹配ZIP。
- 基线本轮实际重跑1800项1646通过154失败；新增5历史冻结断言和2旧错误码断言，逐项证据保留，未修改旧测试/删除/跳过文件，不称全绿。
- 94个原Prompt函数字节一致、原正则全保留；完整差异自复核及故障修正日志见`Hearttrace-r84.25-review-evidence.zip`。
- 原生IndexedDB/浏览器CryptoKey事务未完成（环境策略拦截）；真实提供方、ST/TT/云酒馆、iPhone/Android未执行。Node原生WebCrypto与显式存储模拟分开记录。
- Codex Security专用动作不可调，专用扫描未执行；自复核未发现未处理新增High/Medium，独立回执未取得。GitHub无写入或发布。

### 7.0A 历史候选：r84.24（真实r84.23基础上的用户设置窄修）

- 基线与权限见文件首段；本轮已重新运行基线完整1758项，1612通过、146失败，零跳过／取消，不继承旧数字。
- manifest `0.8.100`；build/cache `0.8.100-tt-cg-r84.24-user-settings`。独立交付目录中的`hearttrace-upload.zip`与`Hearttrace-r84.24-user-settings-full.zip`为同字节完整包，不覆盖旧同名包。
- 新增纯生产模块`core/outputBudget.js`；改动只围绕额度规范化、标签输入与身份、设置控件、世界书弹窗及必要诊断文案。没有新网络入口或第三方依赖。
- 新增42项定向链路用例；预检已实跑，覆盖1/1024/16384/60000/60001/120000/1000000额度、Profile与手动JSON/SSE、360个标签保存、超过500条和256k字符扫描、长标签跨片段、实际归档保存和续批、身份改变拒绝旧结果、零标签。保留全部旧tests/tools文件及预期。
- 实际Chromium检查真实设置按钮、523种合成标签全选／反选／保存、输入额度到模拟请求、世界书500本滚到末尾仍能关闭、320/375/390/430/1280宽度与深浅主题；模型／宿主／存储为模拟。预检原生IndexedDB打开被SecurityError阻止，未绕过策略，不称事务验证。
- 原隔离器对照真实r84.23做12,000组确定性合成变体，验证旧隔离结果、新保留集合补集与原片段投影一致；新增回调不改变tokenizer或嵌套状态机语义。不是实际用户正文的验收。
- 预检中两项旧浮窗夹具因空标签面板无谓创建DocumentFragment而报错，改为无标签时不分配子树后14项浮窗原回归全通过；没有改旧夹具。新测试开发的模拟Profile能力声明、扫描计时、片段序号和模糊测试未知标签预期错误已修正，初始失败日志保留，不能当成三轮回执。
- 最终三轮精确计数、8项新增历史源码／函数冻结失败的归因、原146项、CRC／SHA及所有未执行项见包外验证报告和证据。若实跑结果不同必须以实际日志为准；全文测试不是全绿，不能只凭“哈希冻结”放行。
- GitHub未发生写入，未调用真实模型或自动发布。三轮都是本地自复核，不是三个独立reviewer或手机真机验收。

### 7.0.23 历史候选：r84.23（真实r84.22基础上的模型预算窄修）

- 基线ZIP：`a83315c3d9acc62ef91303133713b039fa1862e52eec154c9789828154832933`；本轮重新实跑1731项，1585通过／146失败，未取旧报告数字充作重跑。
- 候选manifest `0.8.99`；build/cache `0.8.99-tt-cg-r84.23-model-budget`；新完整上传包保存在独立交付目录，basename仍为`hearttrace-upload.zip`，另有版本归档包。旧同名r84.22包不覆盖。
- 生产变更仅`archive/requestBudget.js`、`archive/importRecovery.js`、`generation/client.js`建档预算分支及`core/text.js`对应提示，另有manifest/index的新版本身份和确定性生成的dist；无生产模块增删。
- 新27项＋既有相关74项＝101项定向检查；完整tests＋tools 1758项，1612通过／146失败，0跳过／0取消。全部284个JS/MJS语法、119模块连续两次构建及逐文件范围核对均作为每轮实际命令执行；逐轮退出码／路径／时间见包外回执。
- 全量146项与本轮实跑r84.22的失败集合、首个断点相同，无新增失败：128历史源码哈希、6历史固定版本、5退役timeJourney、2旧tokenizer次数、2旧Persona提交、1旧来源截取、1旅行函数尾部注释、1CG元数据对象比较。不能把这些旧失败说成已修复，也不把“没有新增失败”冒充完整全绿。
- 94个既有Prompt函数、所有原正则字面量与动态RegExp、所有原模板字符串逐字节一致；全部UI／modes与CG实现不变，bootstrap仅前两行身份变化。实际完整输入的计量路径变化不等于创作要求改写。
- 第一轮检查固定上限修复的真实按钮／发送／验证／保存链；第二轮独立物理副本对照r84.22；第三轮最终ZIP新解压重跑同口径测试、全部语法、两次构建及CRC／SHA。最终ZIP哈希在包外校验文件，不内嵌自引用哈希。
- 一轮预复核发现旧草稿强制进入建档预算后可能新增tokenizer等待，已在建档分支尊重原skipTokenCount并重新开始最终三轮；旧恢复身份和校验未改变。预复核／工具超时日志保留，但不替代最终三轮。
- 本地浏览器每轮新高token场景8项＋原分批场景8项；Chromium实际DOM，模型、tokenizer、host及后端模拟。原生IndexedDB、真实ST／TT／云酒馆、真实模型和第三方共装、iPhone／Android真机未执行／未验收；不从截图推断本地上限修复后提供方必然接受。
- 专用Codex Security扫描未执行（本轮发现已安装但无可调用动作）；仅本次差异安全自复核，独立reviewer回执未取得。无真实模型费用，无GitHub写入或自动发布。

### 7.0.22 历史候选：r84.22（当轮从真实r84.21重新实施）

- 唯一基线：`Hearttrace-r84.21-response-routing-full.zip`；2,539,378字节／327文件；SHA-256 `614fa70f1ef9410f9b7ed94513ae2ffd0f795725391e9e30197c2f763c10752d`。本轮两次实际全量复跑均为1693项1569通过124失败，不取用撤回报告数字。
- 新manifest `0.8.98`；index/build/cache `0.8.98-tt-cg-r84.22-archive-batches`；完整上传包`hearttrace-upload.zip`与版本归档`Hearttrace-r84.22-archive-batches-full.zip`为同一份新代码封包。最终字节数、336文件清单、CRC、ZIP SHA-256见包外`release-integrity.json`／验证报告（ZIP不能内嵌自己的最终SHA）。
- runtime共119个可达模块，相对基线增加`archive/importBatches.js`、`archive/requestBudget.js`两个生产模块，未删旧模块；bundle SHA-256 `6ab42ac1fad718444f8fb8366fdbcfd723180d7428417ab473363915c0c56681`，source SHA `5c4293ec7e4750d93f0899d3013105412017931e863f8257895f1161800ade00`。
- 本轮修改11个原文件（含manifest、index、dist），新增2个生产模块及6个测试／测试工具，另纳入本份CURRENT。327个原文件无删除，旧tests/tools文件零修改；差异清单和unified diff随证据交付。
- 第一轮：工作目录，新修复38项＋既有相关36项＝74/74；完整1731项1585通过146失败；全部283个JS/MJS语法通过；连续两次bundle重建与包内bundle一致；实际Chromium DOM 8/8（模拟宿主／provider／tokenizer／存储）。
- 第二轮：独立物理副本，重复同口径74/74、完整1731项1585通过146失败、语法283/283、两次重建一致、DOM 8/8。只读对照真实基线：94个既有Prompt函数原文一致，全部原正则字面量及new RegExp表达式一致，2154个既有具名函数一致；18个有改动函数均属于建档及必要提示连接；bootstrap除版本前两行外字节一致。
- 第三轮：实际最终ZIP全新解压后重跑74/74、完整1731项1585通过146失败、全部JS/MJS 283/283、连续两次bundle一致、DOM 8/8；版本、单根、完整清单、CRC、SHA及解压前后逐文件哈希核对。实际目录与命令时间见包外round3/review.json；封包后无生产代码改动。
- 完整测试**不是全绿**：146＝124基线已有＋22新增，全部逐项记录名称、文件行号、抛错点、原因和原始TAP。124原失败首个断点相同：112历史哈希、5历史固定版本、5已退役timeJourney夹具结构、1旅行函数字符串尾部注释、1既有CG元数据形状。旧失败没有借本轮修复而消失，不宣称这些旧模块全面验收。
- 22新增：16旧整文件哈希冻结、1新版本与0.8.97冻结冲突、2个tests/tools重复tokenizer次数断言（完整抽取与简介各计量一次，provider仍2次）、2个tests/tools重复Persona夹具（计量途中换Persona却期待旧身份提交，现按新要求停止且0请求）、1个旧完整来源应<=240k断言（新要求改为完整捕获后分批）。无删除／跳过／放宽旧断言；不存在本轮未归因的新行为失败。不是仅凭哈希类别自动判安全，另有实际范围和行为审计。
- 每轮三个问题的本地结论：新修复在所测合成UI链路可用；既有其他Prompt未擅自改写；发现的两处实现回归（已完成结果纯显示名改动的save-only兼容、简介只读保存新增状态字段）已修复并复跑，未放宽原保护。真实渠道与真机不在这个结论范围。
- 实际浏览器为Chromium 144.0.7559.96，运行新dist并通过DOM按钮点击；610,000字符／610来源，三次显式点击、逐次销毁页面再读模拟持久后端、唯一来源无重发，用户输出12000保持。1280×900／390×844、深浅主题均截屏和布局检查。第二轮该合成三批耗时约515／321／226ms，仅为无真实网络的桌面模拟，不是手机性能承诺。
- 存储证据：模拟事务失败、保存重试、另一Node进程读写磁盘fixture、浏览器销毁页面后回读模拟后端均执行；不等于原生IndexedDB。原生浏览器测试首次被环境URLBlocklist（net::ERR_BLOCKED_BY_ADMINISTRATOR）拦截，未更改／绕过策略，原失败日志保留。
- 未执行：原生IndexedDB事务／配额；真实ST、TT、云酒馆；真实火山／其他模型上下文与网关；真实SP／千千结／柏宝书共装；iPhone／Android真机和实际长聊天性能；专用安全扫描、独立reviewer回执。三轮都是本地自复核，不冒充独立审查。无真实模型费用、无GitHub写入。
- 约8万字符的用户现场仍待其脱敏诊断与渠道确认；已证实的是本地合成路径及完整请求计量，不给“用户太多／模型不行／数据库冲突”的无证据归因。单个固定背景本身超既有预算时仍在发送前安全失败并保留来源，不直接扩容。

### 7.0.21 历史候选：r84.21（上轮记录；本轮唯一实物基线）

- 唯一开发基线：真实r84.20 `Hearttrace-r84.20-visual-authoring-full.zip`，SHA-256 `162c34e8cbc5255364d402b3cf969d5c64d96aa0e4235702f9e5853d6c2c07c9`。
- 候选manifest `0.8.97`，build/cache `0.8.97-tt-cg-r84.21-response-routing`；上传文件`r8421-release/hearttrace-upload.zip`，单根`tokimemo-main/`。
- ZIP 2,539,378字节，327文件，CRC与重建后逐文件SHA全部一致；SHA-256 `614fa70f1ef9410f9b7ed94513ae2ffd0f795725391e9e30197c2f763c10752d`。
- runtime 3,052,372字节，117模块，SHA-256 `ffa540ed82c12406c3d8771ee5062e5a9ee00d1d472d1f9d29952942999b0500`。
- 三轮新增121/121、相关394/394；全量1693项1569通过124失败（122基线已有+2上轮独立API哈希/固定版本断言），无跳过／取消。语法278/278，连续两次重建各轮一致且匹配最终包。
- 同口径基线本次实际复跑1572项1450通过122失败。未改已有测试预期，不将124失败说成全绿；新增行为测试均通过。
- 三轮实际HTTP回环12/12、Chromium响应16/16（基线另16组因果对照）；旧页面252组零差异；CG布局54、发送12、继续编辑12均通过。保存/重开通过实际归档入口与模拟持久存储边界验证，不等于原生IndexedDB或真机持久化。
- 原145个生产JS中144个逐字节一致，仅independentApi改动；无生产模块增删。94个Prompt相关函数原文一致；原747正则保留，增2处前缀/BOM判断；26动态RegExp一致。编辑/UI/CSS和其他模块不改。
- 专用Codex Security扫描未执行（已安装但无可调用动作）；本次差异自复核未发现尚未处理High/Medium。新独立回执未取得，GitHub无写入。
- 未执行：真实火山/其他API、真实SP/千千结/柏宝书共装、实际ST/TT/云酒馆、原生IndexedDB和iPhone/Android真机。无真实模型费用，不能保证用户此前所有错误都属这一根因。
- 产物：`Hearttrace-r84.21-validation.md`、`Hearttrace-r84.21-review-evidence.zip`、`Hearttrace-r84.21-file-checksums.tsv`及版本归档ZIP。

### 7.0.20 历史候选：r84.20（2026-09-18实际执行）

- 基线真实r84.19，ZIP SHA-256 `c86ca57a68e4ff1f9d9a2d13fa8746842fe579f9ad31a67fcad7e4bc465c6a10`，原包保留。
- manifest/build/cache：`0.8.96` / `0.8.96-tt-cg-r84.20-visual-authoring`。
- 上传文件`r8420-release/hearttrace-upload.zip`，版本归档`Hearttrace-r84.20-visual-authoring-full.zip`，两者同字节、根目录tokimemo-main。
- ZIP 2520447字节，323文件，CRC与解压重建后逐文件哈希全部通过；SHA-256 `162c34e8cbc5255364d402b3cf969d5c64d96aa0e4235702f9e5853d6c2c07c9`。
- runtime 117模块，3046510字节，SHA-256 `44ba5f13c742621a74426ef9c54d3f6095dca8148a1e0386bad28ca770481c0c`。
- 三轮均75/75新增行为范围测试、234/234既有相关回归；全量1572项1450通过122失败，0跳过0取消。基线1497项1392通过105失败；新增17项逐项核对为获准源码/函数SHA或旧build冻结断言，未改旧测试。原105包含cgImageCommit精确比较1项及退役r81TimeJourney 5项，不能一概称退役/全绿。
- 三轮均275/275语法，连续两次bundle确定性重建且与包内一致；252组旧页面零差异，54组CG编辑布局、12组确认/取消发送、12组继续编辑/格式/还原/重构/保存外貌，另起Node进程12写入+12读回全通过。实际browser DOM但宿主/模型/存储为模拟。
- 基线144生产JS中136同字节，8必要连接修改，1新增纯工具，0删除；UI/编辑器文件字节未动，其他剧情Prompt未改。94个prompt命名函数中90个原文一致，4个变化仅本次生图连接；另有buildCgAppearanceInstructions加入共用视觉指令。原737正则全保留，新10处局部规则，26动态RegExp不变。
- 不增加自动翻译或重试；保持既有调用数、输出上限。附加指令/可选草稿可能增加同次token用量，不承诺零消耗增量。
- 本地差异安全自检未发现未处理High/Medium；Codex Security无可调用动作未执行；本版独立审查未取得。原生IDB前置本地导航被策略阻止未执行；实际TT/云酒馆、真机、真实第三方共装/模型均未执行。无GitHub写入，无真实额度使用。
- 证据：Hearttrace-r84.20-validation.md、Hearttrace-r84.20-review-evidence.zip、Hearttrace-r84.20-file-checksums.tsv。此前r8419/r8418已核验记录以下仅作历史，不作为最新版本。

### 7.0.1 历史候选：r84.19（2026-09-18实际执行）

- 唯一基线：真实r84.18；SHA-256 `ea58f73e3d828c160ec9b2c5e869e4acaf4de5e232396f8c9e40b6bde0058767`，未覆盖。
- manifest `0.8.95`，build/cache `0.8.95-tt-cg-r84.19-cg-format-preference`。
- 上传包`r8419-release/hearttrace-upload.zip`；版本包`Hearttrace-r84.19-cg-format-preference-full.zip`；同字节、根目录`tokimemo-main/`。
- ZIP 2,494,834字节、318文件、CRC全通过；SHA-256 `c86ca57a68e4ff1f9d9a2d13fa8746842fe579f9ad31a67fcad7e4bc465c6a10`。
- runtime 116模块、3,035,271字节；SHA-256 `ef71e4ca09133905c95210eb5917f64630c82a5e991849e59612f3d06fee161a`。
- 三轮：工作目录／独立副本／最终ZIP解压；各54/54新增测试，扩展定向249项248通过1原有失败；全量1497项1392通过105失败；0跳过0取消，全部272/272 JS/MJS语法通过；连续2次runtime构建均与包内一致。
- 原r84.18同口径1443项1350通过93失败。新增12项逐项确认为获准文件和版本变化触发的历史FrozenContracts，不是未归因新行为失败。原cgImageCommit 1项精确对象比较因既有promptFormat附加字段失败，也原样保留；不得称全部105均为退役测试或全量全绿。
- 三轮实际Chromium各252旧页面前后对照零差异、54 CG编辑布局、12三模式两格式两后端确认/取消与实际公共接口调用校验，数据与预览一致。浏览器about:blank不证明真实图片落盘；保存重开用Node内真实生产处理函数＋模拟持久化边界另验。
- 144生产JS中140一致，4个修改；所有737原正则与26动态RegExp原文保持。94个prompt命名函数91相同，变化3个仅发送组装／漫画包装运行校验／编辑器文案，剧情及自动生成指令模板未重写。
- 8个旧行为案例按此次明确授权更新；新增54项，未修改冻结哈希、源码还原助手或无关预期。源文件差异／三轮TAP／AST／浏览器／CRC/SHA证据均随复核包。
- 本地差异安全自复核未发现未处理High/Medium；Codex Security有安装记录但无可调用动作，专用扫描未执行；新独立reviewer回执未取得。
- 未执行：原生IndexedDB、实际TT/云酒馆、iPhone/Android真机、真实第三方共装及模型/生图API。没有消耗真实生成额度，没有GitHub写入。
- 产物：`Hearttrace-r84.19-validation.md`、`Hearttrace-r84.19-review-evidence.zip`、`Hearttrace-r84.19-file-checksums.tsv`。不能引用上一轮无实际工作目录的测试数字作为本轮结果。

### 7.0.2 历史候选：r84.18（2026-09-17历史实测记录）

- 唯一基线：r84.17 `hearttrace-upload.zip`；SHA-256 `07595d529c6e1b5c49a3265009b326c91bd60e724f3bff97b37de549999314a4`；原包未覆盖。
- 新manifest：`0.8.94`；build／cache：`0.8.94-tt-cg-r84.18-sketch-readonly`。
- 新上传包：交付目录`r8418-release/hearttrace-upload.zip`，下载后basename仍为`hearttrace-upload.zip`；单根`tokimemo-main/`。
- 版本归档：`Hearttrace-r84.18-sketch-readonly-full.zip`，与上传包字节相同。
- ZIP：2,483,920字节，315文件，CRC及重建后解压逐文件字节／SHA全部一致。
- ZIP SHA-256：`ea58f73e3d828c160ec9b2c5e869e4acaf4de5e232396f8c9e40b6bde0058767`。
- runtime SHA-256：`a2dbee2d16835b6e9633c6ee4350a028207b5d8c63dd74cc81f635f2515ceed7`；116模块。
- 三轮：工作目录、独立副本、最终ZIP新解压。每轮192/192定向通过（含新增40行为+10范围），完整1443项：1350通过、93失败，无跳过／取消；语法270/270，连续两次确定性重建均与包内一致。
- 同口径基线实际复跑1393项：1313通过、80失败。新增13项逐项归因为已授权改动触发的历史源码／版本冻结断言（含旧片段提取紧邻注释及旧哈希辅助函数先失败）；没有新行为测试失败。原80失败保留，其中原cgImageCommit等未在本轮修。不能称全量通过。
- 三轮实际Chromium分别252组旧页面前后对照零差异、73项新增设计／备份UI／按钮检查通过；模型调用0。宿主、接口和存储后端是模拟边界，不等于真实共装。
- 修改6个既有生产源文件；144个源JS中138个逐字节相同，未新增／删除生产模块。绘制器和CSS未变；引导仅版本身份。原736处正则全部保留，新travel-map合同增1处；26处动态RegExp完全一致。
- 94个名称含Prompt的函数中，仅本轮获准的structuredTravelPrompt改变；其他93个逐字节相同。另有对应postcardDesignInstructions局部视觉文字改变；不把这项说成所有Prompt一字未动。旧视觉指令可逐字复现。
- 两条旧测试获准更新：无水boat从应拒绝改为应接受；新任务期望新的travel-map合同。旧草稿由基线实跑捕获验证请求字节不变，不更新无关历史冻结断言。
- 本地差异安全自复核未发现本轮尚未处理的High／Medium；Codex Security已发现安装记录但无可调用动作，专用扫描未执行。
- 未执行：原生IndexedDB（浏览器禁止本地测试站点导航，未绕过策略）、实际TT／云酒馆、真实千千结／柏宝书共装、iPhone／Android、真实模型API。本轮新独立reviewer回执未取得；旧Claude报告不能视为审过r84.18。
- 产物：`Hearttrace-r84.18-validation.md`、`Hearttrace-r84.18-review-evidence.zip`、`Hearttrace-r84.18-file-checksums.tsv`。

### 7.1 历史候选／r84.18开发基线：r84.17历史身份

- 候选：r84.17。
- manifest：`0.8.93`。
- build／cache：`0.8.93-tt-cg-r84.17-memory-sources`。
- 上传文件：`hearttrace-upload.zip`，单根`tokimemo-main/`。
- ZIP：2,462,526字节；311个文件。
- ZIP SHA-256：`07595d529c6e1b5c49a3265009b326c91bd60e724f3bff97b37de549999314a4`。
- runtime SHA-256：`4f4ebad8a3f9d05c422b0fe04bea9e133d59d66f1c5f36f7cf81e3ca39d4b0c5`。
- 对照基线r84.16：`Hearttrace-r84.16-structured-postcards-full.zip`；SHA-256 `0f5933851312d1b6666ec2ccf61be75aaa4340bdda41d0031b3d8117d6f11b1b`。
- r84.17材料：`Hearttrace-r84.17-validation.md`、`Hearttrace-r84.17-review-evidence.zip`、`Hearttrace-r84.17-file-checksums.tsv`。
- 以上为r84.17文档整理阶段的历史身份核对；当时未改包。r84.18当轮已重新执行基线和三轮候选测试，见§7.0.1；远端main状态本轮未核实也未修改。

### 7.2 原交付材料中的三轮自复核

以下读取自r84.17报告及证据包，不是这次文档整理新执行的独立测试。

| 项目 | 第一轮工作目录 | 第二轮独立副本 | 第三轮最终ZIP解压 |
|---|---:|---:|---:|
| 定向与范围检查 | 76/76 | 76/76 | 76/76 |
| 完整tests＋tools | 1393：1313通过，80失败 | 同左 | 同左 |
| JS/MJS语法 | 268/268 | 268/268 | 268/268 |
| 连续两次bundle | 一致 | 一致 | 与包内一致 |
| 原Prompt函数 | 94个一致 | 94个一致 | 94个一致 |

同口径基线：1317项，1273通过、44失败。交付材料把候选80失败归为基线44及新增36个历史源码／版本冻结断言；**此归因已获用户转交的Claude独立复跑支持（80=44+36），但不是此后版本的免检许可**。不得写“全量通过”“80项都已修好”或凭新版CURRENT改变断言使其通过。

交付报告申报134个旧生产JS逐字节一致，8个既有文件修改、2个新模块；所有modes／generation源码及94个Prompt函数模板一致。原736处正则保留735处，移除`/本次档案生成/`，新增JSON围栏识别，26处动态RegExp未变。用户已转交Claude独立复核报告：确认无超范围、新链路贯通、未发现新增模块回归，结论为有条件通过。其Prompt／正则计数采用不同提取口径；随后Acorn补核可复现94/736，实际变化项一致，不能将模板里的HTML结束标签当成AST正则。

两条历史行为测试曾因HTML识别的新合同调整：歌曲JSON内标签按转义文本处理、可选明信片设计无效时保正文。用户随后采纳的窄修要求明确保留这些行为（§5.3）。歌曲输入不是无效design，而是文字字段内标签字符，仍不能执行；r84.18不再次改动这两项测试。

交付材料还有252组旧页面前后对照及21项来源UI检查。真实Chromium DOM／布局／事件已执行，但宿主、第三方接口和存储后端为模拟边界。

**r84.17独立报告已取得**：用户转交Claude报告，结论有条件通过；报告本身没有执行原生IndexedDB、实际酒馆、手机、真实接口、浏览器DOM及专用安全扫描。助手的既有浏览器材料不是Claude独立复跑，不能混记。r84.18历史验证见§7.0.1；最新r84.21验证见§7.0。

### 7.3 当前技术限制和使用含义

下列是报告／现有实现记录，不因本文而新增或扩大限制：

- 历史来源账本：8,000条／8,000,000字符限制；长条目完整分片。
- r84.22外部摘要**每批**采用上限：256片段／240,000个UTF-16字符；先完整捕获再规划全部批次，不把全任务截成首批。来源账本8,000条／8,000,000字符和UTF-8 12MB保护仍有限；超资源保护明确失败并保旧数据。
- r84.22主动建档正文使用所选范围完整快照，单条超过旧8,000字符的尾部纳入指纹与分块；其他模块默认快照行为不变。单请求和每批仍有界，不把总资料直接一把发送。
- 最终正式记忆仍最多240条；容量待入档结果保留／可导出、游标暂停，不生成无效Mxxx，不假称全部归档。结果待入档不等于所有模块可引用；未授权无限扩容或自动归并旧记忆。
- 原4槽和12MB边界保留。r84.25已确认本机保存的批内草稿可恢复；原版未落盘页面草稿、保存失败记录、在途请求或已清除站点数据不能保证恢复；正式检查点仍独立受canonical保存保护。
- 0来源可能是接口未接、未就绪、无已保存摘要、选择未生效或读取失败；不能从计数直接推断数据库为空或原聊天损坏。
- r84.17来源、r84.18只读及r84.21响应分流等局部修复不保证真实代理不超时、模型每次返回可解析数据或所有记忆插件自动兼容。
- 同名`hearttrace-upload.zip`后来可能被替换，交接必须同时给build／SHA，不能仅看文件名。

### 7.4 需独立审查／后续确认

**当前补充（2026-09-20，r84.30）：**以下三项为在办待查，不因本轮窄修而关闭——
1. **160项既有失败**：r84.29起全量160项失败已逐项分类核实（149项历史冻结/范围类、5项已退役timeJourney、2项旧空回复错误码断言、2项旧tokenizer/Persona夹具、1项旧来源截断断言、1项cgImageCommit精确比较），r84.30保持完全同批、无PASS→FAIL；分类清单在包外 `failure-classification-160.md`。分类不等于已修，也不擅自重写160处功能。
2. **模型首次返回半截正文**：原因未定位，不属于任何已交付修复；需真实渠道证据，不以模拟通过冒充已修。
3. **timeEcho约38分钟超时**：记录到的约38分钟超时仍为待查项，未修；不与本轮生成准备补码混为一谈。

r84.23已用用户现场诊断和本地代码证实47,030输入token被固定32k拦截，已按当前授权解除建档两项硬门槛；不能把该结论推广为此前所有8万字符错误的唯一原因。真实provider上下文和最大输出能力、原生IndexedDB、实际ST／TT与真机仍未验收。不把本地计量与模拟发送通过当作模型承诺，不把240容量暂停说成无限量全部完成。以下历史审查事项继续保留，不构成越界修改授权。

1. r84.17独立报告已确认80=44+36及本轮无新增功能失败；用户已明确采纳相关处理规则。r84.18另保留93项失败和新增13冻结项归因，仍待本版独立审查及真实使用验证，不用旧报告替代。
2. 真实千千结快照未加载、同宿主重置QQJ身份、柏宝书覆盖缺口、手选历史世界书大条目和写入失败的真实共装测试。
3. 原生IndexedDB、TT/iPhone视觉与性能、升级缓存和页面草稿保护未验收。
4. **超时文档／代码差异**：r42.7文档写默认300秒／上限600秒；已检索r84.0文档写默认600秒／上限1200秒；r84.17实际`DEFAULT_GENERATION_REQUEST_TIMEOUT_MS=180000`、硬上限`1200000`。后续对话只确认本轮不取消／不改变180秒行为，未在此追溯到完整调整授权链。现行代码事实和历史规则不得混写，需作为差异核对，不能仅把文档改成180秒就宣布历史授权已完备。
5. r84.0全文未在本次本地挂载，本次已取回其可检索条款；本文未声称对它完成逐字全文迁移。未取回内容不凭印象新增合同；如后续取得原文发现未覆盖条款，先核对是否已被后续用户要求替代，再补入同一文件，不回滚现有获准行为。
6. 用户本轮已明确授权r84.18两处窄修并三轮自复核；不再沿用“小克仅检查”的旧任务限制。其余待办仍不构成扩展授权，不自动发布、恢复退役模块或开发新适配。
7. r84.19已按§4.13移除cgSegmentValidator、validateCgPreparedFormat、formattedCgProviderPrompts和日常一格包装中的语言／句式拒绝；三模式预览／公共调用／保存重开已本地验证。旧断言工具函数保留导出定义但生产管线不再调用；仍需实际后端与真机验证，不保证任意语言成图效果。


---

## 8. CURRENT维护、证据来源与变更记录

### 8.1 单一现行文档

每次任务先完整读取实际CURRENT。下载序号、文件修改时间、聊天标题和旧助手“已读取”声明不能证明拿对了版本；核对正文日期、候选build、状态和关键产品规则。

每次明确的新要求与候选交付，应同步正文、候选身份、已替代规则、已知失败及未执行项。不能仅在尾部追加“以后面为准”让正文继续保留冲突。

项目资料区只把`Heartbeat-Memories-Project-CURRENT.md`作为现行条目。历史副本可归档但标明非现行，不同时保留多个内容冲突的CURRENT供检索。文件传输ZIP只是同一文档的容器，不是插件安装包或第二套需求源。

本次直接更新同一本地CURRENT并提供传输包；**本地交付不等于ChatGPT项目资料区附件已自动替换**。资料区旧附件需撤下现行标记。本轮在原包独立副本中产生新ZIP，未覆盖原包、未改GitHub、未操作用户真实数据。

### 8.2 先前整理依据与置信边界（历史来源保留）

- A：本地`Heartbeat-Memories-Project-CURRENT(1).md`／旧`Heartbeat-CURRENT-transfer.zip`，已完整读取；正文日期2026-08-27、r42.7、20,109字节。只用作历史来源，非当前基线。
- B：项目文件`01-Heartbeat-Memories-Project-CURRENT-1-.md`，正文2026-09-14、r84.0。实际检索到§1–§8多处内容，含公共CG API、r82撤回timeJourney、r83请求合同、r84蝴蝶修复、导航／来源及安全边界。不是说不存在更新过的CURRENT；此前转发r42.7属于拿错旧副本。
- C：本对话后续用户明确要求：限定五项人设扩展与HEART解耦、实际成果可读、原风格三栏UI、私密分类、默认ADV图文、房间与时空回响、NAI格式、歌曲双模式及语言／群像、结构化明信片和记忆来源修复。
- D：本地r84.17安装包、验证报告与证据包。实际身份／CRC／哈希在本次重核；测试数字来自原TAP与JSON记录，不冒充本次重新运行。
- E：第三方接口说明沿用本对话已获取的作者文档与B中记录。本文不是实时上游核查；下一次修改接口前重新只读核对作者公开合同，不能以本文件代替许可和版本门禁。

### 8.3 已替代的关键旧说法

| 旧说法／早期方案 | 现行处理 |
|---|---|
| r42.7、撤回声明或无ZIP的r84.25中间结果可作为当前基线 | 本轮实际基线为SHA核实、已交付的r84.25完整包；当前r84.26独立修复并以新三轮及最终ZIP实物核验，旧撤回/中间数字不作证据 |
| 未知模型也应固定限制32k输入token／96k字符 | 建档不再这样拦截；未知保持未知，不调大共享常量，不降低输出、不更换模型或自动重试 |
| 只截取首批256片段／24万字符即可说来源已归档 | 完整捕获后有明确后续批次；每批显式点击，正式成果与游标一致保存，未提交和容量待入档均不算完成 |
| 有240条上限只能覆盖旧Mxxx或完全重建 | 旧Mxxx与上限不变，未入档结果有界保留／导出、暂停；扩容或schema改造另获授权 |
| 8万字符就是8万token，或足以断定用户／模型／数据库原因 | 分单位计量完整请求；本次现场是本地32k硬拦截，providerRequests=0。真实提供方能力和其他错误仍不得无证据推断 |
| 普通建档按钮没有恢复分流 | 基线已有分流；按实际按钮至请求／提交链验证续接和 typed 变化类别，不删身份校验 |
| 一切世界书都只能作背景 | 用户明确标为历史摘要的手选书可经来源与证据流程建档；普通设定仍只是背景 |
| 选择全部楼层就会读到第三方全部记忆 | 正文范围、外部接口、世界书和模型预算分别控制、分别统计 |
| 柏宝绘没有任何可用公开生图接口 | B已记录正式API v1的获准接入；私有桥接禁令不变 |
| 没有生日祝福／基础语言完整包就不能进入角色互动 | 基础语言独立，生日可空，有效成果可读 |
| 私密是隐藏内容的屏障／密码锁 | 私密是可见独立分类，排在结局前 |
| 默认阅读ADV只显示文字 | 默认CG与正文同屏 |
| NAI 5必须英文、切换格式即转换完成 | 自然语言允许中文；切换不请求，转换明确操作并核对 |
| 选择4.5／5后只能发送完全匹配的语言与句式 | 选择只指导主动生成写法；用户确认的中文、自然语言、Tag或混合文本不因格式不符被拦，真实安全和请求保护保留（r84.19已在三条生图链路实现，本地复核见§7.0） |
| 明信片自由说明＋正则选旧模板就是独立构图 | 受限结构化图元实际决定新卡构图，旧卡原路径 |
| 图元25种、60“图形”就是节点预算 | 名单实际24种；当前按实际展开最多90子节点计费 |
| 小船没配水面／画得不好看应丢弃 | 简笔画、重叠、非写实都可；只拒绝无法解析、超支持能力或超资源边界的数据 |
| 一次读取失败就等于原聊天永久只读 | 当前只是只读备份；用户可主动重读经验证的源，备份本体不解锁、不覆盖源 |
| 自动测试完成就已真机验收／正式发布 | 自检、独立复核、真机和GitHub发布分开记录 |

### 8.4 历史节点（简记，不作未改范围之外的回滚指令）

- r42.7：早期档案、ADV和性能保护规则整理起点。
- r46及后续：后台关闭／切聊、逐日草稿、明信片图文与作用域写回要求完善。
- r73／r74及后续：公共CG API、双方外貌与完整场景、TT条目回填和插件内大图要求。
- r79–r84.0：私人终端时空回响、悬浮与书签、诊断；timeJourney最终撤回；请求协调与蝴蝶叙事／分歧修复。9月14日已存在更新后的CURRENT。
- r84.6–r84.8：有限人设推演、HEART基础语言解耦、有效成果按实际数量展示。
- r84.9–r84.13：三栏原风格UI、HEART路由／生成状态隔离、歌曲与双视图／新增语言、ADV默认图文、设置去重、NAI与剧情日历。
- r84.14–r84.16：群像歌曲、明信片去重与图文同源、结构化构图及旧卡／邮箱保护。r84.15的自由文本识别方案不作为新设计目标；用户后来明确指定其包为r84.16开发基线，保留其旧数据兼容。
- r84.17：手选历史世界书、千千结、来源计数／材料预算、HTML识别和建档续写窄修；用户已转交Claude独立复核，结论有条件通过，真实环境未验收。
- 2026-09-17（r84.17文档整理阶段）：纠正错发r42.7及未续更问题，将已确认要求写入同一CURRENT；当时仅文档和身份核实，无插件／GitHub修改。后续r84.18变更按以下新授权执行。

- 2026-09-17 r84.18：用户改由助手执行已商定两处窄修；允许boat无水简笔画，临时备份可主动重读合法源，迟到读取不抢页。三轮实跑结果§7.0；原包保留，GitHub无写入，当前文档同份更新。

- 2026-09-17（r84.18交付后）：用户纠正NAI格式选择的含义——只控制提示词生成风格，不得用中文／自然语言／Tag差异阻止用户绘图。本轮只读复现并更新§4.13，未改生产代码、安装包或GitHub。

- 2026-09-18 r84.19：用户追问实际新ZIP和是否覆盖所有生图；撤回上一轮无目录的不可核验结果。从真实r84.18完成4文件窄修和三轮自复核，最终包/构建/逐文件校验实际通过。保留所有非格式保护，未改剧情生成指令，未做真实网络/真机验证。

- 2026-09-18 r84.20：落实本轮自动视觉提炼共用规则与竖向漫画，不重做用户编辑；三轮记录以§7.0为准。更新同一CURRENT，未改GitHub。

- 2026-09-18 r84.21：基于真实r84.20，仅修同一次手动API的有界JSON/SSE分流及必要HTML包装保护，三轮实跑和最终新ZIP通过相应检查；全量仍有124项失败且逐项分类。所有剧情Prompt不变，无额外请求，无远端写入，更新本份CURRENT。

- 2026-09-18 本轮r84.22：撤回的无实物交付声明不作依据；基于SHA已核实的真实r84.21，窄修完整来源批次、原子检查点、240条待入档边界、真实UI续接及完整请求预算，保留r84.21解析和其他模块原意。三轮本地实测以本轮§7.0及实际证据为准；原包保留，GitHub无写入。

- 2026-09-18 r84.23：用户授权修复建档模型未知时的固定输入硬拦截，并复检三次。使用真实r84.22基线，旧草稿发送统一计量但保持skipTokenCount、恢复身份和验证器；其他Prompt、正则、响应分流与所有存储保护保持。按最终ZIP实际回执交付一键命名包，更新本份CURRENT，GitHub无写入。

- 2026-09-19 r84.25：用户要求完成未交付修复并追加未完成模块/分段保存、手动API默认保存、国模和3.6以上空回适配。基于真实r84.24重做，三轮与最终ZIP实物核验；同一CURRENT更新，不继承旧中间数字，不写GitHub。

- 2026-09-19 r84.26：用户要求可恢复安装包和三轮复核。补齐档案页自动读取、显式无生成读取/保存及可靠导出；三轮增加真实旧包到新包的2000楼/63段/41成功升级链路，明确旧未落盘RAM无法事后找回。更新本份CURRENT，不改第三方接口，不写GitHub。

- 2026-09-19 r84.28：按用户要求将修复放在全部任务共用兼容/恢复层，安全补全旧可选内部标识、防失败重试污染原背景、明确变化类别及显式导出；原Prompt/正则/请求与安全保护保持。三轮回执以本轮包外实际报告为准。

- 2026-09-20 r84.29：修复续写覆盖旧进度（retainedPartials单调保留、代码自有schema合并、冲突报RMT_RECOVERY_MERGE_CONFLICT）；Prompt零变化，34/34哈希经独立复跑一致。Kimi独立回执已取得（包外 `Hearttrace-r84.29-independent-review.md`），结论为模拟边界内修复成立、因160项既有失败与无真机验收不放行公开下载。diff行数口径统一为“净行数+逐文件补丁可重放”，不引用算法相关的毛计数。

- 2026-09-20 r84.30：按用户授权窄修三处——容量满时新成果页面内有界保全并可随“导出未提交草稿”带出；建档保存-before-request失败按容量/配额/CAS冲突分类（新码RMT_ARCHIVE_DRAFT_CAPACITY，保护不取消）；生成准备环节错误补码（RMT_CACHE_BACKUP_CORRUPT等），不删记录、不自动重复生成。新增31项测试全过，全量160项失败与基线同批，16/16突变仍捕获，双重建字节一致。真机/原生IndexedDB未验收。更新本份CURRENT，GitHub无写入。

- 2026-09-20 r84.31：真机房间输入34,282 tokens超写死32,000预算被拦截（0请求0费用）；用户授权把输入预算开放为设置（默认32,000不变，可调8,000–128,000，越界拒绝保存），拦截文案补可行动建议。新增8项测试全过，全量160项失败与r84.30基线同批，34/34 prompt哈希不变，双重建字节一致。更新本份CURRENT，GitHub无写入。

- 2026-09-21 r84.34：按用户17项总任务的P0部分施工（审计→实施→测试），四项修复——(1)高楼层分批：新增楼层覆盖账本 coveredRanges（区间归并/缺口推导/补录与增量标签区分/中断续接不倒退不冒领，逐批手动点击与防重付保护不变；旧档案无法证明全覆盖者不伪造回填）；(2)CG大图查看器：关闭按钮补 capture 阶段 pointerdown/touchstart 兜底并与 click 500ms 去重，根节点 touch-action:manipulation 治理 iOS 页面级缩放把工具栏推出可视区，任何加载/缩放/拖动状态必可退出；(3)切后台：hidden/pagehide 紧急落盘（deferred队列/压缩缓存/建档草稿），超时改时钟戳判定且 hidden 时长不计入（恢复不再误杀在途请求），段级"已付费但保存失败"进 holdUnsavedReply 可经恢复区导出，durable=false 显式提示导出未提交草稿，状态未知错误（连接/网络/服务器）恒不自动重试；(4)相册分类：normalizeAlbumCategory 别名表标准化、未知→待分类（不再洗成日常）、手动分类打 categoryManual 不被合并/重生成覆盖、新增"重判分类"轻量路径不动正文/图片、regenerateAlbumEntry 不再置空 cgImage。存量"日常"不批量迁移（无法区分真日常与被洗白者），提供手动与重判两条显式路径。新增30项测试全过；全量失败158项均为r84.33基线160项子集（新增0、修复2）；Prompt 34/34逐字节不变；16/16变异捕获（publish-before-durable-ack锚点随授权改动更新）；语法366/366；双重建字节一致；冻结门经r8434ApprovedDelta（21文件）逆变换登记。iPhone/TT/云酒馆真机与原生IndexedDB验收未执行。更新本份CURRENT，GitHub无写入。

- 2026-09-21 r84.35：17项总任务P1第一批（CG簇）。P1-5多人卡生成范围经审计验证链路已正确（范围=勾选名单全体，缺人抛错/名单外ID拦截/归户CAS均有测试钉住），零改动。P1-7生图锚点：六类槽位审计确认外貌/玩家独立且按角色区分、玩家覆盖优先级正确；修复legacy元数据对重复role静默丢弃（cgAppearance.js改抛RMT_CG_PROMPT_INVALID，与快照路径严格度一致）。P1-8 CG历史图：每张图最多保留5版历史引用（cgImageHistory，只存同源路径引用、拒绝dataURL/外部主机/路径穿越、按url去重、FIFO淘汰）；重绘/清除旧图自动入历史；图片设置面板新增历史版本查看/恢复，恢复=纯指针交换（当前图转入历史，不重新生图、不删文件，CAS提交通道与clear同构）；cache三处白名单与contentManager内部字段登记保证文本重生成不丢历史。柏宝绘公开API v1无图库查询能力，版本历史在本插件数据侧实现，未读取任何第三方私有Key/数据库。新增7项测试全过；全量158失败与r84.34基线同批（新增0）；Prompt 34/34逐字节不变；16/16变异捕获；语法368/368；双重建字节一致；冻结门经r8435ApprovedDelta（8文件）登记。真机/原生IndexedDB验收未执行。更新本份CURRENT，GitHub无写入。
