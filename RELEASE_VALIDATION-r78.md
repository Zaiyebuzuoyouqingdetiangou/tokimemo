# Hearttrace r78 头像悬浮入口

版本：0.8.73 / 0.8.73-tt-cg-r78.0。2026-09-14。

基于已交付 r77 完整 ZIP 的校验解压副本继续开发，保留此前修复；未使用 GitHub main 替换源码。原始 r74 和 r77 ZIP 未修改。

## 使用

- 首次从魔法棒打开插件，再关闭插件，显示可拖动的头像圆钮。
- 插件首页的主题设置中选择“悬浮头像”：char（默认）、user 或关闭。
- 在某人的档案室查看后收起，显示该档案对应头像；点按恢复刚才页面。历史档案重新读取后以只读状态打开，不切换酒馆当前聊天。
- 未找到头像时显示爱心。旧档案缺少 user 绑定且无法从其源聊天读取时，也显示爱心。
- 头像选择和拖动位置保存到原有插件设置。最后查看页面只在当前网页会话保留；刷新后需先从魔法棒打开。档案更新或删除了原详情时，不恢复失效详情。

## 实现与验证

无新增依赖、图片文件或模型请求。仅使用宿主现有头像路径；档案可增加一个不足 300 字符的 user 头像文件名。完全重建使用本次捕获的人设头像，生成中的后续人设切换不影响已捕获值；增量保留已有绑定。

启动仍为原有懒加载，悬浮入口在完整运行时首次主动加载后建立。样式仅限定本插件元素，拖动不接管页面全局触摸，禁用时清理节点与监听器。

本地自动检查：177/177 测试，23 个测试文件；128 个 JS/MJS 语法检查；94 模块 bundle 重建两次完全一致，并验证五个既有公共导出。新增检查覆盖头像来源、拖动、设置持久化、真实档案保存路径、历史阅读恢复、版本变更、删除以及异步关闭/切换聊天。

检查使用宿主、DOM、存储边界替身，不能替代真机。TT、iOS Safari、安卓本地酒馆的实际头像可见性、拖动安全区和键盘弹出后的布局仍需验证。此前安卓存储兼容性问题也未在本次测试中获得真机确认。

安装时将 ZIP 内 tokimemo-main 里的文件覆盖原插件目录，保持 manifest.json 同层，重新加载后核对 r78.0。不要另装第二份同名插件。

最终 ZIP 的体积、SHA-256、CRC、全新解压复测见外附 Hearttrace-r78-validation.md。
