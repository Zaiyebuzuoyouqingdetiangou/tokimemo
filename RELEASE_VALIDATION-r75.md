# Hearttrace r75 修复与验证说明

版本：**0.8.70 / 0.8.70-tt-cg-r75.0**。日期：2026-09-14。

唯一源码基线为用户上传的 `Hearttrace-r74-trace-fix-完整版(1).zip`。原包未覆盖，未以 GitHub main 替代，未提交、推送或发布到 GitHub。项目 CURRENT 未改写。

## 六项核对结果

| 项目 | 本轮处理与证据 | 尚需确认 |
| --- | --- | --- |
| char / user 外貌与场景 | 新图的编辑器及直接绘图入口读取同一份已保存外貌；角色卡与 persona 分开取证。修改画面会清空旧 sceneTags 和 flatPrompt；模型 tag 与明确已知外貌冲突时拒绝整份新草稿并保留原稿。 | 模型自由文本仍可能语义矛盾，无法用字段检查保证每次实际画面完全遵从；绘图前可在图片设置核对。 |
| TT 成图替换与重开保留 | 修复绘图期间退出并重进相簿 / ADV 后只写入缓存、不刷新当前视图的问题。HEART 共用单图原子提交、镜像失败容错和失败回填。自动测试覆盖当前视图、重新读取缓存、存储失败与旧图保留。 | TT 完整退出再启动后的宿主存储和本地图片路径需真机验证。成功保存才属于可重开恢复的图片；失败后待回填引用仅在当前页面保留。 |
| 插件内查看大图 | 沿用包中已实现的插件内查看器，适屏 / 原尺寸滚动 / 返回均保留，不另开网页。现有查看器测试纳入全部测试。 | iPhone 触摸、横竖屏、安全区及实际大图显示未执行。 |
| 档案已返回却卡住 | 本地 token 计数最多等待 5 秒，取消立即结束等待。简介计数超时进入既有本地简介兜底，保存通过校验的记忆；不重抽成功分块。真实 import 入口回归确认只发出一次模拟 provider 请求。修复取消简介重试时的未定义变量。 | 此次修复的是已复现的计数等待路径；真实代理的流式结束、浏览器存储异常仍需现场诊断。其他未通过 JSON / 来源校验的返回不会强行保存。 |
| 档案室外诊断与脱敏导出 | 酒馆扩展设置保留独立“心迹回廊 · 诊断”入口。冷态、完整运行模块已加载、模块加载失败三种情况下可用。支持 JSON 下载、复制和失败时手动复制。按实际响应 / 解析 / 校验 / 分块 / 保存 / 渲染记录阶段，失败不误报成功。 | TT 的下载和剪贴板权限需真机验证；报告文本框保留手动复制出口。 |
| 外貌保存与日常图文分格 | 新增“保存外貌”，无需生图；每侧最多 400 字，明确履历、HTML、链接被清理，陌生短外貌 tag 可保留。手动清空不会自动补回。1 / 2 / 4 格的本地设置与现有文字展示保留，Q 版、格数、逐格动作及无文字要求进入实际主 prompt，两类后端均覆盖。 | 实际模型的格数、人物一致性、Q 版和无字效果仍需出图核对。 |

## 保存与兼容边界

- 独立外貌副本只在主动保存时写入插件自己的小型本机记录，并尝试同步聊天 metadata；完整聊天、角色、用户身份隔离，旧值签名与档案 revision 校验。未删除的有效 live 档案才能读取备用记录或保存，历史快照不可写。保存失败保留旧值与编辑草稿。
- 已有图片继续使用其已确认提示元数据；保存聊天外貌只影响后续新图，不自动重画旧图。
- 图片保存失败时，到“图片设置 → 回填已生成图片（不再生图）”重试。当前页面的待回填引用在完整刷新后不会保留；成功提交的图片引用按原有缓存与独立备份保存。
- 升级前只有柏宝绘图库文件、尚未写入插件档案的旧图片不会自动匹配归属；图库文件仍须存在。
- 沿用原 CAS、revision、只读与删除栅栏、备份和 12 MB 缓存保护。未修改缓存、独立备份或删除实现。
- 仅使用作者已公开、供第三方调用的 API v1。本轮只读核对了作者固定提交的 [PUBLIC_API.md](https://github.com/baibai-git/ST-BaiBai-Image/blob/34157f4f08da5559f295b027d46279d06f4de5bf/PUBLIC_API.md) 与 [公开类型](https://github.com/baibai-git/ST-BaiBai-Image/blob/34157f4f08da5559f295b027d46279d06f4de5bf/src/public/types.ts)；未调用私有接口或打包第三方源码。

## 已执行验证

- 原 r74 包所带测试：55 / 55 通过；本轮工作副本全部 12 个测试文件：**92 / 92 通过**，失败、跳过、取消均为 0。
- 相同的图片回填定向测试放入独立 r74 副本后有 5 项失败，修复版相应测试通过。测试调用真实插件逻辑，只替换宿主、存储和 provider 边界。
- 全部 **114 个 JS / MJS** 语法检查通过。
- **91 个模块** runtime bundle 连续两次重建字节一致；生成 bundle 可导入，主要生命周期导出存在。
- manifest 与 index 的版本、build 一致，加载查询参数使用新 build。
- 对冻结的 r74 → r75 源码差异进行了独立安全复核：未发现尚未修复的 High / Medium。该结论是静态差异审查，不是线上渗透测试。
- 原包未包含 CURRENT 历史版本提到的完整旧测试集；“全部测试”仅指本 ZIP 实际附带的测试，不声称重跑了未提供的历史测试。

原 ZIP SHA-256：
`cffff7fe661be936a11df13aeb433b020de9f57bf0ee313b3620a7508108ef70`

runtime 源码指纹：
`1993965316189e2428d9bcbab6804f21ef7340c89130a28adaf19d1928a47eaf`

runtime bundle SHA-256：
`c786ed7f31a6adfbacf946ce421ea9cb3c7c7f4619af8033d7e161a3b4ff7799`

最终交付 ZIP 的 CRC、逐文件 SHA-256、重新解压测试及 ZIP SHA-256 见随包提供的 `Hearttrace-r75-validation.md`。

## 安装与真机验收

将 ZIP 内 `tokimemo-main` **里面的内容**覆盖原插件目录，保持 `manifest.json` 同层；完整重启 TT 后确认版本 **0.8.70**。不要重复安装为第二份插件，也不要清除站点数据。

以下项目**未执行**：真实浏览器视觉测试、iPhone / TT 测试、真实文本服务请求、真实柏宝绘出图及付费请求。本环境没有可启动的浏览器内核；DOM 测试不等于浏览器或真机实测。

建议验收顺序：保存双方短外貌并重开图片设置；生成一张 CG，生成中退出再进入相簿，确认完成后替换；完整重启后查看同图并打开插件内大图；核对日常 1 / 2 / 4 格；最后从酒馆扩展设置导出诊断。若档案生成仍卡住，直接在该入口导出脱敏报告，不必进入档案室。
