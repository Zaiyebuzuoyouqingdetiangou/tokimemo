# r55.0 候选验证与使用说明

版本 0.8.51，BUILD `0.8.51-cover-terminal-r55.0`。基于 r54 独立副本修改，旧目录、旧 ZIP 和旧档案保留。未推送仓库、未安装用户宿主、未使用真实模型或凭据。

## 更新后怎么用

1. 替换插件文件并重新加载页面。在当前档案点“写下判词”（已有新判词时是“重写判词”）。这是一次独立 API 请求，不需要完全重建档案。
2. 封面只展示短判词。旧总结和索引仍可在“查看记忆梗概与索引”中展开，不会删除原记录。
3. 私人终端有草稿时点“继续生成”，只请求未完成应用。没有可核实原文的条目显示为空，不凑固定句数。全部来源不足时需补充资料并手动更新档案后再试；勿删除旧档案。
4. 草稿设定来源发生变化时，已完成应用仍保留；恢复原来源后可以继续，也可以通过明确的重新生成操作重新规划。
5. 认证/余额/权限真实有问题时，先检查当前独立 API 配置。此次修复不会绕过上游限制，最多只做失败段的一次有界重试，不会连环重试整个组合任务。

## 本轮根因与安全收口

- 封面生产提示本来要求剧情摘要，UI 原样展开。改成独立判词、双方关系阅读、真实意象来源校验；旧梗概折叠。题辞不进入 Mxxx 或后续历史证据。
- 终端 folio/relic 提示词与局部/最终长度门槛不一致，且强制聊天与稀疏真实来源冲突。新配额按来源，保留双向原话及说话人归属要求，不明确的转述不猜。
- 草稿错误不再吞掉认证/限流原因。修复全空 N/N 续跑死循环；最终来源重验若会丢成功 App 必须停止，不能静默保存残缺结果。unavailable 不覆盖旧真实条目，不作为后续事实输入。
- 状态码优先；Profile/手动 HTTP 200 内嵌错误均从公开生成入口分类。保留 Retry-After，明确 quota 不重试，Heart 认证/连接/限流失败后不继续兄弟请求。
- 两个只读 reviewer 的实证问题均由主 agent 重现后统一修复。A→B 在定向重生成读取世界书期间切换时，必须在 provider 调用前停止；保留原 revision、CAS、删除与生命周期围栏。
- 按 UI/UX 的信息层级与恢复原则，判词优先、梗概默认折叠，错误就近显示原因和进度；没有全站改色。

## 已取得的本地证据

- 完整自动测试：392/392 通过（其中本轮新增 23 项）。包括只改封面而保留记忆/草稿、切聊/改 revision 丢弃晚到判词、真实生成入口状态分类、401 一次请求、429 至多两次、终端保存→JSON 重开→只续未完成、全空恢复、来源改变/恢复、真实定向重生不以空覆盖旧值、转述与三人群聊、A/B 等待期间请求前阻断。
- JS/MJS 语法：105 个文件通过。runtime 两次构建 SHA-256 相同；63 个可达模块构建，bundle 动态导入通过。
- 浏览器：本地 Edge，390px 日间/夜间/跟随宿主及 1280px 日间，共 4 组真实封面按钮→模拟接口→保存→页面重开；默认折叠、键盘展开、无自动重复请求、旧记忆及草稿不变。已查看日夜封面截图。
- TT 回归：使用先前固定提交 `9693a4ec47cd4552f90878bccab453f176de0f18` 的完整 TT CSS（不随插件打包）和额外恶意标签样式，5 组检查通过：320/390/844 宽度设置、关闭后台任务后切聊天、设置自动更新→宿主事件→隐藏消息增量→保存，以及旧历史真实变更时保护旧档案。模型响应均为本地合成。
- 最终 ZIP 的单根目录、CRC、安全路径、逐文件 SHA 与新鲜解压记录由压缩包旁的 `.verification.json` 给出；最终解压复测记录单独保存，不靠 ZIP 文件名推断通过。

## 尚未验收，不作为正式公开保证

- 用户真实 API Key、Profile、额度、代理响应和真实模型生成成功率。
- 真正 TT 原生 WebView、iPhone、用户自定义美化、杀进程后的存储恢复。
- 判词的文学质量和对用户实际关系的理解。当前本地校验只能证明结构/引用/复制边界，不保证模型理解准确。
- 此包是本地候选，不宣称“所有故障已解决”或“全记忆插件无条件兼容”。建议先备份并用原来失败的同一档案验收，再决定公开。

复现工具：`tools/verify-r55-checks.mjs`、`tools/verify-r55-browser.mjs`、`tools/verify-r54-tt-browser.mjs`。测试需要 Node；浏览器测试需要 Playwright/Chromium，并使用输出环境变量将证据存到插件目录之外。
