# 心迹回廊 r84.7 定向修复候选

版本：`0.8.83-tt-cg-r84.7-scopefix`。以用户提供的 `tokimemo-main 8(1).zip` 为修改基线，在 r84.6 候选副本中修正复核遗漏；原包及 r84.6 未覆盖。GitHub 无写入。

## 本轮范围与实际行为

1. **日历页角随笔**：人设当下表达不要求 Mxxx。日期由已经验证的日历条目或本次捕获的本地日期决定，忽略模型随意提供的日期；写入具体日期页，保存后重开保持归属。单项重新生成保持原日期/条目身份。历史摘录仍须有效 Mxxx 与完整原锚点，失败不得降级成人设随笔。
2. **明确关系称呼**：只接受直接表达当前 char/user 关系的受控设定，不以姓名靠近“已婚”等关键词授权。当前档案明确的离婚/分手/朋友关系可覆盖初始设定；同事已婚、提问、假设、梦境不授权配偶称呼。邮箱和前世今生的最终组装保持同一份捕获的关系依据；不写入正式档案。
3. **手动生活扩展**：房间和“他的物品”的实际入口支持没有新 Mxxx 的手動推演追加。原父节点/容器/旧描述保留；新节点不冒充共同礼物或往事，不能搬移已有子树。路线允许同一地点的新当下对白作为新版本保留，完全相同的版本不重复追加，上限不变。新增推演权限仅由本地手动操作授予，自动历史同步不获此权限。两个人的陈列柜没有改动。
4. **私人终端**：设定联系人在前置校验、分项归一化、最终会话、保存重开使用一致规则。姓名和身份必须有该联系人明确设定；普通备注可创作，电话、地址、账号等私密字段不因联系人存在而获授权。沿用原有单条 unavailable/补齐机制，合格兄弟项不重做。
5. **HEART**：档案室主卡片、头像入口、已有/空内容与缓存重开不再依赖语言库或关系锚点。打开空壳不调用模型、不写空会话。基础语言独立生成；原类别、数量、严格生成校验保持原样。部分语言可读但不标为完整；语言任务失败不破坏四季/萤火虫/日常一格。主动替换已有语言使用两次确认，取消不发送请求；真正写入仍用原有身份、revision、删除/写入栅栏及 CAS。无内容首次失败支持原续写入口。
6. **模型选择器**：保留 r84.6 实际 UI 修复：可手填 ID，同时使用受容器宽度约束的下拉列表，不再依赖 datalist。没有改 API 请求协议、主连接、密钥存取方式。

## 验证

- 新增行为回归 **58/58**；新增不改动合同检查 **31/31**。
- 完整测试**未排除任何测试文件**：候选 **433 项 / 428 通过 / 5 失败**；原包 **340 项 / 335 通过 / 同样 5 失败**。失败均为已退役模式的 `tests/r81TimeJourney.test.mjs`，没有为了通过而删除、跳过或恢复该模式。
- 全部 JS/MJS 语法：候选 **217/217**，原包 **211/211**。
- 98 模块 runtime 连续两次重建一致；打包 bundle 可实际 import，5 个导出接口有效。
- 27 份受保护文件与原包逐字节相同；4 个关键函数原文合同相同，包括共享 Prompt 和 HEART 原始语言生成要求。bootstrap 除版本/build 身份外与原包相同。
- Chromium 隔离 UI：320/375/390/430/1366px 下模型列表、选中同步、宽度检查通过；模型名称作为惰性文字，不解释为 HTML。模型和宿主是本地测试替身，不是真实 API。
- 最终交付包的文件清单、逐文件 CRC、ZIP 与 bundle SHA-256 见随包外部验证记录。

## 安全与未执行项

本地差异复核已覆盖复核报告中的误授权、隐私字段、覆盖确认与读写隔离问题；未发现本轮尚未处理的 High/Medium。Codex Security 搜索显示已安装，但命名空间没有可调用工具，**专用扫描未执行，独立回执未取得**，不能把本地复核视为独立审查。

**未执行 iPhone/Android 真机、真实 TT、实际云酒馆或真实模型/API 网络测试。**这里只验证本地模块、模拟宿主的实际生命周期/压缩缓存/提交路径以及隔离 Chromium UI。

没有修改相簿、ADV EVENT、ENDING、蝴蝶效应、成就、陈列柜的产品要求；没有改动 API Unauthorized/异常响应 JSON 的处理。未放宽真实历史证据规则。没有重新引入外部 CSS bundle 或 stylesheet link。

## 安装

先导出需要保留的档案备份、备份现用插件目录，等在途任务结束。使用本完整版同名覆盖插件目录，保持根目录层级，不要只覆盖 src 或只换 bundle。完整刷新客户端后诊断版本应为 `0.8.83-tt-cg-r84.7-scopefix`。

不清除站点数据、IndexedDB、聊天或档案；旧内容不强制重生成。手动 API Key 的本页有效期仍沿用原包规则，刷新后的重新填写需求未改变。
