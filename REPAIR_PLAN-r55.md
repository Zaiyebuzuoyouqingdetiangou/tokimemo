# r55：判词封面与生成失败收口

范围：当前档案封面由事实摘要改为基于双方经历的短判词；旧档案可仅重写封面；终端计划与详情/最终校验一致；保留认证/限流/内容失败的安全分类。不是公开发布、品牌迁移或全站美化。

主 agent 已汇总两个只读 reviewer 的独立结论，并核对生产代码：
- archiveProfilePrompt 明确索要 120–300 字摘要，fallback 拼接六条总结；UI 原样展开它。统一改为独立判词字段，事实摘要留在数据/折叠区，不用截断摘要冒充判词。
- Phone 的 folio/relic 提示 8、局部验证 12、最终验证 8；目录强制 chat 与逐字事实证据能力矛盾。取消与证据无关的硬凑数量；保留每条消息来源、双方身份校验。有据短记录可成立，无据内容明确为空，不伪造历史。
- Phone 外层草稿错误抹掉 cause；修复请求未获得分类反馈。保留有界、白名单原因与进度；已经完成 App 不重复请求。
- 429 可能被鉴权文字先匹配；HTTP 200 error envelope 丢分类；Retry-After 丢失；Heart 连接失败后继续下一段。统一修复分类、一次有界等待和组合任务停止条件；不绕过真实认证/额度限制。

不变量：V3 键、已有 Mxxx、来源、archiveRevision、旧派生结果、Phone 草稿和图片不因重写判词改变。A/B、CAS、删除/lifecycle 围栏、转义和无凭据日志边界保留。所有生产修改由主 agent 完成。

验证：先针对当前缺陷复现，随后针对性单测、完整回归、生成/持久化/重开/继续的浏览器合成宿主检查、确定性构建、ZIP 新鲜解压与哈希。真实 TT/用户模型仍单列未验收。
