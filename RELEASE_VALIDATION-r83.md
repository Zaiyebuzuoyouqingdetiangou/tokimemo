# Hearttrace r83 验证说明

版本：0.8.78 / 0.8.78-tt-cg-r83.0

## 基线与范围

唯一修改基线为已交付 Hearttrace-r82-full.zip，SHA-256：
`8acbf7db566d1e87a6e967ac18a97812d1fc2e129441631c83d99118d0e97547`。
延续用户上传 r74 的修复链，没有使用 GitHub main。恢复工作区后重新下载并验证 r82 SHA 与 CRC；前轮未交付修改从该基线恢复并重新测试。

## 已确认与修复

1. r82 的 requestJson 在本地输入准备前取得 provider 名额，其他任务等待时仍显示 prompt。改为准备及预算检查后取得名额；generateConfiguredJson 的直接档案调用也进入同一队列。解析、校验、保存不占 provider 名额。
2. 同步终端校验失败后原 validate 标记未关闭，等待重试与排队时仍会显示 validate。现在失败立即关闭阶段，分别记录 queue、pacing、retry、request；原 stages 是累计时间，新增 durations 才记录 begin 到结束的耗时。嵌套阶段可重叠，不能把 durations 相加当总耗时。
3. 原普通分段最多两次，但 429 最多四次，与 CURRENT 的最多重试一次不一致。现在每次文本分段生成总共最多两次尝试；连续 429 或校验失败均在第二次失败后停止该段自动重试。保留 Retry-After、限流降为单并发和原不可重试错误分类。
4. 逻辑主任务上限由实际旧值 10 修正为 CURRENT 约定的 5；provider 名额仍最多 2，出现限流后最多 1。
5. provider release 绑定 runtime 代次，旧 runtime 迟到回调不会误减新 runtime 计数；外部取消信号转发，排队后及发送前重验生命周期与配置，历史档案继续绑定原 origin。
6. 诊断新增 hasPendingWork、每段 attempt、脱敏 retryCode、实际传输调用数 providerRequests。实际发送时向父任务累加，旧子段移出末 4 条列表不丢总数。输入检查或纯等待不增加该计数；计数不是计费凭证。重试开始清除旧 input/response 摘要。

## 验证证据

- 从 r82 ZIP 独立解压复现：输入检查占名额、直接档案请求绕过并发，两个用例均在原实现按预期失败。
- 11 项调度回归：阻塞 token 检查、直接档案并发、预算前置、排队取消、配置变更、HTTP 200 内包裹 429 的释放顺序、旧 runtime 释放、未知错误不重试、连续限流与校验失败最多两次、5 项逻辑容量。
- 5 项追踪回归：真实阶段时间、并发父子隔离、重试摘要清除、数量与隐私边界、实际发送计数及被裁掉子段的总数。
- 既有 token fallback 测试两项随新合同调整为预检不占名额，实际 transport 仍断言占用 1 个名额，保留取消及释放检查。
- 源码完整测试：266/266，通过；0 失败、取消、跳过。共 34 个测试文件。
- 全部 JS/MJS 语法：142/142，通过；97 个源模块运行包重复构建逐字节一致。
- 五个公开运行导出保持：destroyMemoryTheater、initMemoryTheater、isGenerationBusy、openArchiveLibrary、openSettingsHome。
- 独立只读差异复核未发现阻断项；没有新增付费请求路径、没有修改 Prompt 或来源校验、没有改聊天/缓存提交算法。

Source SHA-256：`56fc30034f65aab73c86c806dac6f184c6f7812603fae0ccbb0308ff3385f313`

Bundle SHA-256：`d2e5930841d06357faf33e99c3036c99950c7fc9df2227042f39d7754ee2c77e`

Bundle 大小：2,735,500 bytes，比 r82 增加 6,090 bytes；没有新增依赖或资源图片。

## 仍需真机确认与费用边界

- 未登录用户云酒馆、未执行 TT/iPhone/安卓真实安装、浏览器视觉或付费模型请求。宿主、提供方和部分存储采用替身；自动测试不能证明真实服务稳定或手机耗时改善幅度。
- 用户 r81 日志的 relations、achievements 已保存；room 第一段成功但第二段断连，pastLives 和 butterfly 的成功段后仍有后续段。不能把子段成功当整个模块已提交。
- RMT_CONNECTION_FAILED 的底层网络或服务端原因没有出现在报告中；本次没有宣称修复该未知上游错误。终端 RMT_PHONE_EVIDENCE 仍需原返回来区别模型缺字段/缺来源与校验误拒，本轮不降低来源要求。
- 不同分段正常生成与同段失败重试不同。现代终端最多有目录加 10 个 App；本版在每段首次加一次重试都用尽的极端情况下仍可达 22 次传输尝试。不是必然发送次数，也不是账单次数；成功段本地验证后复用，不重新调用模型。
- 格式/证据校验失败不意味着提供方未产生用量，断连也不能证明对方没处理。实际费用以提供方用量记录为准。原诊断最多末 4 个子请求，不能据此反推旧任务全部请求或金额。
- 关闭档案室仍按原约定后台继续，不是取消。没有新增文字生成一键停止按钮；重试等待没有独立取消入口。禁用/销毁后的旧 runtime 会阻止新发送，但不能承诺撤销已经发送的上游请求或其费用。
- 未改既有世界书准备调用；宿主读取或同步主线程阻塞仍可能延迟，不能把本次调度修复理解为所有长等待已消除。

## 安装

先处理旧页面当前生成任务及仅本页保留的草稿，再用 ZIP 中 tokimemo-main 里面的内容覆盖原插件目录，完整刷新/重启后确认 0.8.78。不要安装第二份，也无需清空站点数据或重建已保存档案。刷新可能丢失未持久保存草稿；新版不会接管旧页面的在途任务。
