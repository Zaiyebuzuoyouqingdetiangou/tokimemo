# 心迹回廊：房间安全预算 / 长记忆储存上限 —— 根因、方案与审计

日期：2026-09-21，现场对照更新于 2026-09-22  
依据：两份真机诊断、三张界面截图、当时仓库源码（`0.8.111 / r84.35`）  
说明：下面的现场数字仍然有效。默认预算后来已改为 6 万，快照改为投影，同一来源正文只发一次。当时写的「本文不改代码」已经过时。哪些落地、哪些还没做，以 `施工方案.md` 为准。

诊断身份对照：

| 文件 | 现场 | 版本（诊断自报） | 关键失败 |
|---|---|---|---|
| `Hearttrace-diagnostic.json` | Tauri，60 楼，18 条记忆，房间 ×3 | `0.8.108-tt-cg-r84.32-oversize-egress` | `RMT_INPUT_BUDGET`，0 次模型请求 |
| `Hearttrace-diagnostic (1) 3.json` | HTTP，1850 楼，**240 条记忆满档**，相簿 ×3 + ADV ×1 | 同上 | `RMT_RECOVERY_SNAPSHOT_TOO_LARGE`，0 次模型请求 |

截图与诊断对齐：

1. 房间：红条「超过安全预算」叠蓝条「设定容量只装下 5/19」——且同样内容出现两遍。
2. 建档：1939 来源 / 1846 已保存 / 93 未完成 / 3 条待入档，「档案已达容量边界」。
3. 续写草稿：「尚未收到可独立阅读的完整条目」，继续按钮还在。

结论先说清楚：**用户感觉「没多少字」是对的；插件拦的也不是「所选那几条世界书」。两处失败都发生在发模型之前，费用为 0。** 过去几轮主要在换错误码和文案，没有拆开「真正送给模型的包」和「为了续写而克隆的包」。

---

## 1. 问题一：多人卡生成「他的房间」报超过安全字数

### 1.1 现场数字（不是感觉）

三次房间任务完全同构，约 220–233ms 内失败：

- 实际拼好的输入：**47,393 字符 / 34,282 tokens**
- 默认输入预算：**32,000 tokens**（设置项「输入预算」，不是「最大输出」）
- 字符硬顶：96,000 —— 所以 **字符检查过了，token 检查没过**
- `providerRequests: 0`

界面却只显示固定文案：

> 本次输入超过安全预算，已在发送前拦截；请精简档案或减少世界书，或在设置中提高输入预算。

原因：`safeErrorSummary()` 一旦看到白名单错误码，**丢掉** `client.js` 里带具体数字的那句，改用 `text.js` 的短文案。用户看不到 47393 / 34282 / 32000，只能以为「就勾了几条，怎么会超」。

蓝条「5/19」是另一件事：所选**设定世界书**按整条装进 12,000 字符袋，装不下的整条丢弃。它不是安全预算本身。

### 1.2 真正进房间请求的东西

房间请求不是「勾选条目 = 全部输入」。实际拼接是：

```
受控上下文信封（角色卡 JSON + 用户人设 JSON + 世界书正文）
+ 房间 Prompt 模板（很长的 JSON 合同）
+ 档案切片（最多 24 条记忆）
+ CONTROLLED_WORLD_PRESENTATION_JSON
+ 多人卡时：UNTRUSTED_SELECTED_PARTICIPANTS_JSON（含每人 sourceRefs 全文）
```

信封里的世界书又是两层叠在一起：

1. **自动 dry-run**：拿最多 64 条记忆的标题/摘要/锚点当扫描词，去激活宿主世界书，再截成 12,000 字。扫描词很长，容易把整本设定都扫亮。
2. **手选设定**：另有 12,000 字整条预算；两条合计硬顶 16,000 字。手选优先，dry-run 被挤到剩余空位。

多人卡还会再加一层：**每个被勾选人物的 `sourceRefs.content` 在人物快照里不截断**（`participantSource: true` 时上限是 Infinity）。这些全文再进 Prompt，和信封里的角色卡、设定条目高度重复。

所以用户勾 19 条，本意是「只要这些人/这些设定」；系统实际送出的是「整张多人卡 + 扫亮的世界书 + 装得下的 5 条 + 19 人全文快照 + 房间合同 + 档案切片」。47k 字符对中文 tokenizer 变成约 34k tokens，很正常（本日志约 **0.72 token/字符**，CJK 常见）。

60 楼、18 条记忆的聊天也会炸，说明瓶颈**不是高楼层**，是**多人卡设定包 + 默认 32k token 预算**。

### 1.3 为什么「勾选」没有变小

| 用户以为 | 代码实际 |
|---|---|
| 勾选 = 只用这些条目 | 勾选是**追加**；角色卡、人设、dry-run 世界书仍在 |
| 5/19 表示输入已经很小 | 表示 12k 设定袋只装下 5 条整条，另外 14 条被丢，**其余通道照旧** |
| 「安全字数」= 看见的条目字数 | 拦的是 **token 预算 32k**，不是条目字符 |
| 把「最大输出」调到 12 万能过 | 输出额度与输入预算无关（r84.31 已分开，但界面仍容易搞混） |
| 失败只弹一次 | `generateMode` 自己 toast，外层 `overlay` catch **再 toast 一次** → 截图里红/蓝条各两份 |

整条装袋还是贪心顺序：排在前面的大条目会占满 12k，后面更相关的短条目进不去。证据校验又要求「引用必须整句存在」，所以故意不截半条——合理，但用户完全看不到「哪 5 条进了、哪 14 条没进、各占多少字」。

### 1.4 方案（按优先级）

**P0 先让人看懂，再谈能不能发。**

1. Toast / 面板同时给出：字符、token、当前预算、差多少；拆开角色卡 / 人设 / dry-run 世界书 / 手选设定 / 档案切片 / 人物快照 / 模板。
2. 去掉双重 toast（只在一处报）。
3. 「5/19」改成可点开的清单：已送 / 未送 / 每条字符；未送不要跟红错叠成「整件事失败因为世界书」。

**P1 多人卡房间按「选定人物」收包，而不是整卡再加一遍。**

1. 已有手选设定或已有人物 `sourceRefs` 时，**不要再 dry-run 扫亮整本世界书**（或 dry-run 仅作补缺且去重）。
2. 信封里的 `CHARACTER_CARD_JSON` 对多人卡改为：卡标题 + **仅所选人物**的短资料；不要把全员长描述再塞一遍。
3. 人物快照与手选设定去重：同一世界书 uid 只保留一份。
4. 设定装袋改为「与所选人物匹配的条目优先」，不要纯文件顺序贪心。
5. 房间 Prompt 模板可拆：首次只要空间蓝图结构，把超长枚举/反例放到本地校验，而不是每次都把整份合同当输入。

**P2 预算语义与中文 token。**

1. 界面写明：默认 32,000 是 **token**；中文大约 1 字 ≈ 0.6–1 token，4 万字常见会超 32k。
2. 房间/多人卡可用更高默认输入预算，或按实测 token 自动建议（仍封顶 128k，并提示费用）。
3. 建档已不再用 32k 硬拦（r84.23）；房间等派生任务仍走 `assertPromptBudget`。要对齐产品语义：要么派生任务也「未知模型不拦、交给渠道」，要么明确告诉用户这是插件本地保险。

**不建议当主方案：** 只让用户去设置里把输入预算拉到 128k。那能绕过这次 34k，但多人卡重复打包还在，下一次换更大卡仍会炸，且费用随输入涨。

---

## 2. 问题二：高楼层长记忆总是「储存上限」，修了几次只换说辞

### 2.1 现场其实叠了两道闸，文案却混成一句

**闸 A —— 正式档案条数 240（`MAX_MEMORY_ITEMS`）**

建档截图是这道闸在工作：1846 条已保存、93 未完成、3 条已校验结果进「待入档」（不编号、不算完成）。这是既有设计，不是膨胀 bug。满档后不再为新来源付费请求，旧 Mxxx 保留。

**闸 B —— 派生任务续写快照 120 万字符（`GENERATION_RECOVERY_LIMITS.requestChars`）**

相簿/ADV 诊断是这道闸：240 条满档后点相簿/ADV，340–412ms 内 `RMT_RECOVERY_SNAPSHOT_TOO_LARGE`，**请求未发、草稿未建**。用户听见的「不能继续用了」，主要是这句，不是 240 本身。

两道闸数值上会在满档处撞车：240 条合法档案序列化后，常常大于 120 万字符快照预算。计数上限说「档案合法」，快照上限说「派生任务不准开始」。

### 2.2 「只换说辞」对应哪些轮次

| 轮次 | 实际做了什么 | 用户感知 |
|---|---|---|
| r84.30 | 满档时待入档可导出；新码分类保存失败 | 建档能导出，相簿/ADV 仍秒失败 |
| r84.32 | 把快照超限从 `RMT_RECOVERY_LIMIT` **改名为** `RMT_RECOVERY_SNAPSHOT_TOO_LARGE`；超大草稿可导出/放弃但不能续写。CURRENT 写明 **不做快照瘦身** | 红字换了，功能还是不能生成 |
| r84.33（仓库已有，诊断包仍是 r84.32） | 超预算时把记忆 `summary` 蒸到 1200/700 字；id/锚点等保留 | 只削摘要。当前角色卡仍 **整对象深拷贝** |
| 真机诊断 `0.8.108 / r84.32` | 用户当时跑的就是「改名那一版」 | 「修了好几次都没修好」 |

即使用上 r84.33，下面这包仍可能单独撑破 120 万：

```js
// captureGenerationContent：当前角色整卡 structuredClone
if (String(index) === String(context.characterId)) return cloneContentField(character);
```

整卡包括 `character_book`、extensions、示例对话等。多人卡/内嵌世界书动辄数十万到上百万字符。其它角色才截成 5 字段 × 5000 字；**当前这张反而最肥**。`fitGenerationContentSnapshot` 不碰这块。

派生 Prompt 真正用的档案是 `memoryPayload`：最多 64 条、summary 700 字。续写校验只用 id / title / anchors / 库级字段。把完整 240 条原文和整张角色卡塞进每个相簿/ADV 任务的恢复快照，是结构错误，不是「用户档案太大」。

### 2.3 方案（上限数字可以先不动）

产品合同多次写过：未经授权不把 240 和 120 万抬上去掩盖问题。下面方案遵守这一点。

**P0 让满档档案仍能开相簿/ADV（不扩容、不删成果）。**

1. 快照**不要** `structuredClone` 当前角色整对象。当前卡与其它卡同一口径：姓名、头像、若干人设字段上限。世界书走已有受控信封，不要藏在 `characters[i]` 里。
2. 快照里的 `memoryBank` 只留 Prompt/校验需要的投影（id、title、date、anchors、participants、messageRange、sourceKind、summary≤700），不要整库原文。r84.33 的蒸馏应变成**默认投影**，而不是「超限才削」。
3. 完整档案仍只存在正式存储；续写用 `archiveRevision + digest` 对活档案，对不上再走现有身份失败码（真换卡/换聊天仍阻断）。

这样 240 条满档、1850 楼也可以开相簿；闸 A 继续管建档条数，闸 B 不再误伤派生任务。

**P1 把三种「满了」拆开，停止换词。**

| 现象 | 应用的码 | 用户应看到 |
|---|---|---|
| 正式记忆 240 条 | 容量边界 | 「正式档案已 240 条；新结果在待入档，可导出；旧相簿/ADV 仍可生成」 |
| 续写快照 > 120 万 | `RMT_RECOVERY_SNAPSHOT_TOO_LARGE` | 「为续写保存的资料包过大（x / 120 万字符），尚未请求模型」+ 构成（卡 / 记忆 / 其它） |
| 草稿 journal > 180 万 | `RMT_RECOVERY_OVERSIZED` | 「这份未完成草稿不能继续生成，只能导出或放弃」 |
| 缓存 / 12MB | 缓存码 | 不要说成「记忆上限」 |

诊断里加：`snapshotChars`、`snapshotBudget`、`memoryCount/240`、是否已蒸馏。现在的诊断只有 `memoryCount: 240` 和错误码，看不出快照到底有多肥。

**P2 建档 240 的产品出口（另授权再做）。**

当前待入档导出是保全，不是无限档案。后续可选：按时间/重要性合并旧 Mxxx、第二库「冷归档」、或把 240 改成「进 Prompt 的热记忆」而不是「磁盘条数」。没有单独授权不要做。高楼层真正该做的是 **楼层覆盖账本 + 分批建档**（r84.34 已有方向），不是让 240 条再膨胀。

**明确不要再做的事：** 只改 `text.js` 文案；只把 120 万改成 200 万；只把 240 改成 500。都会在下一张大卡/下一次满档重演。

---

## 3. 审计：明显问题（不限于上述两处）

### 3.1 会直接让用户觉得「修不好」的

1. **错误码文案吞数字。** 白名单码优先于具体 `message`。预算、快照、容量全变成同一句「精简/上限」。
2. **失败 toast 双重绑定。** `generateMode` 内 `toastr.error` + `overlay` catch 再弹。截图已证实。
3. **`ui/` 与 `modes/` 双份同名文件。** CURRENT 已警告必须以入口可达性为准。`overlay.js`、`settingsPanel.js`、`calendarView.js` 等两边都有。改错副本 = 「我改了但线上没变」。这是长期回归源。
4. **当前角色整卡进恢复快照。** 与「其它卡只留身份字段」不一致，是满档派生任务全灭的主因候选。
5. **手选设定 + dry-run 世界书 + 人物 sourceRefs 三路重复。** 勾选不仅不减小输入，还可能变大。
6. **dry-run 扫描词 = 最多 64 条记忆摘要。** 等于用整本档案当关键词，世界书几乎全亮，再截 12k。与「用户精心勾选」对着干。
7. **默认输入 32k token vs 字符顶 96k。** 中文会稳定踩中「字符看着不多、token 已超」。建档已取消这道硬拦，派生任务没有。
8. **「最大输出」与「输入预算」并列但失败文案不提当前值。** 用户已经有一次把输出调到 12 万仍失败的记录。

### 3.2 容量与状态机缠在一起

9. **240 条、120 万快照、180 万 journal、12MB 缓存、16k 设定袋、96k 字符、32k token** 至少七道闸。失败都像「存不下了」。
10. **满档派生任务在 `createGenerationRecovery` 处失败，草稿不建。** 于是没有「导出/放弃」卡，只剩 toast。r84.32 给超大**已有**草稿开了出口，没给「快照过大根本建不出草稿」开入口。
11. **待入档 3 条与 93 未完成并存。** 数字自洽，但「重试未完成部分」在 240 满档时不应再让人以为还能变成 Mxxx。
12. **缓存诊断：60 楼房间现场 `sourceChars: 814,218`。** 楼层不多，派生缓存已经很大。1850 楼那份压缩后仍有 82k base64。缓存与快照是两条线，但都会在长线聊天上发沉。

### 3.3 工程与产品债

13. 全量测试长期约 **160 项失败**（冻结哈希、退役 timeJourney 等）。分类过，不等于健康；改预算/快照极易被冻结测试误伤或反过来不敢动真问题。
14. CURRENT 极长，把历史交付、合同、未执行项堆在同一文件。新人会把「r84.32 明确不做瘦身」和「r84.33 做了蒸馏」读成互相取消。
15. 真机 / 原生 IndexedDB / TT 多次标明未验收。房间预算和满档快照恰恰是真机才爆的问题。
16. 房间生成失败后还有字段修补、最终局部修复等多段请求设计；当前连第一段预算都过不了，后面的修复链是死代码路径。
17. 多人卡适配在 CURRENT §4.11 写过「此前暂停」；仓库里房间/相簿已有 participant 路径。产品状态和代码状态不完全一致。
18. `timeEcho` 约 38 分钟超时仍为待查；与本次两问题无关，但同属「长任务只换提示、没有阶段预算」。

### 3.4 相对不是 bug、但必须跟用户讲清的边界

- 建档 240 条、待入档导出、不自动无限归档：有意设计。
- 整条设定不截半：为了证据逐字校验。
- 发送前拦截超大输入：避免费钱。问题是拦的内容和说明不对。
- 换角色/换聊天阻止续写：保护；r84.33 已把「同一张卡被宿主规范化」从误判里拆出去。不要为了好用而拆掉真换卡保护。

---

## 4. 后续功能与优化方向

下面按「先把现有玩法打通，再加新玩法」排。前两项就是本文两个主问题。

### 4.1 必须先做（否则长线聊天和多人卡不可用）

1. 输入构成可视化：生成前预检一页，显示各块字符/token，可关掉 dry-run、可只对所选人物打包。
2. 恢复快照改为投影，满 240 条仍能生成相簿/ADV/房间。
3. 错误模型：码、数字、下一步（提高输入预算 / 减少勾选 / 导出待入档 / 无关）分开。
4. 消灭双 toast、统一 `ui` vs `modes` 入口。

### 4.2 建档与长记忆（高楼层的正路）

5. 把 240 条解释成「热记忆 / 进 Prompt 的窗口」，冷记忆可查可搜，但不默认塞进每个派生请求。
6. 楼层覆盖账本（r84.34 已开工）：缺口补录、增量标签、不要假装已经全覆盖。
7. 外部摘要（千千结/柏宝书/世界书历史）与聊天正文分栏计数，避免「1939 片段 vs 1850 楼」让人以为丢楼。
8. 满档合并：手动选一组 Mxxx → 一条「阶段综述」，腾出热位；原条进冷归档而不是删除。

### 4.3 多人卡

9. 生成范围 = 勾选名单（审计称相簿链路已对）；房间应同样：**没勾选的人不进信封、不进外貌、不进 residents**。
10. 人物条目选择器与设定世界书选择器合并成一次「本轮带上哪些人/哪些设定」，避免两套勾选互相加码。
11. 各人房间/终端可以共享空间蓝图、分人日程，但请求按人分片，避免一次 34k token。

### 4.4 生成体验

12. 预检通过后再创建恢复草稿，避免「失败了但没有草稿卡」。
13. 本地可算的内容（外形枚举、时态、空间列表）尽量不进模型；模型只写需要文笔的字段。
14. 输入预算按模式建议：建档跟渠道走，房间/相簿给中文默认更高一档，并写费用提示。
15. 诊断导出已有；可加「本次将被发送的构成」开关（仍不要把正文/Key 写进可分享日志）。

### 4.5 性能与存储

16. 派生缓存已压缩，但 60 楼就能到 80 万源字符，说明各模式 session 偏肥。房间 SVG 状态、相簿 CG 历史、timeEcho 正文应分库，不要全塞 `chatMetadata`。
17. TT / 切后台紧急落盘（r84.34）值得做成所有长任务的统一纪律。
18. 诊断增加 snapshot 字节、信封各段字符，否则以后还是只能猜。

### 4.6 可以晚做的产品向

19. 记忆检索：按人物/时间/关键词取证，而不是每次均匀抽样 24/64 条。
20. 相簿/ADV 对满档改为「只引用未做过的热记忆」，减少重复收费。
21. 世界书「作为历史」与「作为设定」在 UI 上彻底分开（CURRENT 已规定四条通路，界面仍容易混）。
22. 不要继续堆新模式，直到房间、相簿、建档三条主路径在多人卡 + 2000 楼真机上能走完。

---

## 5. 建议的实施顺序（仍等你明确授权再改代码）

1. **只改说明层：** 预算/快照数字进 toast、去掉双弹、诊断加 snapshot 尺寸。不改 Prompt、不改上限。用于确认用户看到的数字与这两份日志一致。
2. **房间打包：** 关 dry-run 或与手选去重；多人卡信封只带所选人物；设定袋按人物优先。目标：同类 5/19 现场降到 32k token 以内，或明确差在哪一块。
3. **快照投影：** 当前卡不再整对象克隆；记忆只留校验/Prompt 投影。目标：240 条满档可以点相簿/ADV，且 0 次误伤身份保护。
4. **再谈 240 的产品出口**（合并/冷归档），另开授权。

验收时不要再用「文案是否更温和」当通过条件。应用日志说话：

- 房间：`input.chars` / `input.tokens` / `budgetTokens`，以及各段字符。
- 满档派生：`providerRequests` 仍可以是 0（若仍超限），但必须不再把「24 万合法档案」说成「不能再用插件」；投影后应能发出至少一次请求或给出带数字的构成失败。

---

## 6. 与这两份日志直接对应的一句话

- **房间：** 不是 19 条勾选有 4 万字；是「整卡 + 扫亮世界书 + 5 条设定 + 人物全文 + 模板」≈ 4.7 万字符 ≈ 3.4 万 tokens，超过默认 3.2 万。界面还把具体数字吞掉，并弹了两遍。
- **长记忆：** 不是 1850 楼把磁盘撑爆；是满 240 条后，每个相簿/ADV 任务仍要把整库+整卡塞进 120 万字符的续写快照。r84.32 只换了错误名；蒸馏摘要不够，因为最肥的是当前角色整卡拷贝。

---

## 7. r84.36 复审补充（2026-09-22）

复审基准：当前仓库与实际 bundle 均为 `0.9.0 / r84.36-budget-capacity-scenes`。入口是 `index.js → dist/heartbeatMemories.bundle.js → src/heartbeatMemories.js`；界面入口使用 `src/ui/*`，`src/modes/overlay.js`、`settingsPanel.js` 等同名副本没有进入当前 bundle。所以上文是 **r84.32–r84.34 的历史诊断**，不能再把其中所有结论当成当前事实。

### 7.1 旧问题哪些已经修好

| 旧问题 | r84.36 状态 | 当前代码证据与判断 |
|---|---|---|
| 预算错误吞掉具体数字 | **已修复** | `src/generation/client.js:497-526` 给错误附加 `inputBudget`；`src/core/text.js:239-242` 会显示字符、tokens、预算和字符顶。 |
| `generateMode` 与外层 catch 双弹同一个错误 toast | **已修复** | `src/generation/client.js:1729-1743` 设置 `error.notified`，`src/ui/overlay.js` 的外层会避开已通知错误。 |
| 默认 32,000 tokens 对中文房间过紧 | **缓解，未根治** | 默认已改为 60,000，字符顶随之为 180,000；恰好保存旧默认 32,000 的设置会迁移到 60,000（`src/core/outputBudget.js:24-33`）。但用户手动设置的低预算仍可触发，重复打包也仍在。 |
| 有手选设定仍同时 dry-run 世界书 | **已修复主要路径** | `src/core/cache.js:3051-3077` 在 `selectedSettingText` 非空时跳过 `getWorldInfoPrompt`。没有手选设定时仍会 dry-run，这是预期路径。 |
| dry-run 用 64 条记忆摘要当扫描词 | **旧结论已过时** | 当前房间入口传的是模式固定词与角色名（`src/generation/client.js:227-236, 339-342`），不是 64 条记忆摘要。 |
| 当前角色整卡 `structuredClone` 进恢复快照 | **已修复** | `captureGenerationContent()` 现在只保留身份字段、角色常用字段各最多 5,000 字，以及记忆投影（summary≤700）；见 `src/generation/client.js:103-168`。上文第 2 节把「整卡克隆」视为当前主因的结论已失效。 |
| 240 条合法档案因整库快照而让相簿/ADV 秒失败 | **核心结构已修，仍需真机复验** | 记忆默认按投影进入快照，`fitGenerationContentSnapshot()` 仍保留 120 万兜底（`src/generation/client.js:170-200`）。正常 240 条不应再因旧版整库/整卡复制而失败。 |
| 快照超限只给短文案、没有可处理记录 | **已修复** | `src/generation/recovery.js:360-387` 记录实际总字符、角色卡字符、记忆投影字符，保存 blocked journal 后再抛错；`src/ui/recoveryView.js:21-37` 只提供导出/放弃，不显示续写按钮。 |
| 「blocked 草稿仍可点重试」 | **当前不存在** | `generationRecoverySummary()` 把 blocked 同时标为 `oversized`（`src/generation/recovery.js:271-288`），界面以 `oversized` 隐藏 continueButton。不要按旧截图继续登记为现存 bug。 |

### 7.2 旧问题哪些只修了一半或没修

1. **多人房间仍没有真正做到「只按选定人物收包」。**  
   `buildControlledContextEnvelope()` 已支持 `participantSnapshot` 并可把角色卡缩成 `selectedPeople`（`src/core/cache.js:3052, 3086-3095`），但 `buildWorldPresentationContextFresh()` 调用它时只传了扫描词和手选设定，没有传人物快照（`src/generation/client.js:339-342`）。也就是说能力写了，房间主链没有接上。

2. **人物 `sourceRefs` 仍是不截断全文。**  
   人物选择器以 `participantSource: true` 读取条目；`src/archive/repository.js:331-348` 对这条路径使用 `Infinity`，`src/core/participants.js:27-35, 104-107` 又把完整 content 序列化进 `UNTRUSTED_SELECTED_PARTICIPANTS_JSON`。相簿会清掉 sourceRefs（`src/modes/album.js:27`），房间不会。大条目、多人物时，60k 默认只是把爆点往后推。

3. **人物全文、手选设定、受控信封之间仍没有按 world+uid 去重。**  
   手选设定会进入 `WORLD_INFO_TEXT`，同一条还可能存在于人物 `sourceRefs`；房间 Prompt 又附加完整人物快照。原文第 1 节指出的三路重复，当前只去掉了「手选时再 dry-run」这一层。

4. **设定袋仍是顺序贪心。**  
   `collectFittingSelectedSetting()` 按返回顺序整条装入，超预算就跳过（`src/generation/client.js:279-289`）；没有按所选人物、相关性或短条优先，也没有可展开的「已送/未送/每条字符」清单。

5. **没有发送前的输入构成预检。**  
   超预算后能看到总字符/token，但看不到模板、信封、设定、人物快照、档案切片分别占多少。房间包为什么大，仍要靠诊断和读代码猜。

6. **`ui/` / `modes/` 双份同名文件的维护陷阱仍在。**  
   当前运行时已明确走 `ui/`，所以它不再是「两个副本同时执行」的线上 bug；但维护者改到 `src/modes/overlay.js` 等不可达副本，依然会出现「源码改了、线上没变」。

7. **240 的产品语义已经改变，原文需要作废一部分。**  
   r84.36 不再是简单的「正式档案满 240 后全部待入档」：现在是热位 240，最早的未锁定记忆自动移到冷归档；冷归档最多 100，继续溢出会删除最早冷存（`src/archive/capacity.js:31-72, 89-141`）。这是明确实现并有提示，不算隐藏 bug，但与上文 §2.1、§2.3、§3.4 的旧产品边界不同。若产品要求「不自动删任何旧成果」，当前实现反而不符合该要求。

### 7.3 本轮新发现的明显缺陷

#### P0：切换聊天后，已打开的内容页不会刷新，可能对错聊天执行操作

`bindChatStateEvents()` 在 `CHAT_CHANGED / CHAT_LOADED` 时只把 `activeMode`、`activeSession` 清空（`src/ui/archivePortal.js:254-281`），没有清空内容 DOM，也没有强制退回选择页。随后调用的 `scheduleChooserRefresh()` 又只在 `archiveViewLevel === 'chooser'` 时工作（`src/archive/snapshots.js:78-99`）。

因此，如果用户正停在房间、相簿、ADV 等内容页时切换聊天：

- 屏幕仍可能显示旧聊天的内容；
- 运行时上下文已经变成新聊天；
- 再点生成、继续或房间深层操作时，代码可能按新聊天重新 `loadSession()` / 读取档案，形成「看着 A，操作 B」；
- 这是付费请求与写缓存都可能打错目标的高风险状态问题。

同角色切换聊天时还有一个旁支：`resetArchiveOverviewForCharacter()` 只有角色 key 改变才清 `activeArchiveSnapshot`（`src/archive/snapshots.js:65-75`）。若停在历史档案快照页，同角色换聊天后旧快照也可能继续显示。

建议：聊天事件发生且 overlay 可见时，不论当前 view level，都应使现有 reader 失效并退回 chooser；若任务正在运行，只 background 任务本身，不保留可交互的旧 DOM。`activeArchiveSnapshot` 也应按 chatId 核对，而不只按角色核对。

#### P1：失败页可能重复插入第二份恢复操作条

多个模式页面本身已经渲染 `recoveryBannerHtml()`；生成前景失败时，`showInlineError()` 又会在错误框中追加一份当前模式的恢复 banner（`src/ui/overlay.js:907-924`）。结果可能同屏出现两套「继续/导出/放弃」，尤其部分成果已经落盘、页面原有恢复区还在时最明显。

建议：`showInlineError()` 只显示错误；恢复区统一由当前页面刷新。若必须在错误框里显示，应先检测页面是否已有同 draftId/pageId 的 recovery 节点。

#### P1：设定容量 info 与最终预算 error 仍会形成「一轮两条提示」

旧的「同一个错误被两层 catch 弹两遍」已经修复，但另一种双提示仍在：`generateModeOperation()` 在真正请求前先为 `selectedSetting.note` 弹 info（`src/generation/client.js:1483-1489`），随后 `assertPromptBudget` 失败又弹 error。于是原截图那种「5/19 蓝条 + 超预算红条」仍可复现，只是两条现在代表不同事实。

这不是重复 catch，但 UX 仍有误导：用户会把「只装下 5/19」理解成预算失败原因。建议先完成整轮预检，再把设定裁剪与总预算合成一个结果面板；失败时不要先弹成功式 info。

### 7.4 当前结论（替换原文第 6 节的一句话）

- **房间：** r84.36 已把默认输入预算升到 60k、显示具体预算数字、修掉同错双 toast，并在手选设定时关闭 dry-run；但多人房间仍把未截断人物 sourceRefs 放进 Prompt，且没有把 participantSnapshot 接入受控信封的瘦身分支，也没有跨通道去重。旧现场大概率不再卡在 34k > 32k，但根因只修了一半。
- **长记忆：** 当前角色整卡克隆和整库快照主因已经修掉，240 条改为投影快照；原文「满 240 必然撑爆 120 万」不再成立。新的容量模型是「热 240 + 冷 100 + 冷档溢出删除」，需要按这个新合同重新做 2000 楼真机验收。
- **新增最高风险：** 不是容量，而是切聊天后旧内容页仍可交互，存在对错误聊天发请求或写入的可能。这个应排在下一轮修复首位。
