# r84.8 · 按实际生成数量展示

版本：`0.8.84-tt-cg-r84.8-actual-count`
基线：`Hearttrace-r84.7-scopefix-full.zip`，未混入 GitHub 或其他版本。

## 本轮行为

- 基础语言按实际句数可用；生日/节日等类别为空，不再因为配额缺项判整组失败。可选择一个类别手动追加，替换整套台词仍需两次确认。
- 萤火虫批次不再必须凑 5～6 个或固定颜色分布。每颗仍必须是一段完整会话。日常一格仍要求自身格数、内容和图片提示词完整；同批合格项可以保存，实际坏项单列提示且不自动补数。
- 私人终端正常少返回条目时，不再为未返回的候选创建失败占位；本次正常略过的 ID 由本地记录并绑定原目录，重开/中断续写不重新索取它们。确实返回但无效的条目仍为可选重试；合格条目保留。
- 房间/物品/生活节点的集合最低数量改为实际可用数量，一处完整房间也可保存和重开。出行数量只作为生成建议；单条字段、事实证据与容量上限仍保留。
- 春夏秋冬按已保存篇章阅读；不再因 Voice/Scenario 数量不相等就显示必须补完。真实未完成请求仍可继续，只补原任务未完成部分。

## 这次没有改变

相簿、ADV EVENT、ENDING、蝴蝶效应、正式档案覆盖范围，共用 Prompt 的安全与事实边界，亲密称呼证据规则，历史 Mxxx/anchor 校验，原有段落/单篇会话结构，容量上限、CAS、删除栅栏、只读隔离、并发/超时和原模型连接协议。
`src/core/cache.js` 仅将房间读取最低空间数从 2 改成 1；其余字节经过原包哈希对照。bootstrap 仅更新版本与 build 身份。无外置 CSS bundle 或新增 stylesheet link。

## 验证（工作目录与最终 ZIP 解压后复核）

- 新增行为测试：35/35。
- 新增单条结构/证据/启动合同：16/16。
- 全量测试（不排除文件）：486 项，481 通过，5 失败。原 r84.7 为 433 项，428 通过，完全相同的 5 个失败均属于已退役的 r81TimeJourney 旧测试；没有新增失败。
- JS/MJS 语法：220/220。
- Runtime：98 模块，连续两次重建相同。
- Chromium 隔离 UI：基础语言、终端状态、模型选择器各在 320/375/390/430/1366px 验证，选择/展开可用，未观察到文档横向溢出。
- 未执行：iPhone/Android 真机、TT/SillyTavern 实际环境、真实模型/API、用户数据迁移实测。

## 差异安全复核

本轮检查了正常少返回与真正失败的区分、候选 ID 篡改、遗漏 ID 与实际条目冲突、截断 JSON、保存失败、切换聊天、永久只读备份和成功项保留。复核补齐了一处“单房间能存却不能读”的旧门槛、初次物品推演正文的共同历史防线，以及 HEART 历史目标刷新遗漏的模块引用。
未发现本轮差异仍待处理的 High/Medium。Codex Security 插件可发现但无可调用扫描动作，专用扫描未执行，独立回执未取得。GitHub 无写入。

## 使用与旧记录

这是完整候选包。等在途任务结束，保留旧目录备份，同目录完整覆盖后刷新/重启，并检查诊断中的新 build。无需清除站点数据、聊天或档案；已有作品继续阅读。
旧版本已经保存的真实失败/待补记录不会自动改成成功，也不会删除其恢复草稿；它们保留为可选重试。新完成的短集合适用本轮实际数量规则。保存失败或截断内容仍不能算“已保存”。
