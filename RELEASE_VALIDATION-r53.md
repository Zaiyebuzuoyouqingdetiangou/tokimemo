# r53.0 候选验证

版本 0.8.49；基于 r52 工作副本独立修订，原始 ZIP 和旧工作目录保留。本轮只处理用户最新的配色层次、设置折叠、蝴蝶效应数量三项；未继续柏宝绘接入或实现建议中的新玩法，也没有推送远端。

## 根因与实现

- 统一 structuralThemeCss 的 surface/文字规则覆盖了黄便签、粉备注、紫随笔与纸页。现有主题服务增加本地语义纸色/ink 配对；卡片、纸张、背景分层，四种 GS 灵感主色更鲜明。正文继续正常字重与足够留白，不恢复旧版过小淡字。
- 原设置卡片没有折叠容器。API、标签、主题、自动更新、记忆来源改为默认关闭的原生 details；折叠不拆卸控件、不清未保存输入。档案和更新入口不折叠，专门的配置按钮只展开 API 区。
- 蝴蝶效应在提示词、请求序列、最终校验三处强制十节点。现在只有 buildButterflyPlan 决定初始数量：有效且有可引用标题/锚点的独立 Mxxx 数 n，普通分歧 min(8,ceil(n/3))，加 MAIN 和 Ω，总计 3～10。无有效锚点零请求；最终数量与轴顺序必须和本地计划一致。

已有便签立即使用新样式，不需要重生成。旧十节点原样读取，小档案仍可后续追加。MAIN 来源、世界条件、去重、关系安全、Ω 内容完整性、保存事务与失败不覆盖的边界不变。

## 独立审查及反例

两位 reviewer 先完整阅读 CURRENT 并只读审查；主 agent 汇总根因后独立修改全部生产代码，没有合并任何 reviewer 补丁。

- 语义覆盖：独立 Edge 复现五种纸张同白底同阴影；实际日历 renderer 现已进入主题矩阵。
- 自定义灰底极端装饰色：原增强渐变末端对比降至 3.775。主 agent 复现后改为主题服务检查混色端点和中间采样，不合格减少装饰强度；补数学与真实 computed-gradient 用例。
- 单字标题、非空摘要但空锚点：原计划会误计并请求 MAIN。现在与 MAIN 证据词条件对齐，补零请求用例。为避免宿主依赖循环，数量契约仍为纯模块。
- 设置独立 Edge 复核：默认全收起，点击/Enter/Space 有效，折叠后未保存的手动 API 输入保留，插件外样式未受影响。

## 工作副本验证

| 检查 | 结果 |
| --- | --- |
| 完整 Node 回归 | 360/360，通过 |
| r53 定向测试 | 6/6，通过；覆盖数量边界、缺槽/错轴/额外Ω、无锚点零请求、旧十节点与小档案增量、纸张对比及极端自定义色 |
| JS/MJS 语法与模块导入 | 97 个文件通过，ESM 入口可导入 |
| 确定性 bundle | 两次相同，62 个可达模块 |
| Edge 主题与实际页面 | 91 场景通过：八主题、实际日历/房间/互动等页面、纸张差异、折叠键盘操作、极端渐变、移动尺寸与宿主污染 |
| Edge 设置/调度/更新 | 4 组通过；展开后设置保存/重载、双页锁和隔离、更新按钮原链路。本地模拟宿主，无真实网络更新 |
| Edge 惰性启动 | 320/375/390/430/768px 通过；普通启动及性能诊断的完整 runtime 请求均为 0 |

截图使用虚构角色和本地合成宿主；主 agent 目视检查黄/蓝便签、夜间纸张、折叠设置及 GS 配色。以上不等于真机实测。

ZIP 的 CRC、文件数、逐文件 SHA-256、全新解压与解压后测试记录见压缩包旁的 verification.json 和交付验证说明。不会把旧版结果或仅工作树检查冒充封包验收。

## 仍待用户实测

真实 SillyTavern / TT / iPhone 的第三方主题组合；真实模型的叙事质量、成功率与耗时；实际宿主权限和更新网络。没有付费模型调用，不能保证任意服务商都成功。首次节点减少不影响后续“增量追加”，也不把模拟写为正式历史。
