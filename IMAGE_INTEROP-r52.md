# 生图标签接入：本轮结论

用户给出的线索是 `<bbi_image>…</bbi_image>`，图片文字提到“其他绘图软件用另一个名称，可以自定义图片格式”。这是一条可研究的方向，但还不是已经确认的接口协议。

## 标签不是类名

`<bbi_image>` 是标记/标签写法；CSS 类通常是 `class="bbi_image"`。一个绘图插件可以解析文本标签、查找 DOM 元素、订阅消息事件，或只认模型回复中特定片段。仅写出标签并不意味着它会生成，更不意味着会把图片结果返回给缘侧。

“自定义图片格式”可能指可配置的提示词包裹格式。具体是格式模板还是渲染选择器，需要看消费者代码，不能凭这一句话选定实现。

## 拿到仓库后要核实的完整链路

1. 支持版本、许可证/授权范围、真正识别的标签名、属性与内文格式。
2. 消费位置：模型原文、聊天消息 DOM、独立 HTML、指定渲染器，还是公开的生成方法。
3. 触发方式：自动扫描还是用户点击；怎样防止重绘、重复扣费、同段多图或引用标签误触发。
4. 结果回传：怎样拿到正确请求的图片 URL/失败状态，而不是“看见页面上最新一张图就收走”。
5. 取消、聊天切换、并发、关闭窗口、卸载插件时，结果仍绑定原档案/事件。
6. 图片 URL 和最终展示继续走已有安全校验。不能从私有配置读取别人的 API Key，不执行模型 HTML/CSS/JS。

## 实现方向的取舍

- 优先公开的生成/渲染协议；如果作者提供正式标签消费者桥接，也可以成为一个显式可选适配器。
- 对于“标签只在聊天正文里生效”的实现，不能暗中插一条假聊天来触发。需要作者提供独立渲染入口，或另行设计用户明确选择的导出/粘贴路径。
- 可配置“标签格式”应当只是受限的数据格式，不接受任意脚本、选择器执行或跨页面扫描。
- 当前已实现的公开生图能力和 `/sd quiet=true` 路线保留；本轮没有冒充已适配柏宝绘，也没有自动付费生图。

**待用户提供：已获授权的柏宝绘仓库 URL，最好同时注明正在使用的版本。** 拿到后才能验证是否支持独立档案中的标签调用与结果回收。
