# 心迹回廊 r84.37 施工方案：跨聊天隔离、多人房间收包与生成预检

日期：2026-09-22  
基线：`0.9.0 / r84.36-budget-capacity-scenes`  
依据：`问题分析-房间预算与长记忆上限.md` 第 7 节、当前源码与实际 bundle 可达链  
状态：**方案已拍板（切聊确认并中止任务、B1 / C1 / D1 / E1 / O1）；本轮包含统一任务中心，其余扩展优化后续分轮。可据此实施。本文不代表代码已经修改或真机已经验收。**

---

## 0. 本轮目标

本轮不再重复 r84.36 已完成的默认 60k 输入预算、快照投影、blocked 草稿出口、热 240 + 冷 100 等工作，只处理复审后仍存在的真实缺口：

1. **先修跨聊天状态错位。** 当前聊天有后台任务时，常规切聊必须先确认；用户仍要切换则中止当前聊天任务。切换成功后旧内容页不得继续交互，更不得对新聊天发请求或写缓存。
2. **真正收紧多人房间输入。** 选定人物后，角色信封、人物资料和世界书正文只保留一套受控来源；不再把 `sourceRefs.content` 无界重复塞进 Prompt。
3. **生成前统一预算账本。** 在任何 toast 之前完成输入构成和裁剪计算；设定裁剪与总预算失败只给一次一致的结果。
4. **恢复区只保留一份操作入口。** 失败信息与继续/导出/放弃不重复渲染。
5. **新增统一任务中心。** 用户能看见所有运行中任务的聊天归属、阶段、进度和落盘状态，并可单项取消。
6. **补齐诊断、测试与真机验收。** 不用“文案变了”代替功能通过。

### 本轮保持不变

- 不改 60,000 默认输入预算和 8,000–200,000 用户范围。
- 不提高 120 万恢复快照、180 万 journal、12MB 缓存等安全上限。
- 不取消换角色、换聊天、档案 revision、CAS、删除栅栏和来源一致性保护。
- 不自动重试模型、不自动换模型/API、不把失败请求偷偷拆成多次收费请求。
- 不改已有创作 Prompt 的任务语义；若需改变传入资料，只改上下文装配层。
- 热位 240、冷归档 100、锁定和滚动规则沿用已拍板合同；本轮不重做容量产品。

---

## 1. P0：修复切换聊天后旧页面仍可操作

### 1.1 根因

`src/ui/archivePortal.js` 的聊天事件处理只清：

- `runtimeState.activeMode`
- `runtimeState.activeSession`

但不清旧内容 DOM，不强制退回 chooser。随后调用的 `scheduleChooserRefresh()` 只在 `archiveViewLevel === 'chooser'` 时生效。历史快照又主要按角色 key 清理，同角色换聊天时可能保留旧 `activeArchiveSnapshot`。

这会形成三层状态不一致：

```text
屏幕 DOM                  仍显示聊天 A
runtime activeMode        已清空
currentCharacterGuard()   已切到聊天 B
```

### 1.2 施工方式

本项目已有 `HOST_CHAT_NAVIGATION_SELECTOR`、`hostChatNavigationTargetFromEvent()` 和 `hasCurrentChatBlockingTask()`，但当前 guard 只把任务标成后台继续。改为“导航确认 + 当前聊天任务集中取消 + 页面失效”三段式。

新增统一入口，例如：

```text
confirmAndCancelForChatNavigation(event, target)
cancelCurrentChatBlockingTasks(context, reason)
invalidateArchiveViewForChatNavigation(previousScope, nextScope)
```

常规宿主切聊点击的处理顺序：

1. 捕获宿主导航 click，读取 `currentChatBlockingTasks()` 的任务名称。
2. 没有当前聊天任务：不拦宿主事件，正常切换。
3. 有任务：在宿主处理 click 前弹确认，明确列出任务，并说明“继续切换会中止；已确认落盘的成果保留，未完成部分停止，不会自动重试”。
4. 用户取消：`preventDefault + stopImmediatePropagation`，留在原聊天，任务继续。
5. 用户确认：立即对当前聊天任务发出 abort，再允许这次原始宿主 click 继续；不能先切聊再把任务改绑到新聊天。
6. 已经收到且通过校验的分段按现有 journal/草稿规则落盘；尚未收到的请求和后续分段停止。取消不回滚既有正式成果，也不把未完成标成成功。
7. overlay 走完整关闭流程，停止房间/电话等仅服务 DOM 的计时器，清空 `activeMode`、`activeSession`、旧 reader 临时选择和焦点引用。
8. `activeArchiveSnapshot` 的 `chatId` 与新聊天不同则立即清空；不能只按角色 key 判断。
9. 切聊事件本身不 hydrate 新聊天，也不自动重新打开心迹回廊。用户之后主动打开时，才按新聊天从 chooser 进入。

集中取消 API 必须覆盖当前聊天的全部任务种类：

- 建档/简介的 `activeTaskAbortController`；
- logical generation task 及其 provider 子请求；
- `activeGenerationTasks`、ADV 批量准备、房间今日生活；
- CG/图片生成；
- 仍在准备、token 计数、等待 provider 名额或保存收尾的任务。

同一任务可能同时出现在 logical task 与具体 provider map 中，取消必须按 task id/origin 去重。发出 abort 后无需阻塞宿主导航等待网络 promise 完全 settle，但原任务的最终清理仍要完成并写为 `cancelled`，不能留下永久 busy。

### 1.3 防抖与事件顺序

公开事件只有切换后的 `CHAT_CHANGED`，没有 before-chat-change。常规 UI 导航由 click guard 提前确认；事件层负责最终兜底和幂等清理：

- 导航 guard 记录一次性 `navigationPermit`（来源 chat scope、时间、目标元素类别），只允许本次已确认点击通过。
- 每次 chat 事件递增 `chatNavigationEpoch`，并消费对应 permit；不能把一次确认复用于下一次切聊。
- 第一个事件关闭 overlay 和清理 reader；重复事件发现已经关闭后只更新 epoch，不重复销毁或恢复焦点。
- 切聊前已经启动的异步 reader/hydrate 完成时核对 epoch 和 chat scope，过期结果不得重绘；切聊事件本身不启动新 hydrate。
- 任一迟到结果只能更新对应缓存，不能提交当前 DOM。
- 脚本、其他插件或宿主内部程序化切聊可能绕过 click guard；其切换后兜底行为见第 9 节待拍板项 E。

### 1.4 验收

1. 无任务切聊：不弹确认，overlay 关闭，切换正常完成。
2. 有建档、派生生成、CG 或今日生活任务时切聊：确认框列出正确任务名。
3. 点取消：聊天不切换、任务不中止、overlay 不被错误关闭。
4. 点继续：聊天切换，当前聊天任务全部收到 abort；未完成请求不再继续收费，已落盘成果保留。
5. 同角色 A→B：不得保留 A 的 reader 或 `activeArchiveSnapshot`；重新主动打开后显示 B 的 chooser。
6. 不同角色 A→B：同样清理；重新主动打开后显示 B 的 chooser。
7. 等待 provider 名额、分段间隙、token 计数阶段也必须能取消，不能只取消正在 fetch 的请求。
8. `CHAT_CHANGED + CHAT_LOADED` 连发只执行一次幂等清理；一次 permit 不得放过第二次导航。
9. 切聊不主动 hydrate、不显示旧聊天内容、不触发新的模型请求。

### 1.5 本轮新增：统一任务中心

任务中心是切聊确认的配套能力，本轮纳入正式施工：

- 心迹回廊顶部提供“任务”入口和运行数量；
- 显示任务名称、所属聊天、阶段、分段进度、已收到/已落盘数量；
- 区分准备、等待 provider、请求、校验、保存、取消中、完成和失败；
- 支持单项取消和“取消当前聊天全部任务”；
- 取消按钮复用 `cancelCurrentChatBlockingTasks` 的同一底层，不再为每种模式各写一套 abort；
- 已完成任务提供“回到原聊天并打开结果”，但必须经宿主支持的安全导航入口和身份复核，不能直接改运行时 chatId；
- 任务结束后保留短期只读结果摘要，正文仍从正式缓存/草稿读取，不在任务中心复制一份。

任务中心不显示 API Key、Prompt 正文、世界书正文或完整角色资料。

验收：

1. 建档、普通派生、ADV 批量、CG、今日生活都能显示且名称正确。
2. 等待 provider 名额的任务也可取消。
3. 单项取消不影响同聊天其他任务；取消全部只影响当前聊天。
4. 已取消任务最终离开 busy/任务列表，不留下假运行状态。
5. 切聊确认框使用任务中心同一任务快照，二者数量与名称一致。

---

## 2. P0：多人房间上下文改为“一个来源只发送一次”

### 2.1 当前问题

当前已经具备 `buildControlledContextEnvelope({ participantSnapshot })` 的瘦身分支，但房间主链没有传入 `participantSnapshot`。同时：

- 人物 `sourceRefs.content` 在 `participantSource: true` 路径不截断；
- `participantPromptBlock()` 会把完整人物快照序列化进 Prompt；
- 同一世界书条目还可能作为手选设定进入 `WORLD_INFO_TEXT`；
- 房间 Prompt 再附一份 `CONTROLLED_WORLD_PRESENTATION_JSON`。

默认预算从 32k 升到 60k 只能缓解，不能消除重复和无界输入。

### 2.2 建立规范化来源表

在生成上下文装配层建立一次性的 `ControlledSourceRegistry`，不改正式人物名册存储：

```text
来源键：world + uid
字段：
- world / uid / title
- content
- relatedParticipantIds[]
- selectedAsSetting
- sourceOrder
- chars
```

规则：

1. 相同 `(world, uid)` 只保留一份正文。
2. 人物关联与手选设定只是该来源的不同用途标记，不复制正文。
3. 同 uid 但正文在本次读取中发生变化时，以本次冻结快照为准，并记录 revision；不把新旧两份混发。
4. 手动人物、当前用户 Persona 等没有世界书 uid 的资料使用独立受控键，不与世界书误合并。
5. registry 只存在于本次冻结输入和恢复快照所需投影中，不写回世界书。

### 2.3 分开“给模型看的资料”和“本地校验资料”

避免为了本地逐字证据校验，把全部原文重复发送：

- `participantEvidenceSnapshot`：本地校验使用，可保留冻结后的完整 `sourceRefs`，受恢复快照总上限保护。
- `participantPromptPayload`：给模型使用，只含人物 id、姓名、身份、短资料、来源键。
- `controlledWorldPayload`：每个来源正文只出现一次，附来源键和关联人物 id。
- normalizer 仍可用冻结的完整 evidence 检查引用，不从 live 世界书重新读取。

不能把 Prompt 截断后的短资料冒充完整证据；两者字段和命名必须明确分开。

### 2.4 接通房间主链

调整调用顺序：

```text
先冻结 participantSnapshot
→ 建 ControlledSourceRegistry
→ buildWorldPresentationContext(..., participantSnapshot, sourceRegistry)
→ buildControlledContextEnvelope(..., participantSnapshot)
→ 生成 participantPromptPayload
→ 计算统一预算账本
→ 通过后才请求模型
```

房间受控信封中的 `CHARACTER_CARD_JSON`：

- 单人卡保持现有字段投影；
- 多人卡只保留卡标题、选定人物的 id/姓名/短资料；
- 未选人物不进入信封、不进入 residents 合同、不进入外貌资料。

`UNTRUSTED_SELECTED_PARTICIPANTS_JSON` 不再携带重复全文，只带来源键。正文统一在受控来源段出现一次。

### 2.5 设定装袋策略

现有纯返回顺序贪心改为稳定优先级：

1. 与选定人物直接关联的来源；
2. 用户明确手选但未关联人物的设定；
3. 无手选时 dry-run 激活的补充设定。

同一优先级内保持用户/世界书原顺序，不按长度擅自重排剧情语义。仍坚持整条装入，不截半条。

超出容量的条目形成结构化结果：

```text
included: world / uid / title / chars / reason
excluded: world / uid / title / chars / reason
```

这份清单用于 UI 和诊断；可分享诊断只保留字符数和脱敏标识，不写正文。

### 2.6 验收

1. 同一 `(world, uid)` 同时属于人物 sourceRef 和手选设定时，最终发送正文只出现一次。
2. 未选人物姓名与正文不出现在多人房间请求中。
3. 19 人/设定现场能输出各段字符，并明显低于 r84.36 同现场。
4. 超预算仍在发送前阻断，`providerRequests=0`。
5. 续写使用首次冻结的 registry；中途世界书变化不得污染原草稿。
6. 本地证据校验仍能找到完整来源，不能因 Prompt 瘦身放宽无据描述。

---

## 3. P1：统一生成预算账本与提示

### 3.1 新增 `GenerationInputLedger`

在最终拼接和 token 计数前，为每段输入登记：

```text
promptTemplate
archiveSlice
characterEnvelope
personaEnvelope
selectedSettings
activatedWorldInfo
participantPayload
worldPresentation
other
```

每段至少记录：

- `chars`
- `includedItems`
- `excludedItems`
- `deduplicatedChars`
- `truncated` / `dropped`
- `reason`

最终完整 Prompt 仍只做一次宿主 token 计数；不能把各段 token 估算相加冒充真实 tokenizer 结果。

### 3.2 提示时序

当前问题是先弹「只装下 5/19」info，再发现总预算失败并弹 error。改为：

1. 完成来源去重、装袋、最终 Prompt 拼接和 token 计数。
2. 得到一个 `GenerationPreflightResult`。
3. 根据最终状态只通知一次：
   - 全部通过且无降级：不弹 toast；
   - 通过但有条目未送入：一次 warning/info，包含“已发送 x/y，可展开查看”；
   - 总预算失败：一次 error，包含总字符/token/预算以及最大占用段；条目裁剪作为同一详情，不另弹。
4. 当前页使用内联状态；后台/离页才使用 toast。

### 3.3 预检详情

详情面板显示：

- 总字符、总 tokens、当前 token 预算、字符顶；
- 各段字符与占比；
- 已送/未送设定清单；
- 去重节省字符；
- 建议动作：减少人物、减少设定、提高输入预算。

默认不展示 Prompt 正文，不把角色卡、世界书内容写进诊断。

### 3.4 验收

1. “5/19 + 超预算”只出现一个最终提示。
2. 当前页失败只出现一处 inline error，不再另弹同义 toast。
3. token 计数失败时明确显示“tokens 未知”，只按字符安全顶判断。
4. ledger 各段字符之和与最终完整输入字符严格一致；若有分隔符差异，统一归入 `other`，不允许账目对不上。

---

## 4. P1：恢复区去重

### 4.1 改法

`showInlineError()` 只负责错误文本，不再无条件 append `recoveryBannerHtml()`。

恢复区由每个 reader 的统一 refresh 流程重绘：

1. 失败写入 journal/部分成果；
2. 保存成功后触发当前 reader refresh；
3. reader 从 canonical cache 读取一次 recovery rows；
4. 按 `draftId + pageId + mode` 去重；
5. 同页只保留一套继续/导出/放弃按钮。

如果某个空白页没有恢复区，使用一个统一的 `ensureRecoveryHost()` 建立固定容器，不能把恢复按钮塞进错误文本节点。

### 4.2 验收

- 同一草稿在当前页最多一套恢复按钮。
- blocked/oversized 草稿只有导出和放弃，没有继续。
- 普通失败、部分成功、单项重生成、ADV 子任务分别核对 draftId/pageId，不互相覆盖。
- 重复 refresh 不增加 DOM 节点或事件监听器。

---

## 5. P2：工程与性能优化

这些项目不应阻塞 P0，但适合在同一轮或紧随其后的清理提交完成。

### 5.1 输入装配只做一次

当前多个阶段会重复 `JSON.stringify`、复制 participant snapshot、提取世界书正文。优化为：

- registry、prompt payload、evidence snapshot 各自冻结一次；
- ledger 直接复用最终字符串长度；
- token 计数只对最终完整输入调用一次；
- repair 请求复用首次冻结上下文，不重新读取 live 世界书。

收益：减少大卡片下的主线程阻塞和临时字符串峰值。

### 5.2 取消无意义的深拷贝

只对会跨异步边界、需要冻结的输入做 `structuredClone`。纯展示、纯计数和只读投影避免反复深拷贝。任何优化不得把 live 对象重新暴露给恢复任务。

### 5.3 可达入口收敛

对 `src/modes/overlay.js`、`settingsPanel.js`、历史 View 副本建立可达清单：

- 不在生产闭包且无独立逻辑的文件改为显式 re-export，或在确认测试不再依赖后删除；
- `src/modes/room.js`、`album.js`、`advEvent.js` 等领域逻辑保留；
- 构建时生成/校验模块可达清单，避免再次改错副本。

这项单独提交，避免与功能修复混在一起导致冻结测试难以归因。

### 5.4 CURRENT 文档更新

`Heartbeat-Memories-Project-CURRENT.md` 顶部仍停在 r84.33，而 manifest 已是 r84.36。施工完成时：

- 顶部现行版本改为实际候选；
- 新增本轮合同和未验收项；
- 历史记录不重写；
- 明确热 240 + 冷 100 已取代旧“240 满后全部待入档”的描述；
- 不把静态代码审计写成真机通过。

### 5.5 诊断增强

新增不含正文的字段：

```text
view.chatNavigationEpoch
view.renderedChatId
view.currentChatId
input.sections[].chars
input.deduplicatedChars
input.selectedSettingIncluded/Excluded
input.participantCount
input.participantSourceCount
recovery.snapshotChars/cardChars/memoryChars
```

诊断导出时世界书名、uid、人物名应按现有脱敏策略处理，不能泄露正文、API Key、连接信息或完整 hash。

---

## 6. 测试与验收矩阵

### 6.1 纯函数测试

1. registry 按 `(world, uid)` 去重。
2. 同 uid 内容变化不能错误合并旧快照。
3. stable packing 优先人物关联，再手选，再 dry-run；同级顺序稳定。
4. ledger 字符总数与最终输入一致。
5. participant prompt 不含 `sourceRefs.content` 重复全文。
6. evidence snapshot 仍保留 normalizer 所需证据。

### 6.2 状态机测试

1. 无任务时导航事件不被阻断。
2. 有任务时取消确认会阻断导航，任务继续。
3. 有任务时确认切换会 abort 当前聊天全部任务，并让导航继续。
4. 已收到且持久化的分段保留；未完成分段停止且不自动重试。
5. 内容页切聊天后 reader 失效，同角色不同 chatId 清 active snapshot。
6. 双聊天事件只提交最后 epoch，一次 navigation permit 不能复用。
7. 迟到 hydrate 不覆盖当前聊天。
8. abort 后迟到响应不得写新聊天，也不得把取消任务改成成功。

### 6.3 UI 测试

1. 预算通过无降级：零 toast。
2. 设定部分装入但可请求：一条提示。
3. 设定部分装入且总预算失败：一条合并错误。
4. 同一 recovery draft 只有一套按钮。
5. blocked 草稿没有继续按钮。
6. 导航确认列出任务名，并明确已存成果保留、未完成部分中止。
7. 用户取消切聊时 overlay 保持；用户确认切聊时 overlay 关闭且不会自动重开。

### 6.4 回归保护

- Prompt 任务指令正文 hash 不因上下文装配重构而意外变化；若结构化上下文段必然变化，单独登记并人工审查差异。
- 真换角色、换聊天、换档案、换来源、换设置仍阻断错误续写。
- CAS、删除栅栏、只读历史快照、独立备份不放宽。
- 手选设定为空时仍可按现有设置 dry-run。
- 单人卡与没有 participant roster 的旧记录保持兼容。
- 相簿现有去 sourceRefs 行为不倒退。

### 6.5 真机验收

至少执行：

1. 桌面 HTTP 酒馆。
2. Tauri/TT 环境。
3. 同角色两个聊天互切。
4. 多人卡 19 个选择项的房间生成。
5. 240 热记忆 + 冷归档存在的相簿/ADV。
6. 切后台、关闭 overlay、生成中切聊再返回。

真机记录：

- 实际安装版本/build；
- providerRequests；
- 输入总字符/token/预算；
- ledger 分段字符；
- 原聊天与当前聊天 id 是否一致；
- 最终写入作用域；
- 是否出现重复 toast/recovery 区。

---

## 7. 建议实施顺序与提交边界

### 提交 A：跨聊天 UI 隔离（最高优先级）

涉及：

- `src/ui/archivePortal.js`
- `src/archive/snapshots.js`
- `src/ui/overlay.js`
- `src/core/requestCoordinator.js`
- `src/core/state.js`
- 必要的 runtime state / timer cleanup

通过第 1 节验收后再继续，避免输入重构掩盖错聊天问题。

### 提交 B：统一任务中心

涉及：

- `src/core/requestCoordinator.js`
- `src/core/state.js`
- `src/ui/overlay.js`
- 新的任务中心 View/样式

先统一任务枚举、阶段和取消 API，再接 UI；切聊确认必须复用该 API。

### 提交 C：多人来源 registry 与房间接线

涉及：

- `src/generation/client.js`
- `src/core/cache.js`
- `src/core/participants.js`
- `src/archive/repository.js`
- `src/modes/room.js`

先完成去重和单份正文，再做预算 UI。

### 提交 D：预算账本与统一提示

涉及：

- `src/generation/client.js`
- `src/core/text.js`
- `src/ui/overlay.js`
- 设置/预检详情组件
- 诊断字段

### 提交 E：恢复区去重

涉及：

- `src/ui/recoveryView.js`
- `src/ui/overlay.js`
- 各 reader 的 recovery host

### 提交 F：副本收敛与文档更新

最后单独处理不可达副本、CURRENT、版本号和 changelog，不与核心状态机改动混为一笔。

---

## 8. 可以继续优化但不建议本轮强塞的部分

下面分为“优化已有功能”和“新增能力”。**8.1 统一任务中心已拍板并入本轮**；8.2 以后留作后续独立迭代，不与本轮状态机/收包修复混做。

### 8.1 统一任务中心（已拍板并入本轮）

在心迹回廊顶部增加“任务”入口，集中显示：

- 任务名称、所属聊天、阶段、已完成/总分段；
- 正在准备、等待名额、请求中、校验中、保存中、已取消；
- 单项取消、全部取消；
- 已收到成果是否落盘；
- 完成后“回到原聊天并打开结果”的明确按钮。

这能让“切聊前确认”不再只靠一句笼统警告，也避免用户误以为关闭 overlay 等于取消任务。

### 8.2 按模式设置输入建议值与预算档位

房间/多人卡、相簿、电话的常见输入规模不同，可以只给建议，不自动改用户设置。需要积累诊断分布后再定，当前不要凭单个现场猜阈值。

可增加三个只影响本次收包策略、不偷偷修改全局额度的档位：

- 轻量：较少档案和设定，优先速度/费用；
- 均衡：当前默认；
- 完整：尽可能带上更多资料，仍受用户输入预算约束。

### 8.3 记忆覆盖地图与增量建档（已有建档的可解释性优化，优先级高）

把聊天楼层、外部摘要、世界书历史分别显示为覆盖区间：

- 哪些楼层已经形成 Mxxx；
- 哪些只有摘要、没有正文覆盖；
- 哪些尚未处理；
- 点击缺口只处理缺口，不重新收费整理已覆盖范围。

它比单纯显示“已保存 1846 / 未完成 93”更能解释长聊天是否真的漏记。

### 8.4 冷归档检索（已有冷归档的实用化，优先级高）

现在冷归档用于保留证据，但 Prompt 默认主要看热记忆。增加按人物、时间、关键词和来源的本地检索：

- 用户可以查看、筛选、锁定或恢复到热位；
- 派生生成按需取少量冷记忆，不把冷档全塞进 Prompt；
- 显示“本次用了哪些热/冷记忆”，提高可追溯性。

### 8.5 大型多人任务按人分片

若去重后仍有超大人物卡，可以按人物分片生成 residents，再本地合并空间蓝图。它会增加请求次数、费用和恢复状态复杂度，必须单独授权，不能作为本轮默认降级。

### 8.6 缓存分库与长列表虚拟化（性能优化）

房间 SVG、CG、timeEcho、恢复 journal 可逐步从大 `chatMetadata` 拆到独立存储。风险较高，需先做容量画像和迁移/回滚方案，本轮只减少重复嵌套。

档案、冷归档、世界书场景等长列表使用虚拟滚动；解析、压缩和大段 JSON 投影可移到 Web Worker 或分时执行，避免手机/TT 主线程卡顿。

### 8.7 来源追溯面板（新增功能）

在相簿、ADV、房间台词等生成内容旁增加“依据”入口，显示使用的 Mxxx、世界书来源键、人物来源，不展示隐私正文。用户可以判断内容为什么出现，也能快速定位错误记忆。

这必须读取已保存的来源引用，不能让模型事后编造“依据”。

### 8.8 全局搜索与交叉筛选（新增功能）

统一搜索热记忆、冷归档、相簿、ADV、房间、邮箱等：

- 人物、时间、关键词、模块、来源筛选；
- 搜索结果跳转到原内容；
- 默认纯本地，不调用模型。

### 8.9 世界书选择排序

当前方案保持同级原顺序。后续可提供用户显式拖动优先级或“人物相关优先”开关，不应由隐藏算法擅自改变用户选择。

### 8.10 一键健康检查与安全清理（新增功能）

只读检查：

- 各聊天缓存体积；
- 孤立草稿、blocked journal、待写回任务；
- 热/冷档数量；
- 过期 CG blob 与无引用派生资源；
- schema/版本兼容情况。

任何清理都先列明对象、可导出、二次确认；不能提供“清空全部修复”的危险按钮。

### 8.11 生成前费用估算

只有字符/token 数，没有可靠的渠道单价就不能显示伪精确费用。可先显示输入/输出额度，等提供方公开价格和模型身份可可靠取得后再做。

---

## 9. 需要用户拍板

### A. 切换聊天时，心迹回廊当前页面怎么处理

**新拍板：当前聊天存在任务时先确认；用户仍要切换则中止这些任务，然后关闭 overlay 并切换。**

- 用户取消确认：留在原聊天，任务继续。
- 用户确认切换：abort 当前聊天任务；已安全保存的成果保留，未完成部分停止，不自动重试。
- 不自动打开新聊天 chooser；用户主动再次打开心迹回廊时，才读取新聊天。
- 关闭必须走完整清理流程，不能只视觉隐藏旧 DOM。

### B. 多人人物正文的预算策略

**已拍板 B1：每个来源正文全局只发送一次，并受统一世界资料袋限制；人物 JSON 只留短资料和来源键。**

- 不增加模型请求次数。
- 本地完整 evidence 仍冻结保存并用于校验。
- 超出资料袋的来源明确列为未发送，不截成半条。

### C. 生成前预检是否需要用户每次确认

**已拍板 C1：自动预检，不增加确认步骤；仅在裁剪或失败时展示一次详情。**

### D. 当前已拍板容量合同是否继续沿用

现合同是：热 240、冷 100、冷满后删除最早冷存并提示，不另做导出确认。

**已拍板 D1：本轮沿用，不把容量产品再次混入 bug 修复。**

### E. 绕过宿主 UI 的程序化切聊如何兜底

公开 API 没有切聊前事件。脚本、其他插件或宿主内部程序化切聊可能绕过第 1 节的 click guard，此时插件只能在 `CHAT_CHANGED` 后发现已经切换。

**已拍板 E1：发现后立即中止原聊天任务并通知用户。**

- 与“切换聊天代表放弃未完成任务”的新合同一致；
- 已切换完成，不能再补一个假的“是否切换”确认；
- 已落盘成果仍保留。
- 不自动切回原聊天，避免循环导航和覆盖用户当前操作。

---

## 10. 完成定义

只有同时满足以下条件，本轮才算完成：

1. 常规切聊有任务时先确认；确认切换会中止当前聊天任务。切换后旧 reader 不可交互，任何请求/写入都不能错绑新聊天。
2. 多人房间同一世界书来源正文最多发送一次，未选人物不进入输入。
3. 输入 ledger 能解释完整字符构成，设定裁剪与预算失败不再分两次提示。
4. 同一恢复草稿同页只有一套操作按钮。
5. 统一任务中心覆盖所有任务类型、显示归属与落盘状态，并能可靠单项/当前聊天全部取消。
6. 旧身份、CAS、恢复、blocked、只读和容量保护没有倒退。
7. 静态测试、构建检查与真机验收分别记录；未执行的环境明确写“未验收”。

