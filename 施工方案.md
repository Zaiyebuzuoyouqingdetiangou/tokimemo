# 心迹回廊施工方案

日期：2026-09-22
现行版本：`0.99.5 / r84.50`（`测试` 分支）
这一份是唯一的施工方案。预算、快照、任务中心和重试合同的旧方案已经并进来，不再另开一份。

不变的边界：热位 240、冷归档 100、恢复快照 120 万字符、journal 180 万、缓存 12MB、默认输入预算 6 万（可调 8 千–20 万）。不取消换角色、换聊天、档案版本、CAS 和删除栅栏。字数下限这一轮不放宽。

---

## 已经做了

### 预算、快照、容量

- 派生任务默认输入预算 6 万 token。旧存档里恰好仍是 3.2 万的，读设置时升到 6 万。可调范围 8 千–20 万。字符顶跟着本次 token 预算走，不再另写死 9.6 万。
- 最大输出和输入预算是两项。各页请求里的 3000 / 3200 不再拿去压低供应商输出上限。
- 恢复快照一开始就是投影（id、标题、锚点、摘要口径），不再把整卡整库克隆进每个新任务。120 万字符上限数字不动，超了仍报 `RMT_RECOVERY_SNAPSHOT_TOO_LARGE`。
- 热记忆 240、冷归档 100。满档不再让相簿、ADV、房间在发请求前全部死掉。
- 生成前先算完整输入账本，裁剪或超预算只提示一次，并带上构成数字。

### 跨聊天和多人房间

- 当前聊天有任务时，常规切聊先确认。确认后中止这些任务并关掉回廊；取消则留在原聊天。程序化切聊无法提前确认，切换后中止原聊天任务并提示一次。已落盘成果保留。
- 多人房间同一世界书来源正文只发送一次。人物提示只带短资料和来源键。超出资料袋的来源整条不送。

### 任务中心和两次生成

- 后台任务在顶栏，档案室标题和图标中间。失败标在名称旁边。
- 任务中心一项一行。进行中、排队、未完成在上，串行完成的在下。已完成可一键清空，不清未完成草稿。打开结果进入对应内容页。
- 主界面不再重复未完成草稿卡片。
- 内容页可以勾选，按目录顺序一次生成一项。
- 第一次格式合法就标完成，不再写成「没有新的可保存分段」。失败行写具体原因。
- 他的房间、春夏秋冬、私人终端、回忆相簿、结局路线、ADV 事件索引可以第二次生成：对白和描述、小事件、各应用正文、共同回忆、路线正文、事件正文。设置「第一次完成后，自动进行第二次生成」默认关。
- 空回自动整段重来一次。写到一半没闭合的，自动接着补，不丢掉已写内容。「失败后自动重试未完成部分」仍默认关。
- 各页提示词共用短任务说明，文末要求只输出一个 JSON。各页字段要求和字数下限保持原样。
- 截断续写的登记改到模块初始化之后。先前它在恢复模块导出之前就调用，整包加载会抛错，界面只显示「心迹回廊加载失败」。

前世今生和蝴蝶效应没有拆成两次。后一段依赖前一段，只留下第一段时页面还不能读。

### 卡名 / 主角名 / Persona 别名

- 共用提示词头不再写「当前角色 = 卡名」。当前用户优先用档案记忆里的主角名，Persona 和冻进档案的 userName 只当别名。
- 终端通讯：用户线程只收主人草稿；owner 认成员真名；补缺发具体标题；没有合法对象就省略，不再发空标题。
- 备忘、工作、学习、阅读等日常 App 的提示词也按档案人物真名写，不再把卡名当作者，补缺标题写成「xx的备忘」这类具体条目。
- 角色互动、房间、相簿、春夏秋冬的姓名比对也认这套别名，不再只认 Persona。

### 手动重试带上一轮失败原因

- 「重试未完成部分」不再只重发冻结原提示词。请求正文前先写固定分类：没有完整 JSON、没有闭合、句数不够、字数不够，或通讯被剥空。
- 分类写入草稿 `failureFeedback`，没有原 Error 对象时也能按失败码还原。截断稿标成没有闭合。
- 分段内部那一次自动重试与手动重试共用同一套文案，不把异常原文写进下一轮。
- 通讯空线程（`RMT_PHONE_NO_CONVERSATION`）任务中心可以重试：不丢成功段，只给失败通讯段套「主人未发送草稿、不凑双向」的修订合同。
- 备忘/工作/学习等日常 App 重试同样套日常修订合同。角色互动、房间的句数和字数门槛不放宽，只写失败原因。

### 每一页的最短合法 JSON 例子

- 生成请求在输出合同之后附上【最短合法例子】。字段名和层数跟该页自己的输出格式一致，正文用占位短句。萤火虫颜色、房间观感、储物形态这类枚举不写死某一种值，写成「从枚举里选」。
- 例子不放宽句数和字数下限，也不代替证据、身份和真名规则。截断续写不再追加第二份例子。
- 春夏秋冬 Scenario 的字段是 `season`，id 形如 `SCENE_SPRING`。Voice 仍用 `kind`。
- 相簿第 2、3 段看本页有没有 `UNTRUSTED_SELECTED_PARTICIPANTS_JSON`。有，就是多人卡：`people` 数组，共同回忆的 `comments` 是 `{speakerId, text}`。没有，就是单人卡：平铺的关系对象，`comments` 是字符串数组。单人卡里不是字符串的评论直接丢掉，不存成 `[object Object]`。
- 蝴蝶效应的增量分歧例子是 `{"nodes":[…],"omega":…}`。只有分段生成才示范单个 `node`，并按 `CURRENT_SLOT_JSON.kind` 分开：主线锁定、普通分歧用该槽的 `primaryAxis`、终点 Ω 独白为空且 `trueEnding` 为真。
- 单项重新生成、单项重新整理、单张便签重新生成、页角随笔重新生成、单项重新判断分类不再附例子。这些页的 Prompt 里已经填了原条目的颜色、证据编号和解锁状态。
- 通讯 App 详情的例子保持计划里的 id，聊天草稿和笔记正文仍按该页合同填。
- 本地检查是 `tools/test-json-shape-examples.mjs`：遍历各页标签，确认例子字段名出现在该页 Prompt 里，并单独核对 Scenario、相簿单人/多人、增量 `nodes`、分段 MAIN / 普通分歧 / Ω，以及单项重做不再附例子。没有调用真实模型，也没有做真机验收。

### 严格 JSON 页的温度

- 各页 JSON 请求的温度不超过 0.4。原先更高的页改成 0.4，包括角色互动 Voice / Scenario（原来 0.65）、日常一格、萤火虫、蝴蝶、ADV 正文、房间、出行、陈列、前世今生、时空回响，以及这些页的单项重做。
- 已经低于 0.4 的请求保持原值，例如相簿关系扫描 0.25、分类重判 0.2。连接设置里的温度仍可把某一页再调低，但不能把 JSON 页调到 0.4 以上。设置里的默认 0.9 只作连接缺省，不再直接发给这些页。
- 本地检查是 `tools/test-json-page-temperature.mjs`。没有调用真实模型，也没有做真机验收。

---

## 还没做（按优先级）

### 卡名不是人（全页提示词已改头）

角色卡名称只是卡名、设备名、场景标题。说话、关系、设备主人用档案 `participants`、选定人物名单、记忆正文里的真名。单人卡时卡名往往等于那个人；组卡时卡名不是任何人。

### 当前用户看建档主角，Persona 只作别名

显示名优先取档案记忆里出现最多、且不是角色侧的那个人。Persona `name1` 和建档时冻进 `memoryBank.userName` 的名字都当成同一人的别名，忘了换 Persona 也能对上。换到完全无关的 Persona 仍算来源变化。

同一套解析用在：共用提示词头、终端通讯、角色互动对白、房间既往声称、相簿读者名、春夏秋冬来源守卫。日历、出行、结局、ADV、前世今生、蝴蝶、成就、陈列柜、人际庭园也走这层头。

---

### P0 先做

1. **真机验收还没做。** 桌面酒馆、Tauri/TT、同角色两个聊天、19 人房间都没有在真实页面上点过。代码通过不等于这几条合同在现场成立。
2. **私人终端的「通讯」和笔记类 App 提示词已改完。** 校验接上远端的 draft/daily/history。出错那份要重新点补缺才会换哈希。
   - 和当前用户的线程只写未发送草稿，不要求双向。模型写了用户发言就丢掉这些句子，留下主人草稿。
   - 已发生的双向原话仍只接受 basis=记忆，而且每句必须在所引 Mxxx 里逐字出现。摘要对不上时降成草稿。
   - 组卡说话人用成员真名，设备名仍是卡名。owner 不再只认卡名。
   - 补缺不再发「按此 App 用途与角色生活补齐」。通讯写成「给xx的未发送草稿」，备忘/工作/学习写成「xx的备忘」这类具体标题。没有合法对象就不发请求。三条都被丢掉时写「通讯没有可保存的对话」，不再用同一份提示词重试。

### P1 接着做

6. **锚点和整段对白不要绑在同一个对象上一起判死刑。** 房间、结局、相簿已经拆成「先目录、再正文」。其余仍把「逐字抄锚点」和「写满对白」放在同一次校验里的页，锚点抄错或对白不够都会整段失败。
7. **记忆覆盖地图。** 标出哪些楼层已经形成 Mxxx、哪些只有摘要、哪些还没处理。点缺口只处理缺口，不把已覆盖范围再收费整理一遍。比「已保存 1846 / 未完成 93」更能说明长聊天有没有漏记。
8. **冷归档检索。** 按人物、时间、关键词、来源在本地查看、筛选、锁定或恢复到热位。派生生成按需取少量冷记忆，不把冷档整包塞进提示词。

### P2 有余力再做

9. **来源追溯。** 相簿、ADV、房间台词旁边显示用过的 Mxxx 和世界书来源键，不展示隐私正文。只能读已经保存的引用，不能让模型事后编造依据。
10. **按模式给输入建议，不改用户的全局预算。** 房间、相簿、终端的常见输入规模不同。要先有诊断分布，不要凭一次现场猜阈值。
11. **世界书选择顺序。** 现在保持同级原顺序。以后可以让用户自己拖优先级，不要用隐藏算法改顺序。
12. **生成前只显示输入和输出额度。** 没有可靠单价时不要显示假的费用数字。

### P3 单独开一轮，不和上面混做

13. **超大多人卡按人分片再本地合并。** 会增加请求次数和恢复状态，必须单独授权。组卡通讯的说话人真名放在 P0，不在这里做。
14. **缓存分库、长列表虚拟滚动、解析挪到分时或 Worker。** 先做容量画像和回滚方案。
15. **全局搜索。** 热记忆、冷归档、相簿、ADV、房间、邮箱在本地按人物、时间、关键词跳转。不调用模型。
16. **只读健康检查。** 列出各聊天缓存体积、孤立草稿、blocked journal、热冷档数量、无引用的 CG。清理前要列出对象、可导出、二次确认。不要做「清空全部」。
17. **放宽字数下限。** 春的 Voice 仍要 5–10 句且不少于 280 字，Scenario 仍要 6–12 句且不少于 360 字。少一句现在仍整段失败。这一条明确先不做。
