# Hearttrace r77 修复与验证说明

版本：**0.8.72 / 0.8.72-tt-cg-r77.0**。日期：2026-09-14。

本候选承接已交付的 r76 ZIP，源码来源仍是用户上传的 r74 ZIP。上传原件、已有候选包及项目 CURRENT 未修改，没有使用 GitHub main 替代，也没有向 GitHub 提交或发布。

## 对这份安卓报告的判断

报告来自 **r75.0**。第二次任务的 8 个分块全部通过，档案简介也完成响应、解析与校验，随后在 `save!` 失败。因此这次停点在保存阶段，不能把模型端的 `finish_reason: stop` 当作已保存的证明。第一次还有简介请求失败；`rateLimitHits: 1` 不能解释第二次简介已完成后的保存失败。

截图中“浏览器没有保存待写回结果”的原文来自 **localStorage 待写回队列**，不是 IndexedDB 备份异常的直接证据。旧版会把这一层的具体异常替换成笼统提示，并把备份错误在上层归为 `RMT_UNCODED`，所以无法仅凭此报告认定是空间不足、权限拒绝或其他原因。

报告中的 `storage.hasArchive` 只表示原始聊天元数据含有档案；旧报告没有证明它通过当前聊天身份、版本和删除检查。代码里确实存在 `.jsonl` 后缀比较不一致：备份与任务使用去掉后缀的聊天 ID，部分读取/保存却按原字符串比较。这可导致同一个聊天的档案被误拒绝，但报告没有导出身份比较，**尚不能确定该安卓用户正好遇到此分支**。

安卓本地环境可能使用不同的浏览器存储策略；不能只根据 `http:` 判定 IndexedDB 不可用，也不能根据插件缓存只有约 50 KB 排除站点配额问题。浏览器配额按来源共享，且 localStorage 与 IndexedDB 有不同限制。参见 [MDN：浏览器存储配额与清理策略](https://developer.mozilla.org/en-US/docs/Web/API/Storage_API/Storage_quotas_and_eviction_criteria)。本版没有绕过浏览器权限或把失败伪装成保存成功。

## 本次修改

| 问题 | r77 行为 | 确认范围 |
| --- | --- | --- |
| 诊断位置理解错误 | 放在插件自己的主界面，默认折叠；点开可复制、导出或查看报告。正常运行时不在酒馆扩展设置铺开报告；运行模块加载失败时才保留后备入口。 | 4 项诊断入口与 DOM 事件测试，真实手机排版和系统下载未执行。 |
| 同聊天 ID 后缀不一致 | 读取、保存统一使用既有 `.jsonl` 归一比较；真正其他聊天、角色、旧版本和删除围栏仍阻止写入。 | 7 项实际保存、备份重开与拒绝路径测试。 |
| 已生成却不能保存，重试入口不工作 | 生成完成后即保留待保存状态；点击“仅重试保存”使用原结果和原 revision，不调用模型、不重抽成功分块。失败继续保留结果。 | 配额/安全失败后恢复，以及持续失败的真实导入集成测试。 |
| 旧结果可能覆盖变化后的来源 | 重试前复算聊天指纹和有效消息数；保留角色、聊天、revision、删除及生命周期检查，写入前沿用备份层检查。 | 点击重试前同楼层数编辑、换聊天、新 revision、角色改名及等待期间生命周期变化测试。 |
| 放弃草稿后后台又写回 | 原有明确确认后，先持久移除同源档案待写回项，再清本页草稿；移除失败不声称已放弃。其他待写回类型不受影响。 | 持久删除失败保留、成功清除、其他类型保留的集成测试。 |
| 错误只剩“未知原因” | 8 类备份错误、6 类待写回错误进入固定白名单；存储原生 AbortError 不再误当用户取消，内层 quota/security 原因可识别。 | 真实备份边界、任务追踪、固定提示与嵌套异常测试。 |

诊断增加最近一次实际备份失败的固定代码/类别/阶段、待写回存储标量、已持有的聊天 ID 严格与归一比较结果、格式支持情况及宿主能力。最近备份失败是本页运行时的历史记录，不代表当前聊天或当前健康状态。诊断不探测或写入存储，不请求模型，不解压缓存，不遍历正文，不包含聊天 ID 本文、人物资料、外貌、提示词、响应、异常正文、地址或密钥。

## 验证证据

- 当前全部 **19 个测试文件、150 / 150 项通过**，无失败、跳过或取消。新增保存恢复集成测试 11 项、身份测试 7 项、存储诊断 7 项、待写回分类 4 项；诊断入口由 3 项补至 4 项。
- r76 独立副本复跑最初 6 项保存恢复回归，6 项均失败；r77 修正后通过。`.jsonl` 保存与固定错误码已在修改前复现 5 项失败。存储 AbortError 被判取消、角色改名后保存误报也先复现，再修复并通过。
- 全部 **121 个 JS/MJS** 语法检查通过；最终修改的模块与生成 bundle 另行检查。
- **91 个模块**的 runtime bundle 连续两次重建字节一致，可导入且 5 个公开入口存在；manifest、index 与 build 版本一致。
- 独立只读安全差异复核发现的提交后版本误报、实际写入前生命周期检查和存储 AbortError 分类问题均已修正；复核未发现剩余新增 High / Medium 问题。
- “全部测试”仅指本次交付源码中实际存在的测试，未冒称执行 CURRENT 提及但未提供的历史测试。
- 最终 ZIP 的 CRC、逐文件 SHA-256、全新解压副本测试及重建结果见外附 `Hearttrace-r77-validation.md`。

原上传 r74 ZIP SHA-256：
`cffff7fe661be936a11df13aeb433b020de9f57bf0ee313b3620a7508108ef70`

直接基线 r76 ZIP SHA-256：
`fcb2c6e8f657979eee0296e83c08653b1208967e99fc33147b6ed975e3873e84`

runtime 源码指纹：
`35812ff15a3968b1e4a2757a0d80e9564b8d14edcd3cd2799b665386ac14f099`

runtime bundle SHA-256：
`1298baddaccbb1f150d976e6cc79a065d2686127c8fdd6ddea95bd408194b004`

## 安装与真机确认

**旧版若正在提示“仅本页保留”，先保留那个页面，不要为了升级直接刷新。** 尚未持久保存的临时结果不会随更新迁移，本版也不能找回此前刷新丢失的结果。此包修复后续运行的行为。

安装时，用 ZIP 内 `tokimemo-main` **里面的文件**覆盖原插件目录，保持 `manifest.json` 同层。重新加载后确认 **0.8.72 / r77.0**，不要另装第二份同名插件或清空站点数据。已成功保存的档案不需要重建。

安卓用户请确认：完成整理后档案能显示；关闭再打开仍可读；若保存失败可点“仅重试保存”；若仍失败，从插件主页导出新的诊断，查看 `recentTasks` 的保存错误和 `storage.backup` / `storage.deferred` 分类。r75 旧报告无法回溯此前已丢失的异常。

本版继续包含 r76 的计数超时回退、同一聊天当前页面会话内从魔法棒恢复上次退出页面和阅读位置的修复。关闭插件不取消后台任务；完整刷新、关闭浏览器或重启 TT 后不保留这个内存阅读书签。

**未执行安卓本地酒馆、iPhone/TT、真实浏览器视觉、真实文本服务或付费生图测试。** 正式保存仍遵循现有独立备份先完成、宿主 metadata 请求保存的链路；没有把宿主防抖保存当作服务端落盘确认。浏览器权限/配额拒绝仍会失败。来源检查覆盖点击重试前的变化，不承诺消除保存等待期间所有正文编辑竞态。外貌、CG 与其他模块延续前版，不在本次扩大修改；相关已有回归仍纳入测试。
