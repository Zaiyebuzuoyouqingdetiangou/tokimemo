心跳回忆 0.8.10 手动安装

1. 关闭或停止 SillyTavern（至少先不要在扩展运行中覆盖文件）。
2. 备份旧的 tokimemo 扩展目录。
3. 将本 ZIP 解压后的文件覆盖到 tokimemo 扩展目录；确保 manifest.json 位于该目录根部。
4. 重新启动 SillyTavern，并对浏览器执行一次强制刷新。
5. 扩展 manifest 版本应显示 0.8.10。

本包不会主动删除旧聊天档案。手动“更新聊天档案”仍会按 archive revision 使旧派生剧场缓存失效，这是既有数据一致性行为。
