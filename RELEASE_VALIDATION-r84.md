# Hearttrace r84 验证说明

版本：0.8.79 / 0.8.79-tt-cg-r84.0。日期：2026-09-14。

## 基线与实际修复

唯一修改基线是用户上传的 Hearttrace-r83-butterfly-noretry.zip，SHA-256：
`83e4693e6d48c969c51b953eb09b94ab929252889850348adf8ed88b3981cf57`。
没有使用 GitHub main，没有修改或覆盖原 ZIP，没有调用真实付费模型。

1. 上传包把“全新任务”误判为“延续旧计划”，导致没有已尝试槽位时忽略 MAIN 的 branchAxes，只请求 MAIN→OMEGA。r84 区分实际旧草稿与新任务，按 MAIN 选定维度生成普通分歧；新任务空维度回落到 decision，至少一个分歧，不固定凑满八个维度。
2. 恢复蝴蝶效应的叙事方向：平行体完整第一人称人生独白、现世角色对照后的触动与自省、冷酷系统判定、Ω 汇合已观测命运并回到与 user 当下关系的情绪落点。初次、增量、单项重生均应用；不恢复数代词或机械字数门槛，不强行确立恋爱或婚姻。没有 r59 原包，因此这不是对 r59 的逐字回滚。
3. 旧成功草稿保留原提示词、槽位及精确请求摘要校验；新版草稿通过代码控制的 narrative-r84 槽位后缀保持其提示词版本。成功段复验后复用；旧错误包已成功生成的 MAIN→OMEGA 草稿也不会在恢复时偷偷补发新分歧。重新生成的新版内容才采用新计划。
4. 上传包把“排队加发送总时间超过 90 秒”推断为“网关两分钟切断”，证据不足且新错误码未被追踪白名单识别。r84 撤掉这个推断，未知原因保留 RMT_CONNECTION_FAILED；有明确 HTTP 证据时仍按认证、限流或服务错误分类，文案不再无条件承诺自动重试。
5. 保留上传包公共文本分段默认不自动重试。失败后停止该段，用户主动操作才再次尝试。仅代码显式 allowAutoRetry=true 的内部调用最多两次尝试；本版生产调用没有开启它。已成功后正常生成下一分段仍会发出新的请求。

## 本地验证证据

- 完整测试 284/284，通过，36 个测试文件，0 失败、取消或跳过。
- 新增 13 项蝴蝶回归，调用真实生成、恢复、校验及单项入口；只替换宿主或请求边界。覆盖新建、空轴回落、失败后只续剩余段、完整草稿零发送、旧 readable 精确提示词 SHA、旧空轴、r61 草稿、旧错误标记、取消与来源封锁、真实增量和普通/Ω 单项身份保护、旧单项请求摘要兼容。
- 新增 3 项连接回归：排队 120 秒后发送立即失败仍只请求一次、不误判网关，时长本身不证明原因，明确 401/429/504 分类保留。三个用例在上传基线均失败，修复后均通过。
- 新建蝴蝶轴丢失和叙事要求测试先在上传基线复现失败，修复后通过；已有短而合法内容仍可读取，不因新提示词机械拒绝。
- 全部 JS/MJS 语法 144/144，通过。97 模块 runtime 连续重建逐字节一致；5 个公开导出保持不变。
- 独立只读差异复核未发现阻断项。没有修改全局恢复引擎、精确 r61 恢复配方、身份/来源/写回栅栏或其他模块内容要求。未执行不可用的 Codex Security 专用工具，不把人工差异复核冒充该工具结果。

Source SHA-256：`b854e1556ea2e229624512f657aaa2ed08bcb5a85b8f57fd82250772839d8f03`

Bundle SHA-256：`d9f5279fa62a47807cd1dbc9151b46a2ef306bfa7e44b0ae2aa7d581425c3185`

Bundle 大小 2,743,002 bytes，比上传包增加 5,515 bytes；最小同源提示词 fixture 的基础蝴蝶提示增加 241 字符。没有新增依赖、图片素材、源模块或轮询。最终 ZIP 的 CRC、逐文件 SHA、全新解压验证及 ZIP 摘要另见随附 Hearttrace-r84-validation.md。

## 真实环境与费用边界

- 未执行用户云酒馆、TT、iPhone、安卓的实际安装、视觉或付费生成测试。本地测试确认上述程序缺陷，不能保证真实连接稳定或任意模型一次生成合格内容。
- 用户旧 r81 报告中 relations、achievements 已保存；room 首段成功而下一段断连。子段成功不等于整个模块已提交，旧报告不能用来判断 r84 已运行。
- 未知连接失败、模型缺失终端字段或来源证据仍需失败当次诊断；本轮不降低来源校验来冒充生成成功。保留之前的调度、token fallback 和轻量启动修复，未测量真机启动耗时，也未修订宿主世界书读取。
- 默认不重试不等于整项只调用一次。私人终端目录和不同 App、蝴蝶不同节点均是各自的正常分段。其他已启动任务也可能继续；关闭插件界面按原约定继续后台生成，不等于取消。
- 本次检查使用本地替身，没有消耗用户 API。真实请求即使随后解析、校验或网络报错，也不能据此认定提供方没有处理；实际用量需看提供方记录。未新增一键停止按钮，不能撤销已经送达的请求。

## 安装与最小实机验收

先处理旧页面正在运行的任务及仅本页保留的未提交草稿，再用 ZIP 内 tokimemo-main 的内容覆盖原插件目录，完整刷新或重启 TT，确认版本 0.8.79。不要重复安装第二份；不需要清空站点数据或重建已保存档案。刷新可能丢失仅本页草稿，新版不会接管旧页面的在途任务。

首次实机只主动测试一个模块，确认能够生成、保存并重开阅读。若失败，记录新版诊断中的 mode、code、阶段和 providerRequests；不连续点击批量重试。蝴蝶新建应包含 MAIN、至少一个普通分歧和 Ω；旧草稿续接应只请求未成功段。公开前仍应完成这一步实机验收。
