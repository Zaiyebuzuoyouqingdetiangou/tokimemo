# r84.70 / 0.99.18 — 待入档结果恢复

## 行为

- 当前档案旧版容量检查点新增“保存待入档结果（不生成）”。只保存已校验结果，不请求模型、不重新扫描生成。
- 正式记忆与冷归档默认无条数上限。保留旧记忆内容、锁定、编号及冷存，新增编号从两区最大 M 编号后继续。
- 仅成功写入后推进该批来源覆盖；失败保留原待入档结果。备份已成功而聊天元数据失败时，保留同一个候选版本供本地重试。
- 下一批继续由用户点击；保留原请求划分，不改变模型预算、不自动连续请求。
- 完成任务在界面刷新前移除，避免任务横幅残留“准备”。新提示区分来源总数、已处理和正式保存。
- 增量证据游标支持 240 条之后的编号；每次模型证据及派生内容预算保持原值。无 12MB 限制与邮箱小画修复继续保留。

## 验证

命令：`node tools/build-runtime-bundle.mjs`、`node --experimental-vm-modules --test tests/*.test.mjs`、`node tools/verify-release.mjs`。

回归结果见 `verification/regression.txt`；语法与构建摘要见 `verification/checks.json`。新增回归覆盖：571 个来源片段 / 55 条待入档结果、240 条锁定旧记忆与 101 条冷存保留、旧相簿保留、300 条新生成结果完整保存、零请求重试、来源修改拒绝、聊天切换、并发版本、元数据失败后候选重试、分批续做、真实批次规划完整覆盖及任务中心刷新顺序。

旧版新增重点回归最初 5 项全部失败：无保存入口、容量 240 截断、续做 blocked、结束后仍显示运行。实现后重新运行，不以静态推断代替检查。

打包使用 `tools/package-release.py`：ZIP 名称必须是 `hearttrace-upload.zip`，内层 `tokimemo-main/`，不包含 `.github` 或 `.git`。在隔离本地 Git 仓库执行原工作流的完整 Python 导入段，核对生产文件逐字相同、工作流未改动、运输 ZIP 被移除；不运行工作流的远端提交/推送步骤。附带 tests/tools 供复核，原工作流会过滤它们，不影响运行代码。

## 边界

- 基线已与测试分支 `0d5cd71933ff444d97af35f0be5c9ea4efe794e3` 对照，共有跟踪文件哈希无差异。
- 这是模拟宿主 / 存储边界运行真实源码与打包运行时的回归；没有用户导出的实际草稿、手机现场、真实 SillyTavern 或真实模型联调。不能声称已替用户恢复了实际数据。
- 公开仓库未提供原完整测试套件。没有专用安全扫描服务；做了改动检查、转义/来源边界与并发失败回归。
- 插件取消人为存储上限，不代表浏览器设备容量无限；实际存储失败仍如实停止并保留成果。
- GitHub 写权限此前返回 403，本轮没有绕过限制，也没有宣称已推送。此包供上传测试分支验证，main 未改。
