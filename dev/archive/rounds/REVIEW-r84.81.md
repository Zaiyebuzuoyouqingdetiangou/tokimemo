# r84.81 建档准备取消与本地资料额度复核

版本：0.99.29 / 0.99.29-r84.81-archive-preparation。
基线：测试分支 606bb46cdab079b21a4b24a669c43d59796f3c67（r84.80）。

## 根因与修复

- 首次建档在请求模型之前就要等待世界书、凭据、草稿及来源读取。世界书激活接口原来直接等待，且部分准备读取没有接通取消；宿主 Promise 不返回时，任务一直占着准备状态。
- 现在从准备开始登记取消控制器，贯通凭据、草稿、缓存、外部记忆和来源账本读取。取消能退出等待并释放任务位；下一次点击可以重新读取。取消的旧读取即使迟到，也不能触发模型、发布旧草稿或覆盖新扫描结果。
- 世界书激活和来源账本沿用已有的单项来源读取 15 秒等待策略，无响应则明确退出。模型请求仍为原有的建档 360 秒、其他页面 180 秒。本轮没有增加次数、每日额度、重试冷却或生成字数限制。
- 草稿读取有单独的可撤销共享读取通道；取消使该通道失效，重试不会加入旧的悬挂 Promise。取消后仍等待写入确认的契约不变；账本事务取消时调用事务 abort，不能让旧写入在新任务背后提交。
- 任务中心保留已知失败原因，世界书无响应不再落成“具体原因未记录”。旧记录从未保存的错误原因无法事后还原。

## 取消的人工额度

- 全局最多 4 份未完成建档草稿，包括跨聊天误占额度的情况。
- 恢复资料包 120 万字符、journal 180 万字符、单个成功段 60 万字符、128 段的固定保存额度。
- 本地历史来源账本 8000 条 / 800 万字符，来源白名单及历史版本列表 5000 项截断。
- 本地记忆文件 4 MB / 5000 条 / 400 万字符限制及 JSONL 提前停止读取。
- 选择世界书 200 本限制，以及历史来源精选条目 160 项截断。

去掉历史版本列表上限后，来源投影只为每个来源构建一次版本集合，避免对每条记录重复计算。原有来源撤销、完整基线和部分更新含义不变。

## 保留的边界

聊天、人物、档案归属和 JSON 数据结构核对；实际浏览器存储失败；真实保存确认；单次模型上下文和分批请求预算仍保留。普通设定背景仍为 160 条 / 52000 字符，这与本地历史来源保存不同。本轮没有修改这些预算、模型服务响应保护或其他玩法的内容校验。

旧草稿必须实际保存过冻结来源才能恢复；只有错误标记、缺少来源的旧记录不会被当成完整草稿，也不会自动重新收费生成。取消本地额度不等于浏览器容量或模型上下文无限。

## 新鲜验证

- 全部现有工具回归：45/45，通过；0 失败、取消、跳过。记录：`verification/repair-r84.81-tests.txt`。
- 新增首次建档准备回归 8 项：世界书悬挂取消/重试、超时释放、凭据迟到、外部历史读取、草稿悬挂和迟到返回、读取已完成但延续尚未运行的取消竞态、账本事务取消、数据库打开超时后的重试。
- 新增本地额度回归 15 项：超过旧字符额度的资料包与成功段无损恢复、130 段、6 份草稿及跨聊天读写、大文件/5001 条导入、8001 条账本、201 本历史世界书、完整 UID 白名单、5001 个历史版本，以及身份/结构错误和真实存储失败仍保留旧内容。
- 281 个 JavaScript 文件语法检查通过；239 模块运行包连续构建字节一致；具名导入未发现 undefined，5 个运行入口导出有效。此项不等同于对所有函数执行完整静态分析。
- 运行包 SHA-256：`000d2d98eb387146f7853a8979747f4e712ba60882190d021f6559a6431f192f`。
- 本地浏览器使用真实模块、真实 IndexedDB、虚构聊天和没有 API Key 的宿主：点击开始后等待世界书；通过真实任务中心“取消这项”退出，生成按钮恢复；释放旧响应后重新开始产生新的读取；无响应时退出并显示“酒馆的世界书读取没有响应……”。最终 `busy=false`、活动任务 0、原聊天标记保留。验证页：`tools/preparation-r8481-interactive.html`。
- 测试页只替换原生确认框，生产确认行为未修改。无付费模型请求，不访问用户真实聊天。独立复核未发现新的阻断项；账本取消及迟到错误注入未产生未处理的 Promise 拒绝。

复现命令：`node --experimental-vm-modules --test tools/test-*.mjs`、`node tools/build-runtime-bundle.mjs`（两次）、`node tools/verify-runtime.mjs`。

远端 r84.79 已删除旧 `tests/` 和部分验证工具，因此开发总纲里的旧护栏/TypeScript 检查命令不具备运行条件，历史 83/221 项结果没有被计入本轮证据。

未实测：真实付费模型、真实用户酒馆和 iPhone Safari。本地复核证明上述可复现的等待/取消问题修复，不承诺所有外部服务故障都不会出现。
