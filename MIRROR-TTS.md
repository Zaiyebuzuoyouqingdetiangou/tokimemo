# r84.58：和现在的 TA 聊天

实时通话要求镜译的 [beta/realtime-call 分支](https://github.com/iabil-604/MirrorTranslate/tree/beta/realtime-call)。本次核对公共接口的提交为 `fe3270da5c49a3ded7998e8a5964000d03037b13`。默认 main 若没有这些能力，会提示版本不足，原有朗读仍可使用。

1. 在镜译里配置朗读音色、通话使用的模型连接，以及语音输入；心迹回廊不读取或复制镜译密钥。
2. 在当前角色聊天中打开回廊，进入“内容 → 互动 → 想和现在的 TA 聊天吗？”。多人卡从已有真实人物名单选通话对象。
3. 可选择携带当前档案记忆。点击“连接 TA”只准备本次背景，不请求模型、不开麦。背景含角色卡、所选人物来源、Persona、当前聊天记录，以及沿用原记忆摘要规则的可选档案摘要；通话期间背景固定。
4. 打字发送，或点击“开始说话”→“说完了”。每轮会调用镜译的模型和语音服务，使用其额度；跟随酒馆的模型可能整段返回，独立连接可流式输出。麦克风不可用时可打字。浏览器暂停音频时点“继续播放”。
5. 可打断、挂断、保存本次文字。关闭面板/回廊、切换阅读页面、切聊、切换档案或禁用插件会停止。已提交服务请求可能仍计费。文字只暂存在当前页面，换对象时隔离，不写入主聊天或记忆；关闭前可下载。

公共调用仅为 `__JINGYI__.tts.stream`、`stt.status/start`、`llm.status/stream`；累计回复转成语音增量。代码不执行模型给出的 HTML/SVG，不访问镜译私有缓存。语音失败保留已收到的回复，禁止自动重新收费请求。宿主音频体验和真机录音权限仍需安装后实际验证。

朗读配置位于“内容 → 互动 → 镜译 · 语音设置”。各正文页通过顶部“…”菜单选择“朗读本页”或“朗读选中文字”；朗读和实时通话相互停止，避免同时占用音频。

---

# 历史接入说明（r84.45，入口已按上述 r84.58 调整）

版本：0.99 / 0.99-tt-cg-r84.45-mirror-tts。基于用户提供的 tokimemo--.zip（0.98 / r84.44），本次仅实现清单第 33 项 TTS 接入，其余清单项目未在本包修改。

## 安装与使用

1. 保留旧安装包，保存正在编辑的内容。将本次完整 ZIP 用于原来的扩展 ZIP 更新流程。包内只有一层 `tokimemo-main/`；不要把它当作角色卡导入。手工更新时，将该根目录内文件覆盖到现有心迹回廊扩展目录（含 index.js、manifest.json、src、dist），避免再次嵌套 tokimemo-main。若更新器不支持 ZIP，请使用原有手工扩展更新方式。
2. 刷新酒馆页面，核对心迹回廊版本为 0.99，加载标识为 `0.99-tt-cg-r84.45-mirror-tts`。不清理站点数据，不删除档案、聊天或历史图片。保留的自动更新配置仍指向原项目；上游更新可能替换这个本地候选。
3. 在同一酒馆页面安装并启用作者的 [MirrorTranslate](https://github.com/iabil-604/MirrorTranslate)，使用提供 `__JINGYI__.tts.apiVersion === 1` 的版本。在镜译中开启朗读、填写 Fish Audio 的 API Key 并配置人物音色。当前交付不包含或自动安装镜译。
4. 打开心迹回廊的正文阅读页，展开 **朗读 · 镜译**，点击 **检查连接 / 刷新音色**。选择镜译已登记的人物音色或默认旁白，再点 **朗读本页正文**。
5. 也可以选中当前阅读页的一段文字，再点 **朗读选中文字**。选区模式排除表单、按钮和隐藏内容；不读取酒馆聊天或其他插件页面。
6. 状态区会显示准备、播放段数、停止、结束或失败。点 **停止**、切换正文、关闭回廊、切聊关闭回廊、禁用扩展或离开页面，都会结束当前阅读。准备阶段点停止后，需要等镜译返回旧请求，才能开始下一次，避免重复请求。

本页正文目前自动识别房间描述与对白、四季/基础语言的对话段落、ADV、相簿对白、结局正文、终端消息、时间故事、来信、部分前世今生与旅行正文。未识别的阅读样式可使用选区朗读。目录、设置和未展开内容不会作为整页正文发送。

本次播放整段使用你选择的音色，不进行自动多人配音或额外人物分析。没有登记独立音色的人物由镜译按其配置回退。心迹回廊不持有 API Key，也不另建语音缓存或改写档案。

## 已知边界

- 语音生成使用镜译连接的 Fish 服务额度；接入本身不需要 Cursor。默认 `analyze: false`，不请求额外模型分析，不自动重试。
- 镜译 v1 单次正文限制为 20,000 字。超过时明确提示选择较短段落，不截断正文、不自动连续提交收费请求。
- 镜译预先生成下一段时，停止不能保证撤销已提交的服务请求或其费用。心迹回廊只调用自己的会话句柄与 AbortSignal，不调用镜译的全局停止接口。
- 镜译 v1 共用播放器。检测到其已有普通朗读任务时，本入口会提示先停止；其他扩展在之后抢占播放仍可能中断本次会话。v1 的 done 不区分外部中断和自然结束，因此只显示“朗读会话已结束”，不冒称全文已经播完。
- 实际 Fish 音频输出、iPhone Safari 自动播放策略、TT/Tauri 内嵌环境和用户现场镜译配置仍需安装后试听确认。公开接口若不在当前同一页面可见，会提示未连接；不会越过 iframe 或读取镜译私有设置来寻找密钥。

## 验证与接口依据

- 对照作者仓库 `index.js` 的公开接口（apiVersion 1，apiSpeak/apiSession/installPublicApi，核对时版本 0.31.0）。未复制上游私有模块或修改上游仓库。
- `node --test tools/test-mirror-tts.mjs`：状态、互斥、取消、迟到结果、暂停后停止、异常、上游限制等 9 项。
- `tools/mirror-tts-fixture.html`：真实浏览器、项目原样式、模拟公开接口的 17 项交互断言；通过本地 HTTP 服务打开，点击“运行浏览器回归”。测试按钮是测试页专用，不属于生产入口，不调用真实语音服务。
- 包内原有 `node tools/check-r8437.mjs`、所有 JS/MJS 语法检查、生产 bundle 重建与打包一致性另见包外验证报告。历史文档中更大规模的旧测试数量不作为本包通过依据。

回退：用保留的原版本完整包恢复扩展文件并刷新页面。本次无存储格式迁移；不要通过清除浏览器数据来回退。
