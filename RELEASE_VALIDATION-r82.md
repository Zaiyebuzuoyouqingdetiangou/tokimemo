# Hearttrace r82 删除模块验证

版本：0.8.77 / 0.8.77-tt-cg-r82.0

## 本轮修改

以已交付 `Hearttrace-r81-full.zip` 为唯一基线，SHA-256：`c089b4122f69488fbfe050781f8413171bf94fa4843cfa7d6962c325697ccb87`。沿用用户上传 r74 的修复链，未使用 GitHub main、未修改原包。

按用户要求移除「错位相逢／时空旅行者的妻子」（旧名「错时相逢」，内部 mode 为 timeJourney）：删除档案入口、生成注册与提示词、专属数据校验、双时间线界面、交互和样式。旧书签不会重开此模块，直接调用打开／生成／继续生成也不会发送请求。

保留私人终端内「时空回响」、蝴蝶效应、前世今生、相簿及其他模块。时空回响与被删除模块共用的生成、阅读和样式文件继续存在，仅删除专属分支。r81 的响应提取、输入预算、脱敏诊断及轻量启动修复保留。

旧 timeJourney 内容不作破坏性迁移：缓存合并和增量更新时仍保留为不可打开、不可生成的不活跃数据，保留其原 revision。有效删除标记与较高写入栅栏继续阻止旧数据复活。原有待写回链未修改，合法旧完成结果可按原来源／版本约束落盘，但不会重新开放模块。没有清除或改写酒馆聊天正文。

## 验证证据

- 定向回归：38/38，通过真实插件函数与宿主、DOM、provider、存储替身，验证时空回响生成／追加／保存／重开、历史档案隔离、保存失败仅重试写入、不抢新页面。
- 新增8项移除回归：活动入口拒绝；不渲染旧故事；不生成／恢复旧模块；canonical／mirror 单侧旧缓存保留；仅含旧模块的缓存跨档案增量保留；clear marker／较高栅栏防复活；旧书签不替换当前阅读页。
- 完整测试：249/249，32个测试文件，0失败、取消、跳过。删除9项仅用于时间旅行功能的旧测试，新增8项；共用来源隔离测试改用时空回响，原保障继续检查。
- 全部 JS/MJS 语法：140/140；97模块 runtime 连续两次重建完全一致。
- runtime 五个导出保持：destroyMemoryTheater、initMemoryTheater、isGenerationBusy、openArchiveLibrary、openSettingsHome。
- 与 r81 直接执行对照：4种世界观下时空回响提示词逐字符一致、标准化结果一致；24种阅读状态的 HTML 完全一致。
- 独立只读差异复核未发现阻断项。蝴蝶效应、终端本体、自动更新、待写回和备份本体没有修改。

Source SHA-256：`06a4794c7383465d4047360c2ce95d3651c753e2bfd3a3604c6609268fe23d6d`

Bundle SHA-256：`605bc1d9416a4699b28dabfcfdec4ce982cd07348eb3d521b0614ea36ce5eaca`

Bundle 2,729,410 bytes，比 r81 减少 10,512 bytes。无新增依赖、图片资源或后台定时器。

## 安装与实机边界

用本完整包覆盖原心迹回廊插件目录，再完整刷新酒馆／重启 TT，确认版本为 **0.8.77**。不要安装成第二份插件，不需要清空站点数据或重新建档。

本轮未登录用户云酒馆，未在真实 TT／iOS／安卓 WebView 安装，也未调用真实付费模型。真机应确认：档案入口不再出现时间旅行故事；私人终端仍可进入时空回响；蝴蝶效应、相簿和原阅读位置可用。自动测试及 HTML 对照不能替代实际布局、体感速度或模型输出质量验证。

r81 已说明的模型真实输出、云启动速度及晚到宿主上下文边界仍存在；本次不宣称额外修复这些问题。
