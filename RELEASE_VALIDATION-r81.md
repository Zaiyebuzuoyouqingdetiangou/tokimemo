# Hearttrace r81 验证记录

版本：0.8.76 / 0.8.76-tt-cg-r81.0。日期：2026-09-14。

## 基线与问题证据

本轮从已交付 Hearttrace-r80-full.zip 解压修改，SHA-256 cffb4979ebb972905a3a5576d8d3e58014781e0c16131ca19db7de84531393e3，延续用户上传 r74 的修复链；未取用 GitHub main 替换源码，原 r74、r79、r80 完整 ZIP 均保持不变。

用户两份诊断来自 TT 的 r79（tauri:）。缓存模式变多不能证明正文生成、显示或保存成功；recentTasks 为空也不等于没有失败。截图确实出现多个模式 RMT_JSON_NOT_FOUND，以及人际庭园 RMT_INPUT_BUDGET。云启动慢是另一个待真机测量的现象。

## 本次改动

1. 最终正文提取：识别已知 data/result/response/body 包装，优先明确的最终正文字段；未知对象不再经 String 变成 [object Object] 后误报散文。推理、工具、拒绝和错误内容不充当正文。完整 JSON 代码块不再被前言中的未闭合括号阻断。实际散文、真正截断和错误响应仍会失败并保留旧内容。
2. 输入预算：对真正发送的过滤后 controlledPrompt 计数，不把已排除的 thinking 等内容继续算进预算。仍保留 96,000 字符、32,000 tokens 的限制，以及 token 超时回退和取消保护。
3. 人际庭园：历史摘要世界书不占设定采集额度；每次按实际 JSON 长度装入最多 12,000 字符的完整设定条目，超大条目跳过后继续后面的条目。新节点只能引用实际送出的来源；旧节点只有在当前仍选择、因本轮预算省略且逐字证据仍有效时保留。读取失败或上游采集已省略的来源无法重验，不能承诺全部保留；总输入仍可能达到全局限制。
4. 诊断：派生模式及独立校验请求也记录阶段；并发分段各自保留有限记录，输入计数与返回形态对应同一段。能区分解析成功、校验失败、正式保存失败与待写回；不输出聊天、提示词、正文、密钥或 URL。诊断仍在插件自己的首页折叠区域。
5. 启动：保存过自动更新开关时，仅加载无依赖的轻量楼数策略；当前聊天到达原有间隔才唤醒完整 runtime，仍由原锁与检查点确保一次到期间隔不重复请求。全关时不安装 5 秒检查定时器；相同公开调度输入下，一个已待命未到期检查的快照读取从旧版 47 次降为 3 次，异步后继续重验聊天和设置。没有改动备份事务或用户的开关。
6. 故事：名称按用户最新表述为「时空旅行者的妻子」。主轴改为一方无法控制离开和归来、另一方在缺席日常中等待、两人在错乱时间中相爱与离别；不是单纯调整相遇顺序。正文优先，双时间线折叠辅助阅读，可选等待段落，不增加必填篇幅/幕数/逆序要求；双方均可穿越，不强制性别、已有婚姻或悲剧。保留 timeJourney 内部类型、版本、已存故事及书签。

## 已执行验证

- 在旧基线上复现过滤后小请求仍被大原文误拦；派生请求失败未进入 trace；未识别正文包装变成 [object Object]、明确 content 被包装 text 抢先，以及前言括号干扰 JSON 围栏。
- 本版完整测试 **250/250**，32 个测试文件，0 失败、取消或跳过；新增35项，另按新故事要求调整原逆序断言。
- 新测试覆盖实际 requestJson、requestValidatedSegment、generateMode、公用调度器、压缩缓存写回、历史档案隔离、失败后只保存重试、并发阶段脱敏；使用宿主/DOM/传输/存储替身，无真实付费请求。
- 全部 JS/MJS 语法 **140/140**；97 模块 bundle 连续构建一致，五个既有导出可正常导入。
- bundle 2,739,922 bytes，比 r80 增加 22,145 bytes；无新模块依赖或图片资源。
- Source SHA-256：c45d485cea141f6c2e9c1e0d36f5d863a8d5291c5cef30c63000a598d8687d23。
- Bundle SHA-256：a5d92ce96af30dbc8a6170511e18e122b37be26d7b5879b98bf8f3c440dd0102。

## 安装与未验证边界

将新包覆盖到原插件目录并刷新，确认版本 0.8.76 / r81.0；无需清空站点数据或重建全部档案。先单独生成一项再观察。已有正文保存失败时继续复用已接受分段；如果旧版未提交草稿因本次提示词变化提示来源/提示词已变，原草稿不会自动丢弃，界面可由用户选择放弃该未提交草稿后新生成，正式旧内容不受该动作影响。

本次没有登录用户云酒馆，也没有执行真实 TT、iPhone、安卓或云酒馆浏览器安装。已修复可复现的实现缺口，无法仅凭旧报告确定用户实际收到哪一种模型包装，也不能保证模型从此总输出完整 JSON。真实启动耗时、渲染外观和人物故事质量需要真机确认。若仍失败，重新导出的诊断会包含本轮模式、返回形态及失败阶段，可继续按证据定位。

保留的宿主启动边界：第一次入口检查时若宿主上下文或已保存设置尚未就绪，轻量自动调度不会自行补挂，之后手动打开插件会接入完整调度；该边界在旧版也存在。到期后关闭开关，已排队的运行包下载可能完成，但完整调度器会重新检查开关，不会因此继续自动生成。

多人卡继续暂停，未实施。本文件记录源码验证；最终 ZIP 的 CRC、逐文件 SHA 和全新解压回归记录见随包交付的 Hearttrace-r81-validation.md。
