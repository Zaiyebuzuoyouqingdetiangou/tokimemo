# Hearttrace r79 验证记录

版本：0.8.74 / 0.8.74-tt-cg-r79.0。日期：2026-09-14。

## 基线与范围

本版从已交付 Hearttrace-r78-full.zip 解压修改。基线 SHA-256：235fed0813dfdfbbc7bfabfc621b47cd8bb2b5400778e020dfde036de2b339ca；1,623,297 bytes、142 文件。沿用用户上传 r74 ZIP 的修复链，未用 GitHub main 替代；原上传与 r78 包均未覆盖。r78 的头像悬浮入口、导航书签、诊断位置及既有保存修复继续保留。

## 使用

- 他的私人终端 → 时空回响：未生成终端也可打开。生成一篇后显示双方／同一人的两个时间端点，按世界观使用电话、通讯终端、器物或声音；接通后逐句阅读，最后显示关键信息与后续。
- 档案室 → 错时相逢：生成一篇不受控时间跳跃的番外；双个人时间线连接相遇，按故事、角色或用户的经历顺序逐幕阅读。
- 每次显式生成追加一篇，保留旧篇。番外不写回正式 Mxxx，不混入已有终端记录。阅读、接通、翻句、排序不调用模型。
- 生成使用对应人物与世界设定，不固定字数、章节数量、婚姻或悲剧。类别与结构有本地校验；不合格结果沿用原有分段修复机制，不能承诺每次模型调用必定成功。
- 两种界面使用本地样式和配色枚举，不加载新图片或依赖，不增加轮询、定时器。

## 已执行验证

- 当前源码完整自动测试 **209/209**，26 个测试文件，0 失败、0 取消、0 跳过；其中 r79 新增 **32** 项。
- 134 个 JS/MJS 均通过 node --check。
- 97 模块 bundle 连续两次重建一致，五个既有公共导出可正常导入。
- 新流程测试调用真实 generateMode、受控上下文、requestValidatedSegment、压缩缓存、规范备份和阅读逻辑；仅替换宿主、网络响应、DOM 和 IndexedDB 后端边界，不调用真实模型。
- 两模式首次生成、无新 Mxxx 时追加、压缩持久化后清理运行缓存并重开通过；已存故事和 phone／其他派生模式不变。
- 完整回复后的规范保存配额失败，通过真实 continueSavedGeneration 入口复用同一已验证 JSON；成功写回后当前空故事页立即显示，provider 请求总数不增加。
- 关闭、换页面、同模式换 reader、移动阅读位置及历史来源读取期间收起，不因迟到结果重开或跳转页面。
- 历史 B 采用 B 的角色卡和选择世界书，B 的仍可见 reader 接收结果；宿主 A 的聊天、Persona、档案与派生缓存保持不变。
- 旧格式／身份不符的故事不会作为空记录覆盖；HTML 字符串作为文本转义，模型 CSS／URL 不执行；读取动作只改阅读标量。
- 两个独立只读审查交叉检查生成与 UI／保存接入。修正前置等待重开浮层、保存恢复后空页、手表式终端挤掉第六个旧 App 的问题；未发现剩余阻断项。此为本地交叉审查，并非外部 Codex Security 服务报告。

## 构建标识

- Source SHA-256：3f71c2a1e6a0bf4002a5f519fc69da9b0892492da23edf7b93a0d96c72156055
- Bundle SHA-256：bad3e8b41c6d9085b2c5efaabacfc8529dc02225392aedb223f261ac5652c146
- Bundle：2,717,310 bytes；比 r78 增加 53,188 bytes。

ZIP 的 CRC、单根目录、逐文件 SHA 与全新解压验证结果，以及最后包体大小与 SHA，见随包外部交付的 Hearttrace-r79-validation.md。包内不嵌自身 ZIP 哈希。

## 尚需真机验证

未执行浏览器视觉测试、TT／SillyTavern 安装、iPhone／安卓本地 WebView、真实 IndexedDB 或真实模型请求。小屏样式按局部 CSS 与 DOM 边界检查，不能冒充视觉实测。实际人设贴合度、器物名称、时间叙事质量、字体和手表布局仍需在你的环境生成验收。

覆盖原插件目录后刷新，确认 0.8.74；已保存档案不必重建。旧页面若提示“仅本页保留”，先完成保存再刷新；升级不会搬运未持久保存的临时结果。
