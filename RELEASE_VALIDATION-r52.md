# 0.8.48 / r52.0 候选说明

日期：2026-09-07。基线是保留的 0.8.47 / r51.0。生产修改由主 agent 收口，两个 reviewer 只读审查，建议经实际代码与回归反例确认后处理。

## 安装与使用

1. 备份插件目录，保留聊天、metadata 和本机档案备份。用新包的 tokimemo-main 文件覆盖原安装目录，避免多套插件同时加载；刷新酒馆。
2. **不要删除旧档案，不需要完全重建。** 进入原档案分别重试“他的房间”“蝴蝶效应”；未通过完整校验的结果不会替换旧正式内容。
3. 私人终端在档案室独立卡片。有效旧草稿会继续已有进度；自动档案同步遇到待完成终端草稿时暂停，以免版本变化清掉草稿。
4. 设置 → 跟随当前聊天 · 自动更新。13 项默认全关；档案同步与各派生模块分开设置 1–1000 楼。每条聊天消息算一楼，编辑不加楼；启用时从当前楼数起计。没有档案时请先手动建档。
5. 自动任务在页面仍运行时才有效；关闭插件弹窗不等于关闭自动开关。浏览器休眠/关闭不会后台定时工作。失败不连环重试，等下一间隔；可手动生成，档案同步成功产生新 revision 后解除旧失败阻断。
6. “检查并更新插件”只拉取当前项目仓库已发布代码。Git 安装可用；ZIP/非 Git、管理员权限、服务器 Git/网络问题无法靠按钮绕过。不会强制 reset、重装、自动刷新，也没有替作者发布此次本地候选。

## 主要根因修复

| 问题 | 修复与保留边界 |
| --- | --- |
| User 台词没有气泡 | 新提示明确 User 标签；旧稿按确切姓名/标记和短说话插叙拆分，动作不入气泡，未知主体不猜。 |
| 房间反复校验失败 | 当前邀请独立识别；文字小组修复；spaces 与 pets 可依次修复。增量改成新增有据物件 patch，不让模型重述未发送的旧正文。 |
| 蝴蝶初次十节点 JSON 截断 | 固定十槽位逐个请求，当前槽有界修复，最后完整校验。正常共同关系续句可通过，“各自有新恋人”等不得借主语继承混入。 |
| 自动任务重发/串窗/假进行中 | Web Locks、请求前楼层检查点、busy 竞态回滚、动态规则不重启运行器、档案 revision 恢复；复用现有任务身份与提交链。 |
| 终端受房间失败影响 | 独立入口、返回档案，旧 PHONE v4 和草稿键不变；房间删除不再级联终端。 |

初次蝴蝶会有十次顺序模型请求，减少的是截断和失败重做范围，**不保证总耗时更短**。本轮内已通过节点不重做；刷新页面后尚未完成的整组节点不宣称已持久保存。任何模型仍可能拒绝、超时或给出不合格内容，测试不能替代用户实际服务商验收。

## 配色

新增初叶绿（GS1 灵感）、海盐蓝（GS2）、花漾粉（GS3）、杏糖橙（GS4），原日间/夜间/跟随/自定义保留，共八种。参考官方页面的色彩方向，色板与 CSS 由本项目实现；未复制游戏图片、Logo 或字体。[GS1–3 官方页面](https://www.konami.com/games/girls_side/for_switch/)、[GS4 官方页面](https://www.konami.com/games/girls_side/4th_Heart/)。

主题工作使用 UI/UX 技能检查正常字重、对比度、留白、移动宽度和触控区域；场景插画保留本地艺术颜色，不被结构卡片主题涂平。

## 验证记录

工作副本已完成以下检查；新包解压后的校验由压缩包旁的验证文件另行记录，不能把工作目录测试冒充封包测试。测试使用虚构角色与本地响应，不访问用户真实聊天或真实付费服务。

| 检查 | 结果 |
| --- | --- |
| Node 完整回归 | 354 / 354 通过，0 失败 |
| r52 定向反例 | 20 / 20 通过；包含真实生产请求协调器下的截断节点重试 |
| JS/MJS 语法 | 96 文件通过 |
| 确定性构建 | 两次 bundle SHA-256 相同；62 个可达模块 |
| Edge 主题/生产页面 | 73 场景通过；含 8 主题×8 视图、320–430px 宿主污染、横屏与 reduced motion |
| Edge 自动调度/设置 | 双页真实 Web Locks、楼层去重、刷新、A/B 隔离、缺锁停用、设置点击/间隔保存/重载/关闭/群聊清状态通过；0 API 请求 |
| Edge 更新按钮 | 生产设置点击到 discover/version/update 三步通过，均为本地宿主桩，不访问真实更新服务器；目标目录精确匹配、不刷新页面 |
| Edge bootstrap | 320/375/390/430/768px 按钮可见且无横向溢出；默认启动与诊断请求完整 runtime 均为 0 |
| Edge IndexedDB | 首次写读、全新页面冷读、删除栅栏冷读、来源账本冷删除通过 |

Bundle SHA-256：`15F67D6FD54437DB2FF1B8296DB82384E9290AB01A7579CE3B09A89C6AFC1663`。
主 agent 已目视检查新增主题、房间、User 气泡与自动规则截图；图片是本地合成宿主测试，不是用户真机效果证明。

最终独立回执：生成 reviewer 8/8 定向通过；生命周期 reviewer 7/7 定向通过，均未发现所报阻断项遗留。两位均未编辑生产文件，其真实服务商/真机验收仍未执行。

## 安全复核

- 保留 V3 存储键、Phone v4、Room v3、gzip 格式与现有 CAS/revision/delete fence/lifecycle，未迁移或删除用户档案。
- 模型修复路径只能来自本地枚举；任意属性路径、伪造记忆 ID/锚点与越界关系有负例测试。
- 自动模式不自动生图/TTS/写聊天；失败来源不被当成成功完整读取。
- 更新器依据宿主现有同源扩展接口做 exact-own-folder/remote 检查；使用宿主请求头，不读取私有 Key，错误不回显响应正文。[SillyTavern 官方后端实现](https://raw.githubusercontent.com/SillyTavern/SillyTavern/release/src/endpoints/extensions.js)。
- 这属于改动路径复核和回归，不宣称全仓穷尽安全审计。

## 本轮未做 / 待验收

- 柏宝绘 bbi_image 接入：缺获授权仓库 URL，见 IMAGE_INTEROP-r52.md。
- 朋友建议：仅评估，见 FEATURE_DECISIONS-r52.md，没有实现邀约、送礼、情感数值、平行存档、自动发消息等新玩法。
- 真实 SillyTavern/TT/iPhone、服务商生成质量与耗时、实际 Git 更新权限/网络、真实聊天 A/B 切换与浏览器后台休眠：未执行，不标为通过。
- 公开名称尚未替换生产身份，也没有进行同名/商标检索。
