# 心迹回廊 r84.9 · 原风格 UI 分区候选

版本：`0.8.85-tt-cg-r84.9-workspace`
唯一代码基线：Hearttrace-r84.8-actual-count-full.zip（SHA-256 `b95e80cf8a185915e0a9a06b9489956139f4c31029f1b1113cab3b215db6d640`）。
UI 依据：本对话确认的 v4 预览与八项要求；预览里的示例数据、生成替身、宿主测试代码没有进入运行文件。

## 实际接入
- 设置／当前档案／内容；来源与建档前置，概览折叠，卡片／列表与四个分组，窗口展开／还原。初始设置页和可选阅读位置恢复。
- 未生成可先进入空页面；点击明确生成按钮才委托原功能。不可读取的旧内容不视为缺省空数据，也不显示覆盖生成入口。
- 基础语言、日常一格、萤火虫与未来／后日谈提供直达页面，继续使用原 HEART 存档和原生成任务；没有迁移或复制记录。基础语言可以切换类别、上一句／下一句。
- ADV 默认阅读；CG 单独切换。图片设置使用原 cgPromptEditor.js；时空回响内页源码未改。
- 房间使用代码内置 SVG＋CSS 家具与现有外形枚举；没有固定人像、绿幕或模型提供的 SVG。实际房间、物件、人物对白、宠物、深层入口保留。
- 私密是普通可见相簿分类，位于结局前，全部仍含私密条目。仅按现有明确成人标签／文字本地判定，不读图像像素，不是全量成人内容识别，不加遮挡或加密，不额外调用模型。
- 原主题、跟随酒馆、自定义保留；深色覆盖内卡片和阅读文字。语义纸张与房间仍有区别，不把所有模块改成同一张黑卡。
- HEART 独立页面的管理仅展示当前子模块条目；单项删除、重生成和图片操作仍走原校验/确认。不会用“删除后日谈”去删除整个 HEART；基础语言整套替换仍走原两次确认。

## 未改
生成 Prompt、数量规则、单项内容校验、历史证据、档案身份、archiveRevision/CAS、删除栅栏、12 MB、独立备份、请求并发/超时与生命周期。仅新增 UI 标量偏好，不保存 API Key 或故事正文。bootstrap 除版本两行外与 r84.8 相同。

## 测试
- 新 UI 行为回归：38/38；冻结合同：23/23。
- 全量547项：542通过、5失败；5项均是原包已有的退役 r81TimeJourney 测试。未跳过、删除或改变这5项。
- 全部JS/MJS：228/228语法通过。
- 104模块bundle两次重建一致；SHA-256 `af58321104c6b2490a3b346821c537f200d8358af8ff0117d5cfba99ff061d0a`。
- Chromium隔离宿主53项交互检查通过；20页面×4主题×6宽度=480布局组合无横向溢出或脚本错误；13处深色纯色背景文字采样对比度>=4.5。
- 图像、生成内容、HTTP返回、存储后端是合成测试材料；实际渲染与点击处理使用候选bundle。模型列表只模拟一次HTTP返回，无实际外网请求。
- iPhone/Android真机、实际SillyTavern/TT、真实API未执行。截图中的字体/图标受隔离宿主影响。
- GitHub无写入。Codex Security专用工具未执行，独立回执未取得；完成本地差异安全复核，不代表独立审计或零风险。

## 安装与回退
等待在途任务完成；先备份现用插件目录与档案，再把本包tokimemo-main内文件同目录覆盖，不多套一层目录。不要只换src或单独换bundle。
完整刷新/重启客户端后核对诊断身份。无需清除站点数据、聊天、IndexedDB或重建档案。
回退时还原先前整个插件目录再完整刷新；本轮没有档案迁移，不以清数据作为回退手段。
建议先交给小克对照本包与原r84.8复核，再在单独测试环境确认实际手机体验。
