# r52 收口方案（2026-09-07）

本轮以 r51 0.8.47 为保留基线，主 agent 唯一修改生产代码。两位独立 reviewer 已阅读 CURRENT，只读审查；其建议须经生产入口测试确认，不直接合并。

## 已复核根因与唯一方案

- 房间：无法识别的当前对白被当成往事，删除物件后级联成结构不足。区分固定诊断与文本内容，修复具体字段，不用无条件放宽历史证据。增量改为新增物件 patch，本地保留旧正文，不要求模型原样回传未发送给它的旧正文。
- 蝴蝶：十节点完整 JSON 是局部修复的前置条件，缺节点或截断永远进不到局部修复。改为本地十槽位计划、逐节点请求和校验、最后统一验证与正式提交。人物关系仅允许有明确双人先行词的紧邻同主语续句。
- 对话：UI 已有 User 气泡；需同时完善明确说话人标记、旧稿明确姓名动作归属与 User 样式，未知主体不猜成 char。
- 终端：独立 PHONE 存储已存在，只拆入口、依赖、返回及删除级联；保留所有旧会话与断点。
- 自动更新：默认关闭，逐模块楼层阈值；只跟随当前聊天，复用原生成与归档边界。正式档案同步与衍生模块分开开关；不自动生图、TTS、改主聊天正文或更新插件代码。
- 内置更新：只允许用户显式点击，定位本扩展，调用宿主已有 Git 更新流程；不 hard reset、不重装、不绕过权限。ZIP 非 Git 安装明确提示。
- 配色：四套原创色板与局部装饰，参考 GS1–4 色彩方向，不复制商标或素材。
- 生图：待用户提供获授权仓库地址验证标签消费者；不将未知标签/私有 DOM 当成公共接口。
- 朋友建议：只出产品取舍文档，不实现其中新增功能。

## 必须保留的边界

V3 档案键与 gzip 格式不迁移；来源、聊天归属、revision/fence/CAS、取消与销毁边界继续有效。正式内容只有完整校验和提交成功才标为完成。离线测试不等于真实模型、SillyTavern、TT 或 iPhone 验收。

## 最终收口

两位 reviewer 已取得独立回执：生成路径 8/8、生命周期路径 7/7 定向通过；主 agent 完整回归 354/354。发现的跨组修复、第三方关系续句、busy 检查点、设置中途改动、失败恢复、状态展示问题均先复现再修。没有合并任何未经主 agent 验证的 reviewer 补丁。
