# Hearttrace r76 修复与验证说明

版本：**0.8.71 / 0.8.71-tt-cg-r76.0**。日期：2026-09-14。

本候选承接已交付的 r75 ZIP，源码仍来自用户上传的 `Hearttrace-r74-trace-fix-完整版(1).zip`。未以 GitHub main 替代，未修改上传原包、上一版 ZIP 或项目 CURRENT，也未向 GitHub 提交或发布。

## 本次问题与修复

| 用户反馈 | 修复后的行为 | 证据范围 |
| --- | --- | --- |
| 邮箱、私人终端等提示本地 token 计数超时，未发送生成 | 修正 r75 新增的全局 5 秒硬失败。计数服务超时或不可用时，按 r74 原有的 96,000 字符上限继续本次已授权请求；取得有效计数时，32,000 token 上限仍有效。超大字符输入、取消继续在发送前阻止请求。 | 模拟计数服务挂起、拒绝、迟到成功/失败、无效值与数字字符串；正常请求只发送一次。通过真实 requestJson 入口验证计数期间占用的任务位在成功与取消后释放。 |
| 未完成卡片只说没有可识别的原因 | r75 已保存的计数超时代码可识别；缩短恢复提示。新失败记录仅保存固定白名单代码。 | 真实恢复草稿保存和重开渲染测试；成功分段可复用，未知错误不伪装成校验错误。原先已丢失原因的旧草稿无法追溯，仍显示原因未记录。 |
| 诊断报告铺在正常界面，标题竖排 | 普通设置和档案页不再放重复诊断面板；扩展设置里保留默认折叠的“心迹回廊 · 故障排查”。复制或导出成功不展开 JSON；主动查看或需要手动复制时才显示。关闭会清空报告，迟到的剪贴板错误不能重新打开。 | 冷启动、运行模块切换、加载失败和浏览器能力失败的 DOM 事件测试；样式仅限插件元素。真实 Safari 排版和系统下载仍需真机验证。 |
| 从魔法棒重新打开没有回到退出页面 | 同一聊天、当前页面会话内记忆最后退出页面和可用的阅读位置，补齐首页、档案室入口、档案馆和角色目录。关闭插件保留后台任务；首页设置分区恢复展开状态。 | 页面、阅读进度、重复关闭、聊天/角色隔离、版本和删除失效的回归检查。 |

用户提供的脱敏报告显示那次档案整理已完成：2/2 分块通过，随后解析、校验、简介及保存完成；报告采样时没有活动生成或占用的 provider 请求。它不能证明邮箱等其他任务也已成功。

## 兼容与限制

- 超时后继续使用字符预算，不把字符数伪称为精确 token 数；所选模型的真实上下文上限仍由服务端检查。没有增加模型自动重试。宿主计数接口没有可用的取消参数时，插件停止等待，但无法终止宿主内部已开始的计数。
- 档案简介的正常请求仍在原有已授权流程内；简介返回无效时继续沿用本地简介，保留已通过校验的回忆。更新了 r75 中“计数超时必须不发送简介”的测试断言，因为这正是本次撤回的误拦行为。
- 页面位置只保存在当前页面的内存中，最多 20 个聊天；完整刷新、退出浏览器或重启 TT 后不保留这个阅读书签。历史快照不会借恢复页面变成另一聊天的可写档案；档案版本变化、删除或内容失效时回到安全入口。
- 已保存的档案、图片、双方外貌与日常分格路径没有在本次重做，相关已有测试纳入全部回归。图片持久化、模型画面一致性等仍沿用 r75 的真机验证范围。
- 诊断不含聊天、人物资料、外貌正文、提示词、响应正文、地址或密钥，不为生成诊断而解压缓存或启动完整运行模块。

## 自动验证

- 最终全部 15 个测试文件：**120 / 120 通过**，失败、跳过、取消均为 0。包括本次 8 项计数与请求测试、5 项恢复草稿测试、14 项导航测试及 3 项诊断入口测试。
- 定向复现：修复前的计数挂起测试出现同样的 `RMT_TOKEN_COUNT_TIMEOUT`；已有超时草稿恢复卡片不能识别原因。修复后对应请求、保存和渲染路径通过。
- 导航测试额外覆盖真实档案馆渲染器在关闭、换页、换聊天和较新打开请求之后的迟到返回，均不能覆盖当前页面。
- 全部 **117 个 JS / MJS** 语法检查通过。
- **91 个模块** runtime bundle 连续两次重建字节一致，生成文件能导入且 5 个公开生命周期/入口导出存在；manifest/index/build 版本一致。
- 独立只读安全差异复核未发现剩余新增 High / Medium 问题。档案馆晚到覆盖问题在复核中发现并修复，随后执行了定向回归。
- 原上传 ZIP 的 SHA-256 和只读基线的 119 个源码文件逐一比较，均未改变。
- “全部测试”指当前交付源码实际包含的测试，不代表执行了项目 CURRENT 中未提供的其他历史测试。最终 ZIP CRC、逐文件哈希及重新解压后的测试结果见外附 `Hearttrace-r76-validation.md`。

原上传 r74 ZIP SHA-256：
`cffff7fe661be936a11df13aeb433b020de9f57bf0ee313b3620a7508108ef70`

直接基线 r75 ZIP SHA-256：
`1f29eea0d5f7a797e4f42b210b1f4091605405cc254dc8ab731953ecafc8495e`

runtime 源码指纹：
`d8e2d885bbb82dd3550aa094f98b66d72c4743750b1bce55852cbbc1213cb402`

runtime bundle SHA-256：
`e5b1a5eb1cf37efa56a4db80506b75673d7f5228639de7d4b909efc254761777`

## 安装后确认

用 ZIP 内 `tokimemo-main` **里面的文件**覆盖原插件目录，保持 `manifest.json` 同层；重新加载后确认 **0.8.71**。这次不需要重建已经成功保存的档案。对失败项目点“重试未完成部分”，只由你主动补未完成内容；若升级前的本页临时草稿已因刷新消失，从对应内容入口重新发起即可。

需要真机确认：邮箱、私人终端等正常生成；关闭后从魔法棒重新打开的页面和阅读位置；故障排查默认折叠、复制及导出；原有 CG 与外貌功能。**未执行真实浏览器视觉测试、iPhone / TT 真机测试、真实文本服务请求或付费生图。** 本地 DOM 事件与模拟宿主测试不能替代这些验证。
