心迹回廊 r84.27 完整本地测试候选
加载标识：0.8.103-tt-cg-r84.27-tt-draft-durability
上传文件名：hearttrace-upload.zip；内部单根：tokimemo-main/。

沿用现有一键ZIP导入流程，选择本次包；本助手不会自动上传或发布。
手动安装时把单根内的index.js、manifest.json、src、dist等放到同一插件目录层，不套双层目录。保留旧包和档案备份，不清站点数据。

恢复：
1. 更新前，若旧页面仍有未落盘成果，先导出待入档成果。已销毁且无任何副本的RAM不能被新包还原。
2. 更新后核对加载标识。回原TT/浏览器、原访问地址、原聊天打开“当前档案”，会按需读本机记录，不请求模型。也可点“读取已保存草稿（不生成）”。
3. 读取未完成不等于没有记录；不要清数据/完全重建/重做41次。已有旧导出可使用“导入整理草稿”。
4. 保存失败时区分“本机已确认保存”和“当前页面共有”。先导出保全页面；存储恢复后点“保存本页草稿（不生成）”，实际确认后再继续。
5. 新任务在首个付费请求前确认保存，成功分段逐段保存；失败不再静默变成页面-only继续请求。无可写存储时整理会暂停，而不是冒充安全保存。不会锁住已经归档的内容阅读。
6. 草稿必须匹配原聊天/角色/来源/配置及revision；不会为了恢复删除校验或覆盖其他聊天。换设备或清站点数据并非自动同步。

本机记录的存在与完整性需现场确认；本地模拟测试不是已经找回用户41段的证明。
完整三轮、失败归因、原生IndexedDB/TT/真机未执行事项，以及CRC/SHA见包外Hearttrace-r84.27-validation.md。
