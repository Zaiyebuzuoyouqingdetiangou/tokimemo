# 心迹回廊 r84.11 · 修复与自复核

版本：`0.8.87-tt-cg-r84.11-song-reader`
唯一基线：`Hearttrace-r84.10-reader-cg-full.zip`
基线 ZIP SHA-256：`37f33b1e278e239cad043e52079decc273ccc18b7061e8f1e0bdb57524d7332f`
原包保留；本轮未写入 GitHub。

## 交付范围
### HEART 生成后的页面状态隔离
修复的不是剧情内容，而是生成补丁合并时夹带页面选择的问题。
- `src/modes/heart.js`：内容补丁不覆盖 view、selectedSeason、篇章/日常一格/萤火虫等七项选择标量；规范化后再次保留原选择，避免默认值反向污染缓存。
- 生成完成发布时，校验角色/聊天/版本及 live/历史档案目标；正在显示同一 HEART 时只合并新内容，保留当前阅读选择。
- `src/archive/repository.js`：延期 HEART 写回使用相同的“只合并内容”规范化路径，不再通过默认季节填回路由状态。
- `src/generation/client.js`：整组基础语言生成和印象曲生成均受精确阅读页面检查约束；用户已离开或选择其他篇章时，不抢占页面。
- 原有独立路由、翻页、书签、图片目标与取消方式保留；未修改 HEART 的任何原剧情 Prompt、歌词以外内容规则或数量标准。

### 新增「角色印象曲」
入口：内容 → 互动 → 角色印象曲。
- 角色主题：以当前角色卡、人设和世界书创作，不要求先有特定往事；仍以当前聊天的有效档案身份隔离存储。
- 事件主题：在当前档案中选择真实事件，事件 ID、锚点、演唱者和写入目标由本地取得，模型不能指定。
- 可选择中文／日语／英语歌词，角色独唱／双人合唱／旁观者演唱，及最多400字符的创作方向。
- 分别输出原创歌名、演唱者设定、人声说明、中文曲风描述、英文风格提示词和带段落标记的完整歌词。
- 复制歌名、复制曲风、复制歌词、复制全部及导出 TXT；只生成文本，不生成音频，不接入任何音乐服务，不在产品界面中出现第三方平台名。
- 使用 `[Verse]`、`[Chorus]`、可选桥段/收尾等段落标记；主歌、副歌不能空，最后有 `[End]`。不强迫固定段数，不用“副歌同上”等占位词替代正文。
- 每次明确点击只创作一首新作品，默认不自动重试。失败保留已保存作品；恢复使用原任务的语言、演唱者、主题和请求身份，已完成分段不重做。
- 保存基于现有缓存 CAS、档案身份和模式写入栅栏。新作品与旧作品按本地 ID 合并；同 ID 的不同正文拒绝覆盖。
- 歌词是创作作品，不反写 Mxxx，不当作新的共同历史证据。
- 可单独删除一首，连续两次确认，重新核对被删歌曲内容；只读/独立备份不允许生成或删除，可以阅读、复制、导出。
- 本地边界：每档案最多80首，歌词每首5000字符、英文风格提示词900字符；这些是插件容量保护，不是任何外部工具的兼容承诺。

## 本次变更没有扩大到哪里
- 共用 `src/generation/prompts.js` 与 r84.10 逐字一致，SHA-256：`70a615e2af5dd680e80ba7b06f7ad502cffacf4a8c4d9ed22ea67039c3a5ff25`。
- 所有旧 HEART Prompt 构建器、单篇校验器保持原文。歌曲只使用新的 `themeSongPrompt`。
- 原 NAI 4.5/5 提示词格式、图片编辑器、图片发送协议没有变化；额外8项 CG 复核全部通过。
- 共124个既有生产 JS 文件逐字节不变，包括其他内容模块、备份、证据、请求协调器、手动 API、主题、自动更新策略等。
- 为新模块增加了缓存、模式注册、渲染、生成分支；不能据此声称这些文件“一字未改”，但原身份、CAS、只读、删除栅栏、12MB与并发/超时条件均未放宽。
- `index.js` 只有版本/加载缓存身份变化。普通启动依旧不加载完整 runtime、扫描长聊天、读取世界书或打开 IndexedDB。
- 不恢复私有第三方桥接，不清理历史重复源码，不加入外置 CSS 或 `<link>` 运行依赖。

## 实际测试结果
- r84.10 全量：603项，598通过，5失败。
- r84.11 全量：675项，670通过，5失败；没有删除、跳过任何测试文件。
- 两版5项失败名称完全相同，全部是 `r81TimeJourney.test.mjs` 中已退役功能的既有测试，不恢复该功能来“刷绿”。
- 本轮定向：71/71（18项 HEART 生成/提交/延期/历史档案测试，30项印象曲行为测试，23项源文件及边界合同）。
- 上次自复核原始7项状态断言已纳入；原4项生成后失败均已通过。
- 本轮另运行原自复核8项 CG 发送、预览、格式、版本边界测试：8/8。
- 全部 JS/MJS：243/243 语法通过。
- runtime：112模块，连续两次重建一致，并与交付 bundle 一致。
- bundle SHA-256：`e8dc651f75ee4db4a23ad2b59e7a4f87bf7442810535c44be2dc2876ce83548d`。

### 冻结合同如何处理
历史版本测试的原始期望哈希没有替换，也没有跳过。对本次明确授权修改的14个既有文件，测试记录逐行差异、基线与候选哈希，先确认候选精确等于已复核差异，再仅逆向该差异后运行原有哈希断言。生产代码在行为测试中始终原样运行。
另有本版直接断言：124个未修改生产文件的原始哈希、旧 HEART Prompt/单项校验器的原文，以及新模块自己的实际行为。可查 `tests/helpers/r8411-source-deltas.json` 和 `r8411FrozenContracts.test.mjs`。

### 浏览器隔离验证
实际候选 bundle，而非重写的 HTML 原型；浏览器宿主和模型接口使用本地模拟。
- 320/375/390/430/768/1366px：84项既有相簿/ADV/HEART/图片编辑器页面主题检查。
- 印象曲：18项页面/创作表单主题检查＋12项歌词阅读与纯色文字对比度检查；总计114个状态/主题/宽度组合。
- 本地真实点击模型：9项印象曲交互，覆盖目录入口、三种复制、TXT导出、取消删除、只读、无内容先进入、创作表单→一次模拟请求→保存/重开。
- 未发现页面脚本错误或横向溢出；抽检歌词文字与卡片实色背景对比度最低约9.30:1。只代表已测试的主题组合，不是全部用户自定义背景的保证。
- 浏览检查未发真实文本/图片/音乐请求；测试期间未消耗用户生成额度。

## 差异安全复核
本轮按入口、模型返回、源数据身份、保存回放和 DOM 显示逐层核查：
1. 模型不能选择歌曲 ID、演唱者归属、事件 ID、缓存键、聊天或写入位置；测试中的伪造字段被忽略/拒绝。
2. 新歌词/说明经过有界结构检查，以转义文本显示；复制/导出为纯文本。无新增网络 endpoint、动态脚本或音频服务。
3. 旧内容合并、失败、恢复、切聊天/版本、销毁生命周期及历史档案写入通过实际生产函数测试；高影响保护沿用原实现。
4. 删除动作保留双确认，并在确认后校验阅读对象未变化；生成中、只读和被更新的目标不删除。
5. 错误使用固定安全文案，不反射 Prompt、歌词、角色卡、响应正文或凭据。
结论：本轮已复核差异中未发现尚未处理的 High/Medium。此为本地自复核，不是独立第三方结论。
Codex Security：查询到安装记录，但工具命名空间没有可调用扫描动作；专用扫描未执行，独立回执未取得。

## 未执行
iPhone/Android 真机、实际 TT/云酒馆、真实文本生成与图像/音乐服务调用均未执行。歌词质量、真实模型遵循段落格式及外部音乐工具的最终创作效果仍待实际使用确认。

## 安装
1. 等在途生成结束，保留当前插件目录和档案备份。
2. 把 ZIP 中 tokimemo-main 下的内容覆盖到现有插件目录，保持 index.js、manifest.json、src、dist 同一层。不要多套一层目录。
3. 全量替换，不只换源码或单个 bundle；完整刷新或重启客户端，诊断版本应为本页最上方的 r84.11 标识。
4. 不清聊天、档案、站点数据或 IndexedDB，不需要重新生成已有作品。原图、旧语言、四季和其他模块保持原数据。
