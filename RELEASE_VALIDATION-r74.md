# 0.8.69 / r74.0 — 双方外貌与 TT 回填修复候选

唯一修改基线：上一份交付的 r73 ZIP，SHA-256 `d492b3ab535412a4b2798d0ddd0b4078f080606fa717ccdaf08e691d656cf8a1`。继承仓库提交 `71489e69550b5d6f053be07c73d59b5b080123b7`；未 commit、push、PR 或发布至 GitHub。

## 改动与验证

| 问题 | 本候选处理 |
| --- | --- |
| 场景提示没有 char、user 外貌 | 现有“重新构思／提取外貌”的一次文本请求使用当前条目、公开角色卡及 user persona；同名柏宝绘公开外貌库可作为稳定依据。返回完整场景与双方独立 tag，绑定发起时姓名，不猜缺失资料。编辑器提供标签和发送预览，确认后才绘图。 |
| 后端可能忽略自然语言字段 | 支持多角色时发送 sceneTags + characters；其他后端的主 prompt 使用同次文本生成的完整英文场景，包含双方外貌和动作。修改场景或 tag 后清除旧通用提示，避免继续发送过时内容；保存元数据以便重绘。 |
| 图片已保存但相簿不替换 | 修复同围栏双侧均已完成时旧 canonical 模式覆盖较新缓存的问题；仍禁止未完成镜像、旧版本和删除后的结果回写。图片专用提交在独立备份已确认成功后，不再因宿主 metadata 通知同步失败而回滚已保存图片。 |
| 存储暂不可用导致重复出图 | 本候选当前页面保留已收到的合法图片引用，在图片设置中免费重试回填。只提交这一条图片，仍检查角色、聊天、版本、签名与生命周期；同时重试只执行一次，待回填期间再次绘图不会收费。 |
| TT 大图黑屏 | 保留 r73 插件内适屏／原尺寸查看器，不跳新网页。 |

上述两种缓存／镜像缺陷均先用冻结 r73 的真实 draw→commit 流程复现。正常状态下，r73 的 tauri 测试本身也能成功；不能仅凭截图断定手机唯一触发了哪一种失败。IndexedDB 不可用时不会假称持久化成功。

## 实际验证结果

- 最终包内专项 `node --test --test-timeout=5000 tests/*.test.mjs`：**46/46 通过**。
- 真实生产 draw、单项补丁、缓存合并、备份序列化、metadata 及相簿渲染参与测试；宿主、图像接口和存储边界使用替身。覆盖 TT tauri／HTTPS、重开保留、存储恢复、同目标并发重试、切聊及条目编辑保护。
- 公开参数专项验证双方外貌进入实际生成 request、支持／不支持多角色两分支，以及 deferred 图片元数据和已保存图片兼容。
- 全部 JS/MJS 语法：**104/104 通过**。
- runtime bundle：**88 模块**，连续重建字节一致；可导入并导出 `initMemoryTheater` / `destroyMemoryTheater`。
- source SHA-256：`287720fd7f2e3af784164663888625abaf7ffdc83a10e2d22749dc9e8fd9de26`。
- bundle SHA-256：`c494805fff264f0a317a4d260a0c7bae1fd64786b01e2439aedfc72916c7b107`。
- manifest/index 版本与缓存标识一致：`0.8.69 / 0.8.69-tt-cg-r74.0`。
- 独立 Codex Security 差异审查及通用提示增量复核：**无 High / Medium**。
- 使用项目 `tools/package-release.py` 校验 ZIP 单根目录、CRC、逐文件 SHA-256 与全新解压一致性；解压后再执行全部包内专项。

## 历史完整测试

当前源仓库已删除历史 tests，本轮沿用相同历史测试与测试辅助文件到独立 QA 目录，没有混入旧生产代码。单项超时 5 秒。

| 版本与同组历史测试 | 总计 | 通过 | 失败 | 取消 |
| --- | ---: | ---: | ---: | ---: |
| r73 + 原专项（上一轮记录） | 412 | 373 | 36 | 3 |
| r74 最终源码与 bundle + 本轮专项 | 442 | 403 | 36 | 3 |

失败／取消名称集合完全一致，无新增失败。本候选不能声称历史全套全部通过。原有房间、蝴蝶等问题不在本次修改范围；没有改动历史断言掩盖结果。中途 QA 目录缺辅助文件及 bundle 尚未同步的检查失败，补齐后重新执行，以最终完整结果为准。

## 安装与仍需实机确认

覆盖现有扩展目录中的文件，完整重启 TT，确认版本 **0.8.69**。进入“图片设置 → 重新构思／提取外貌”，核对场景和双方 tag，再确认绘图。新图应替换相簿抽象 CG；关闭再打开检查保留，查看完整原图应留在插件内。

未执行真实浏览器布局、iPhone/TT 安装、真实 IndexedDB/WebView 或真实／付费生图；画面是否正确遵从角色与动作仍需实际图像模型验证。已核对已知 TT 公开上下文具备 getCharacterCardFields() 的 description/personality/persona 及 powerUserSettings.persona_description。

待回填引用只在本版本当前页面保留；刷新、停用或重载会失效，请先重试保存。升级前仅存在于柏宝绘图库而没有相簿关联的图片不会自动匹配；不会扫描或改写第三方图库，不会删除旧图片、档案或宿主聊天。

公开兼容资料：[柏宝绘 API v1](https://github.com/baibai-git/ST-BaiBai-Image/blob/34157f4f08da5559f295b027d46279d06f4de5bf/PUBLIC_API.md)、[公开参数定义](https://github.com/baibai-git/ST-BaiBai-Image/blob/34157f4f08da5559f295b027d46279d06f4de5bf/src/public/types.ts)。
