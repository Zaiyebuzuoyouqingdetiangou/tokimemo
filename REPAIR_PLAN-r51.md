# r51 根因收口

以 r50 已封包文件为基线，保留原 ZIP。两个独立 reviewer 只读审查，主 agent 已对照 CURRENT 与生产代码确认：

1. Room：独立 schema 字段被拼成叙事，名称误触历史校验。按字段检查，保留每一字段内部的跨句历史检测；新生成和旧缓存读取用相同规则。
2. Butterfly：生产提示缺失实际字数、第一人称次数及算法结论条件，修复反馈丢失原因。使用一份本地契约和白名单错误代码贯穿三个生成入口与修复；不回传任意异常详情，不降低历史/关系/唯一性约束。
3. Heart：生成和旧缓存显示都把未知 speaker 当作 char。共享保守拆分器处理角色、动作及明确署名；无法确定归属时使用中性旁白，不猜成 char。
4. UI：主题覆写抹平表面层次，继承宿主字重/text-fill，漏掉页面留白。修订现有主题层，不另叠一套主题。删除用户点名说明。
5. 逻辑任务上限 10；provider 仍为 2；不改变档案写入、删除、聊天导航与来源边界。

验证必须覆盖真实生产 normalizer、旧缓存 renderer、主题实际页面、恶意宿主样式、逻辑与 provider 两级并发。模型服务与 iPhone 真机未接入，不能将离线验证称为真实生成验收。
