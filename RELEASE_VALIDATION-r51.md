# 心跳回忆 0.8.47 / r51.0 修复版验证说明

日期：2026-09-06。基础为 r50 / 0.8.46 独立副本；原工作树、ZIP 和附件保留。仅修改心跳回忆，截图中的兔子镜错误不在本次修复范围。

## 本次修复

1. 房间：物件名称、描述与台词不再拼成一句后判断共同往事，避免把正常的“木桌 / 表面很干净 / 你可以坐这里”误判成历史而移除，继而导致结构不完整。生成、重新打开、最终物件显示使用一致的逐字段检查。同一字段的虚构往事仍拒绝，真实记忆锚点、空间差异、四时段与宠物要求保留。
2. 蝴蝶效应：初次生成、增量和单节点重生共用本地完整性要求；修复请求得到白名单中的具体缺项提示，不再只有泛化失败文本。独白、介入、Ω、系统判词、世界结构要求明确，关系资格、证据与世界差异检查没有关闭。错误原文不会进入补齐提示。
3. 台词：新脚本要求每行一个说话人，动作独立；旧脚本按明确姓名、引号、姓名加冒号拆分，包括多行。NPC 使用自己的标签；归属不明确时中性显示，不冒充 char。旧档案不被显示逻辑改写。各次规范化统一采用拆分后的 120 行 / 50400 字符限额，避免再次截短；超限保留原档案并明确提示。
4. UI：按用户参考恢复白色、轻柔彩色点缀与卡片层次；正文正常字重，恢复卡片和页面内边距。对话气泡与旁白分开，修复小圆点触控范围及图标颜色。日间、夜间、跟随酒馆、自定义仍保留；没有复制第三方任意 CSS。
5. 删除用户指定的庭园视角说明、主题预览三句和设置中的档案/性能长说明。对应安全逻辑仍保留。
6. 同时最多 10 个逻辑任务；实际模型请求仍最多 2 个，超出排队，防止请求洪峰。不能据此声称总生成速度翻倍。

## 已取得的验证

- Node：334 / 334 项通过，其中本次新增 11 项回归。包含真实生产房间规范化与再次展示、虚构历史拒绝、台词拆分/归属/重复规范化、10 任务准入、蝴蝶校验，以及模拟 ConnectionManager 的实际请求协调器补齐流程。模拟服务没有访问网络。
- Edge/Chromium：37 个主题与布局场景通过。四主题的生产设置、档案、ADV、互动、头像、陈列柜和庭园检查；另含恶意宿主字体/颜色污染、窄屏及横屏、减弱动画、关闭按钮、气泡归属、正文对比度与溢出检查。截图经人工复核。
- Bootstrap：320 / 375 / 390 / 430 / 768 五个宽度通过；启动及只读性能诊断没有加载完整 runtime。
- IndexedDB：真实浏览器首次写读、新页面冷读、删除栅栏冷读、来源账本冷删除四项通过。
- 91 个 JS / MJS 文件语法检查通过；59 模块 runtime 连续两次构建一致，打包 runtime 可导入。
- runtime 源摘要：`c477af1d2f2e6285cbf627dab70cd7362ee5e14a664c98e165a359ea36d71352`。
- runtime 文件 SHA-256：`930622A5997733461401A812B5AA3F157FE32EAA0D8F659C001AC5F3ED691EB0`。
- 最终 ZIP 的 CRC、单根目录、路径检查、逐文件摘要与新鲜解压结果记录在同名 `.verification.json`；该文件位于 ZIP 旁，避免包内记录自身摘要的循环依赖。

## 独立复核与边界

生成链、来源/产品两位 reviewer 均取得只读回执；主 agent 汇总并验证根因后统一修改，没有合并独立补丁。第二轮意见补齐了最终房间显示检查、旧脚本二次截短、姓名前缀误归属和头像借用背景档案用户名等反例。生命周期、写回、删除围栏、API 隔离与模型文字转义继续由原回归覆盖。本次不是第三方渗透测试。

没有取得用户服务商的原始失败响应，不能断言截图所有失败都由同一根因造成。真实模型仍可能缺字段、截断或不满足内容要求；首次十节点大 JSON 的输出长度风险仍存在。不会以关闭完整性检查或伪造内容掩盖失败，失败不覆盖旧成品。

本次没有连接真实 SillyTavern / TT / iPhone 或付费模型接口。浏览器尺寸模拟不等于手机实机验收，宿主提供的图标字体也须实机检查；任意第三方主题的完整复刻不作承诺。未知归属旧文本采用旁白，不能凭空恢复缺失说话人。

## 安装与实测

备份现有插件和档案后，覆盖更新当前心跳回忆插件目录并刷新酒馆；确认版本 0.8.47。不需要删除档案、清空浏览器数据或完全重建。要接近参考图，选择“日间”；已保存的跟随/自定义设置不会强行重置。

单独重试房间和蝴蝶效应；检查失败是否保留旧内容。打开已有互动，确认动作在气泡外、明确的用户台词不挂在 char 名下。检查庭园左右留白与头像弹窗；多个生成任务期间关闭插件、切换聊天，确认原任务只写回自己的档案。如仍失败，请保留版本号、模式和安全错误提示；不要发送 API Key 或未脱敏的完整私密来源。
