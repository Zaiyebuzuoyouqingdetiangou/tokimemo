# r54.0 TT 兼容修复候选验证

版本 0.8.50，BUILD `0.8.50-tt-compat-r54.0`。从 r53 独立工作副本修改，旧目录、压缩包与旧档案保留；未推送远端、未安装到用户 TT、未调用真实付费模型。用户此次范围：窗口关闭/切换、TT 设置可读性、Room/Travel 失败、按楼自动增量。

## 根因与对应修复

| 问题 | 代码证据与修复 |
| --- | --- |
| 关闭/切聊 | 关闭档案仍依赖 native confirm。TT 原生层存在未显示确认但返回 false 的路径，会使 modal 留在最上层。移除这一可逆操作的确认，关闭 dialog 不取消任务；删除/重建确认、整页卸载警告和原聊天提交围栏不变。未声称这是用户设备上的唯一原因。 |
| TT 看不清字 | 宿主固定 label 高度、复选框样式及伪元素可裁切/遮挡插件文字。对插件自有字段、复选标签和控件限定重置尺寸/换行/伪元素；不改 TT 和用户主题，保留 r53 语义纸张。 |
| 自动增量 | 原始楼数包含隐藏消息，归档快照却排除全部 is_system。TT 隐藏普通楼层仅切换此标记，使旧指纹不匹配。两处共用严格角色/用户资格判定；真系统/工具仍排除，编辑/删除/重排仍拒绝。增加 event_types 命名支持、缺锁逐项状态及基线不符的固定诊断。 |
| Room | 泛词“宠物”与跨句匹配把鸟画、训狗职业、他人宠物误算为必需物种。改为同物种、分句和明确主体；排除明确第三方、媒体、假设、计划，保留直接养/领养及有据长期照料。原宠物/正脸/历史校验不取消。 |
| Travel | 证据不允许补造地点，却要求近处≥2、远方≥2，缓存也拒绝少于4站。统一为初次1～8站、允许单范围；0个合法地点给固定原因并保留旧地图。 |
| Room/Travel 来源 | 目标档案的显式设定选书先前未进入这两项受控上下文。复用只读选书接口，A-only、非历史、完整才使用；不恢复会读当前B的dry-run。缺条目、读取失败、超过世界书/合并证据容量均在请求前停止。 |
| 证据禁词 | 精确设定引文含禁词时先被当新台词拦截。仅对指定Room/Travel证据路径、800字符内受控逐字引文豁免；台词、伪造引文与其他路径仍拦截。 |

## TT 依据

固定核对提交 `9693a4ec47cd4552f90878bccab453f176de0f18`：

- [隐藏聊天楼层实现](https://github.com/Darkatse/TauriTavern/blob/9693a4ec47cd4552f90878bccab453f176de0f18/src/scripts/chats.js#L147)：保留原文与数组长度，仅改变 is_system。
- [当前聊天公共 API](https://github.com/Darkatse/TauriTavern/blob/9693a4ec47cd4552f90878bccab453f176de0f18/src/tauri/main/api/chat.js)：当前窗口模式 off，不能拿过时 ExtensionDEV 的截尾说明断言用户楼数被截断。
- [Apple native confirm](https://github.com/Darkatse/TauriTavern/blob/9693a4ec47cd4552f90878bccab453f176de0f18/src-tauri/crates/tauritavern/src/infrastructure/apple_webview_js_dialogs.rs)：无法取得呈现控制器等路径返回 NO。

TT stylesheet 仅下载到候选之外作为兼容性测试输入，不随本插件打包；生产代码为自身实现。

## 独立复核与主 agent 收口

两个 reviewer 均先读 CURRENT，只读审查；所有生产修改由主 agent 独立完成。主 agent 对宠物误要求、稀疏地图、隐藏指纹与来源问题先复现，再修改与测试。

- 导航/自动更新 reviewer 的目标文件复核未发现新增阻断，独立18项局部测试通过。
- Room/Travel reviewer 发现长角色卡造成所选设定尾部在本地证据中丢失、朋友/画中宠物误要求；主 agent 新增反例先失败，再前置完整包含检查和收紧主体。复核确认两点收口。
- 第二次复核指出“领养”直接动词漏识别；主 agent 明确加入动词与否定测试，没有恢复任意主体桥接。最终定向及全套回归覆盖。

## 工作副本新鲜验证

| 检查 | 结果 |
| --- | --- |
| Node 全量 | 369/369，通过 |
| r54 定向 | 9/9，通过；隐藏/真编辑、系统排除、宠物/单站缓存、A-only/缺失/漂移/超限、禁词路径、固定安全诊断 |
| JS/MJS 语法 | 99个文件通过；模块源入口与bundle可导入 |
| bundle | 62个可达模块，连续构建确定性核对 |
| TT 样式与关闭 | 320、390、844横屏及390浅色宿主；实际TT CSS加合成宿主裁切规则，文字/输入完整、复选可点击、无横溢出；confirm=false时关闭成功，宿主切聊收到点击且任务未取消 |
| 自动增量完整入口 | 实际设置开关/间隔→event_types消息事件→真实调度→真实归档与持久提交；仅provider HTTP为本地模拟。隐藏旧楼层及一条新楼层后，M001不变、新增M002、原文未改、不打开overlay。随后编辑旧正文，零新provider请求、显示基线不一致、旧档案保留 |
| 主题/实际页面 | 91场景通过；八主题、语义纸张差异、正文对比、移动布局与污染场景 |
| 原设置/调度/更新回归 | 4组通过；双页Web Locks、重载去重/隔离、设置持久化、模拟宿主更新按钮，未实际更新远端 |
| 惰性启动 | 320/375/390/430/768通过，普通启动和性能诊断完整runtime请求均0 |

最终ZIP的CRC、单根目录、逐文件SHA、全新解压后测试结果见包外 verification.json 与 r54 交付校验说明。工作副本检查不冒充真实宿主验收。

## 限制与升级

- 旧档案若当初就漏收隐藏楼层，新完整快照无法与旧基线相等；显示“基线不一致”，不会静默重写指纹、删档或重建。真实编辑历史也保留此保护。遇到此提示应先检查来源，不能承诺任意旧档案自动迁移。
- 当前仅验证固定TT提交的公共契约；旧窗口化TT、实际iOS/Android原生WebView、用户完整自定义主题未实测。普通浏览器测试不能替代手机验收。
- Room/Travel 的实际模型内容、服务商错误、耗时与成功率未验证，不保证任何模型必定成功。明确选书过多会在请求前提示容量不足，需减少所选条目；不悄悄漏掉它们。
- 后台只指保持TT页面存活；关闭应用、杀进程、系统挂起仍可能中断网络。没有修改第三方记忆/绘图插件，没有继续柏宝绘适配或朋友建议新功能。
- 覆盖现有插件文件后重新加载；无需删除档案或站点数据。不要把候选当作已远程发布版本。
