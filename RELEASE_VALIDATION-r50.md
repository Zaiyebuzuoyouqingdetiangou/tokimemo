# 心跳回忆 0.8.46 / r50.0 修复版验证说明

日期：2026-09-06。基础为已核对的 r49 / 0.8.45 工作树；保留原 ZIP，不覆盖原始附件。RabbitMirror 1.5.18 仅供输入标签过滤行为参考，没有套用其 UI、持久化或连接实现。

## 九项反馈与本次结果

1. **无新记忆也能扩写**：ADV、相簿、蝴蝶分歧、角色互动、出行文字与既有类型后日谈可主动继续。旧正文/图片保留，仍绑定原 Mxxx；正式档案不会自动增量。相同成品会去重，达到模式容量上限仍停止。ENDING 不能借扩写解锁旧锁定路线。
2. **世界书 NPC**：本档案选择的非历史设定书独立进入庭园，无须先在剧情登场；world/uid/原文必须匹配。剧情关系和设定人物分层。超过 18 人可打开完整列表切换；模型一次最多提取 32 个设定人物。书读取失败/截断时保留旧庭园，不以空结果覆盖。
3. **房间与来源账本**：房间进入有界校验/重试链，空间结构、宠物和共同往事错误显示不同的安全原因。IndexedDB 首次打开失败不再永久复用拒绝的 Promise。无当前聊天与存储失败分别提示；不打印私密详情、不建议删除站点数据。这不能据截图证明用户环境的 IndexedDB 故障已彻底消失，也不等于所有模型结果都会通过。
4. **蝴蝶效应**：修正把“未爱上别人”误当成第三方恋情的局部判断，同时防止否定前句放行后续肯定。有效的有界 10+ 节点不再因恰好不是 10 被拒绝；缺失/重复/不合格节点单独修复，原有深度、轴差异和关系安全校验不降低。
5. **隐藏心跳**：心跳波形、记忆星座、信号灯塔、信笺抽屉四种本地结构，角色/告白意象与文本可不同，四种实时操作继续可用。不是无限生成任意程序，不执行模型代码；已有保存文字不会被后台重写。
6. **共同陈列柜**：具体物件、逐字原文和两人关联须在真实记忆中核实。第三方之间的礼物、虚构物件拒收。点击查看出处；无证据时空柜合法。图形是本地类别示意，不是真实物品照片。
7. **主题**：日间/夜间/跟随酒馆/自定义；设置、结构卡片、弹窗和文字共用调色。修正旧高优先级渐变造成的白底白字与手机图标文字重叠。跟随使用 body 标准计算背景/文字，不读取其他美化插件私有配置，不承诺复制任意第三方皮肤。自定义有预设起点和控件，必要时自动收敛到可读配色。场景画面保留本地艺术配色。
8. **标签过滤**：最近 500 条、最多 256000 字符扫描；排除列表最多 32 项。默认 thinking/updatevariable/updatevarible。支持大小写、嵌套、编码、未闭合与自闭合；保存需显式确认。只过滤模型输入副本，不修改聊天、来源原文、档案或覆盖游标；已生成内容不会自动清洗。
9. **速度**：分段最多两个并行，仍受全局 provider 2 限制；重试复用同请求受控上下文；无新历史的 ENDING 不再重复扫告白。去重尽量前置。没有真实服务商测速，不给出虚构的提速百分比。

## 自动验证

- Node：323 项测试通过，包含 ArchiveTarget、持久化、删除、API 隔离、来源协议、表现安全和 r50 新负例。
- Edge/Chromium：主题矩阵 35 项（7 项基础与污染/尺寸检查，28 项真实生产 renderer 页面检查）。四主题下检查设置、档案室、ADV、角色互动、头像弹窗、陈列柜、人际庭园；有截图人工复核。
- Bootstrap：320/375/390/430/768 五种宽度通过；普通启动及性能诊断请求完整 runtime 均为 0。
- IndexedDB：首次写读、全新页面冷读、删除围栏冷读、来源账本冷删除通过。
- 88 个 JS/MJS 文件语法检查通过，57 模块 runtime 连续两次确定性构建一致；ZIP 做 CRC、路径、单根目录、逐文件 SHA-256 比对及新鲜解压复测。最终 ZIP 身份见旁边的打包校验 JSON。

## 独立复核与安全

生成链与来源/产品分别取得只读 reviewer 回执。主 agent 核实根因、统一修改，未直接合并 reviewer 补丁。复核提出并修复了第三方礼物、未闭合过滤块、模型 JSON 键误过滤、跨聊天世界书归属、无新证据结局解锁、同锚点成品被去重吞掉等问题。详见 SECURITY_REVIEW.md。本次不是第三方渗透测试。

正式记忆/派生缓存 V3 key、12 MB 提交上限、CAS/revision/delete/lifecycle 围栏、懒加载、API 连接隔离不变。没有 GitHub 写入，没有读取或删除用户真实聊天。

## 安装与尚待实机验收

先备份酒馆数据；将包内 tokimemo-main 文件覆盖到现有心跳回忆扩展目录，不并排安装第二份。重载页面并确认版本 0.8.46。旧派生内容保留；选择新主题后，主动刷新庭园/房间或扩写需要的功能。

仍需用户在自己的 iPhone/TT/SillyTavern 和 API 上验收：房间与蝴蝶完整生成、真实大档案耗时、切换 A/B 聊天继续生成、关闭窗口后档案保留、系统杀进程/存储限制、第三方主题污染和触控调色。自动测试不证明这些已在用户设备通过；不要因此清除站点数据或删除原聊天。
