# P0 修复候选 0.99.1

加载标识：`0.99.1-tt-cg-r84.46-p0`。在上一份 0.99 / r84.45 TTS 完整候选上累计修改，保留镜译朗读。此次按包内《施工方案》的 P0 范围处理：手动重试反馈、私人终端通讯，以及可执行的验收检查。顶栏空圈等另列在开发清单中的问题不在这次修改内。

## 修复内容

- 手动恢复未完成段时，根据上一轮的安全分类说明完整 JSON 缺失、截断、结构或长度不足。说明进入实际模型消息及预算测量，不再被冻结的旧 payload 覆盖；不修改原请求身份，不重发已完成段，不保存或发送原始错误文本中的密钥与任意指令。
- 与用户的推演通讯允许主人一侧至少一条未发送草稿。过滤模型编出的用户回复，保留合法草稿；界面明确显示“未发送草稿 · 不代表已发生的聊天”。
- 历史双向原话只接受所引记忆中的逐字内容与说话人归属；只有摘要、无法核对时降为主人草稿，清除历史来源标记，不把推演当历史。
- 设备名与角色卡标题保留。通讯发言者使用经受控资料核验的成员真名；不把同一张多人卡的标题显示成所有成员姓名。
- 旧目录缺少成员名单时，可在同一次通讯响应中提供名单及原句，由原冻结资料核验；旧失败请求在发送前附加通讯合同修订，不需要为了修名字重新生成已完成 App。已有明确多人选择时，未勾选成员不能作为新消息的主人。
- 补缺使用明确对象和草稿／日常类型，跳过没有合法对象的空槽位。通讯没有可保存对话时给出专门原因，不自动用同一请求重试；其他已完成 App 与正文保留。

## 更新方法

保留原包，保存当前编辑中的内容，再按原来的扩展更新方式使用 `Hearttrace-0.99.1-r84.46-P0-full.zip`。ZIP 内是单层 `tokimemo-main/`。手动更新时将其内部文件覆盖到现有心迹回廊扩展目录，包含 manifest.json、index.js、src、dist；不要再嵌套一层目录，不要当角色卡导入。

刷新酒馆后核对版本 0.99.1 / r84.46。查看已保存的私人终端，打开通讯或按已有恢复入口继续未完成项。没有合法说话人或对象时应补充受控资料，不能通过编造用户回复来让校验通过。现有生成额度提醒、身份／聊天／档案版本保护与保存失败处理仍然生效。

极旧的失败草稿若缺少完整冻结请求或资料快照，会提示先导出保留草稿，再单独处理未完成通讯；不会自动拿当前资料替代旧来源来强行续写。已通过的其他 App 不因此重做。

镜译继续在原来的“朗读 · 镜译”入口使用，接口配置说明见 MIRROR-TTS.md。此候选未自动部署到酒馆，未推送 GitHub。原项目自动更新设置保持原样，上游更新可能替换本地候选。

回退时恢复上一个完整扩展包并刷新；本次不要求清理站点数据或重建档案。请保留更新后新生成的内容副本；旧版本对新增通讯草稿字段的展示可能与本版不同。

## 验证范围

包外验证报告列出实际定向回归、语法、bundle 导入与确定性重建、ZIP CRC／SHA 和浏览器检查。浏览器通讯用例使用合成的 19 位成员和项目真实阅读组件，无真实模型调用。

已只读连接用户酒馆，现场显示 SillyTavern 1.18.0、心迹回廊 0.8.111，与此候选不一致。因此 P0 的真机验收项仍未全部完成：候选在用户酒馆、iPhone、TT/Tauri、真实 19 人房间和同角色双聊天中的表现仍需更新后核验。代码测试通过不替代这些现场结果。
