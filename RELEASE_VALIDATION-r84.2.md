# Hearttrace r84.2 · 四季对白修复候选验证

日期：2026-09-15。版本：`0.8.80`。完整 build：`0.8.80-tt-cg-r84.2`。

## 基线与范围

本次基线为用户上传的 `Hearttrace-r84-full.zip`，叠加已经交付的 `Hearttrace-r84.1-HEART-persona-fallback-replace.zip`。两份原 ZIP 保留，未修改。没有从 GitHub main 引入代码，没有 GitHub 写入，没有真实模型调用。

原 r84 ZIP SHA-256：`7953669d4b81378c6e76c94662042dd4920b06ab531cf7e9b2cb96c658ef1279`。

原 r84.1 替换 ZIP SHA-256：`e483fea2bcd08a99d01768ba57af9b3b764c6e72375e9fa0dc0a7df7daf3120a`。

相对组合基线，生产逻辑仅修改 `src/core/dialogue.js`；`index.js` 只改 VERSION/BUILD，`manifest.json` 更新版本、入口缓存身份和说明，实际 runtime bundle 由源码重建。`src/modes/heart.js` 与 r84.1 逐字节相同，原人设 fallback 保留。增加定向测试和说明文件。

## 实际修复

1. 在共享归一化器中处理姓名标签、明确说话提示、带副词的“姓名……问/说”、与正确标记同项的代词说话插叙。支持同一项中多个具名人物的对白、冒号后的无引号对白、紧邻下一行的明确说话提示，以及下一旁白明确说明某人“说着/说完”的归属线索。仅有动作、性别代词或发言顺序不能证明另一行的说话人。
2. 已匹配当前传入档案姓名的 speakerName 优先纠正泛化的 char/user 枚举；解析后的本地姓名保留在脚本行，防止保存再渲染时再次换人。NPC 独立显示，不借用 char 头像。两个明确姓名互相矛盾时，新输出返回已有安全错误码；旧稿中性显示并保留固定的 unresolvedSpeaker 限制标记，重复显示仍不猜人。该标记只减少归属推断，不赋予权限。
3. 拟声词、内心所想和普通引文保留在原段落，不能因为出现引号就拆成对白。旧版已经拆成三条 narrator 的“脑子里 / 轰 / 的一声”类严格匹配碎片，只在显示时拼回；不增加词句或引号，不写入原缓存。
4. 新生成的 Voice/Scenario 保存前校验与旧剧本显示仍使用同一归一化入口。四季与未来、分段独立保存及只补缺失部分的生产路径未重写。

## 未修改与能力边界

- 原 Prompt 和 DIALOGUE_CONTRACT 不变；9 个四季/未来 Prompt 对照逐字相同，新增 Prompt 字符为 0。不为纠正显示新增模型调用，不更改恢复请求摘要或重试策略。
- 不修改正式档案 schema、Mxxx、历史证据门槛、archiveRevision、CAS、删除栅栏、只读保护、12MB 上限、独立备份、请求并发/超时/生命周期。
- 不修改 `heartView.js` 的头像来源、HTML 转义或主题 CSS；没有在普通启动/消息事件新增解析、缓存解压、世界书或 IndexedDB 工作。
- 没有取得截图对应剧本的原始结构化数据；本轮以同类结构的合成样例复现与验证，不能声称已逐条恢复截图中的原缓存。
- 旧稿若只剩错误的 `speaker=char` 与一句话，且姓名、标签、明确前后说话提示全部丢失，本地无法可靠确定归属；本版不会凭性别、轮次或台词语气强改成 user。已有身份线索的内容优先本地纠正，不要求全部重生成。

## 本次实际验证

| 检查 | 结果 |
| --- | --- |
| 组合基线现有测试 | 284/284 通过 |
| 本轮定向回归 | 51/51 通过 |
| 修改后完整测试 | 335/335 通过；0 失败/取消/跳过 |
| 全部 JS/MJS 语法 | 145/145 通过 |
| runtime | 97 模块，连续两次重建 SHA-256 一致；实际 bundle 可导入 |
| 解析属性组合检查 | 5,376 组合通过：重复归一化不改变结果、输入不变、行数/字符与角色集合有界 |
| 恢复回放 | 旧形状成功 checkpoint 经真实 Voice 校验器重新校验；模拟请求发送次数 0；checkpoint 原文不变 |
| 版本身份 | manifest VERSION/入口、index VERSION/BUILD、bundle 来源一致 |
| ZIP | 最终 ZIP 的 CRC、文件清单、逐文件 SHA-256 与重新解压测试见外部交付校验报告 |

以上 Node 检查使用本地固定数据或宿主/请求替身；没有替代用户环境的实机验证。定向测试还覆盖同名人物、姓名前缀歧义、具名 NPC、冻结旧缓存、HTML 注入字符串、超过 120 行/50400 字符的关闭式处理和长输入。

Bundle SHA-256：`b163fb9e87b80d739a49be673d74366fc6a7eed88c64c16f48228ad05f8822b7`。

Source SHA-256：`f669250ab0a7f69a3e70906250f82f59a4c3aac816759e705a5c94fda2dcdf55`。

Bundle：2,753,170 bytes；相对 r84.1 增加 9,443 bytes。没有新增依赖、网络端点、后台任务或轮询。

## 安全复核状态

已对本次差异做本地安全复核：角色只落入固定枚举；姓名仅匹配传入的当前档案身份；不根据模型数据选择 URL、CSS、缓存键或写入目标；输出继续经过原渲染器转义；所有扫描受既有脚本预算及新增有界语法提示限制；中性标记只能收紧显示归属。对比确认持久化、恢复、备份、并发与身份围栏文件逐字节未变。

本轮本地差异复核未发现新增 High/Medium 问题。Codex Security 专用工具本次检索未找到可用项，**未执行专用工具；独立回执未取得**。不把本地复核或测试冒充独立审计。

## 安装与真机确认

替换包包含 5 个运行文件：`index.js`、`manifest.json`、`dist/heartbeatMemories.bundle.js`、`src/core/dialogue.js`、`src/modes/heart.js`。最后一个文件用于保留 r84.1，不是把人设 fallback 回退掉。将替换包根目录内内容覆盖既有插件目录，不要创建第二份插件；不要只替换 src。

先等待在途任务结束、保存草稿，再完整刷新酒馆或重启客户端。诊断 declaredVersion 应显示 `0.8.80-tt-cg-r84.2`。无需清除站点数据、删除聊天或重建档案。

先打开已经生成的四季内容，确认有明确依据的 user 台词采用 user 姓名/头像/气泡，旁白和拟声词不再异常拆段；再确认新生成、保存、重开显示一致。完整包与替换包二选一，不必安装两遍。

**未执行：浏览器视觉、iPhone、TT/云酒馆/实际 SillyTavern 安装、真实模型及真实网络请求测试。**当前容器未找到可用浏览器测试依赖，因此 HTML 输出检查不能称为浏览器实测。
