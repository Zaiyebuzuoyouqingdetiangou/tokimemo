# 心迹回廊 r84.57 累计修复与复核

版本 `0.99.12 / 0.99.12-r84.57-reviewed`。远端施工基线为测试分支 `10ec1038c2d378fc744fc66d6f6d6562be897737`。本版累计 r84.56 本地修复；用户已明确授权复核通过后推送测试分支。旧包与旧记录保留。

## 本轮新增修正

- r84.53 F2：合并待补记录的显示、计数、导出、修复都按角色、聊天、档案版本和写入目标匹配。聊天名相同不再共享待补列表；缺身份的旧记录仅可保全导出，不补造归属。F5 提示“至少几个生成任务”，实际请求单独统计。
- 重新找回早期 P0 回归测试，修复两处被后续改动带回的通讯问题：已核验群像成员名不得改回卡名；没有真实用户名不能创建 `{{user}}` 联系人草稿。原有逐字证据、参与人物、旧 App 和拒绝伪造说话人的校验保留。
- 新来信插画使用 v2 受控结构：逐字核对角色外貌/标志物依据和信件里的场景依据。不同发型、发色、衣着、眼镜等由实际设定决定；不足则用有依据的相关物件或留白。模型不能传入 SVG、HTML、路径或执行代码，不调用生图 API。旧 v1 已保存插画兼容读取，不重绘旧信。单独收信、合并生成和冻结恢复采用同一原证据。
- 前世今生 ≤600px 使用单列阅读，正文 16px、1.9 行高，旁批排在正文后，线索与操作按钮重新流式布局，无模块内部滚动。≤360px 收窄留白；不改任何故事文本或探索进度。
- 新增“想和现在的 TA 聊天吗？”：对接镜译公开 beta 接口；真实人物选择、当前档案可选记忆、文字/语音输入、流式朗读、打断、挂断、保存本次文字、浏览器音频暂停后的继续播放。连接不自动开麦或生成；切聊/切档案/关闭/禁用清理，迟到结果不能继续发声或写到新角色。详见 `MIRROR-TTS.md`。
- 睡前故事和前世今生正文/旁批补入原朗读白名单。朗读与通话互相停止，正文外的设置、按钮、角色资料不会被“朗读本页”收进去。
- **失败写真九宫格按用户最新要求取消新建/绘图入口**。旧 photoshoots 字段、已有图及其保存兼容逻辑保留；不触发九个图片请求。
- 构建器统一 LF/CRLF 后计算源指纹，避免 Windows 与 GitHub 检出换行不同导致确定性构建结果漂移。

## 清单覆盖

以下是代码实施状态；现场验收另列，不把模拟接口成功说成实际服务已通过。

| 原编号/批次 | 已实施并纳入本轮回归的行为 |
|---|---|
| 01、16 / C1 | 实际人物名和显式用户身份；各页选人、随机与群像冻结到本次任务，续接保持原选择；沙盒卡名不代替人物。 |
| 02 | 通讯联系人、真实成员名、草稿与历史引用校验，已完成 App 保全。本轮补回 P0 回归并修成员名和空用户名。 |
| 08–10 / B1–B3 | 本地顶栏图标、更多菜单和任务胶囊、44px 触控入口、选择框与标题对齐、跨分组保留选择。 |
| 11–12 / B4–B5 | 内容页“生成与补全”、目录/顶栏/任务中心状态对齐，未保存不算完成，生成结束不抢走阅读页。 |
| 13–15 / C2–C3 | 房间人物与空间导航整理；人物按实际空间呈现，像素外形按已登记外貌区别；更新外形保留原房间。 |
| 28 / D1 | 独立睡前故事、故事架、阅读、下一章、冻结计划与中断恢复；不重写旧章节，新增页面接入朗读。 |
| 29 / D3 | 四季、ENDING、后日谈、基础语言图片设置、提示编辑与预览、公共生图接口、内容指纹校验回填和保存。 |
| 30 / D4 | 用户最新决定取消；保留旧存档兼容，不再开放新写真入口。 |
| 31 / D2 | 不再用通用恋爱独白填充缺失正文；保留隔屏情感玩法及已有安全结构。真实创作质量待模型样本验收。 |
| 32 / D5 | 选已有日期内容、图片与本次附言后系统打印；中文/换行/图片比例/转义保留，不触发 AI。系统 PDF 落盘仍待现场。 |
| 33 / D6 | 旧镜译朗读保留；新增公开 beta 实时通话，不读私有 Key、不重建第三方 TTS 系统。 |
| 34 | 显式确认同角色卡旧档案继承到空目标，保留原来源和已完成内容；不带入运行中任务；来源/目标变化拒绝覆盖。 |
| 35 / A1–A6 | 连接池可选轮询，任务内连接固定；印象曲配置保留；一份共同背景合并多页；任务子成果独立保全、冻结续接、保存-only与实际发送计量。 |

合并适配现为 **陈列柜、成就库、邮箱、角色印象曲、睡前故事**。附件明确允许其他复杂分阶段模块后续适配；没有把它们说成已经单请求合并，也没有改为并发旧函数冒充合并。群像人际庭园共用关系网，支持切换查看人物。

## 审计与测试口径

r84.53 F1–F7 与 r84.54 成果保全问题均按当前源码和实际入口测试复核：原任务身份、同页多批并存、先存已收到回复、补文成功后保存-only、占用异常回滚、完整最终输入估算、实际请求计数、存储失败保全、modules 类型错误、删除栅栏。独立复核后又恢复早期通讯/重试/镜译用例，防止仅运行新测试漏掉旧功能。

旧 `RMT_PHONE_NO_CONVERSATION` 测试由“所有重试均禁止”校正为当前合同的“无自动重试，用户可按原冻结配方手动修补”；不改生产恢复合同。旧 client 函数测试补齐新增连接选择、提示词组装和温度解析依赖，仍执行生产函数。旧新信 v1 插画生成样本改为证据合格的 v2；另验证 v1 已存信重开，未取消兼容性检查。

Prompt 变更：在本轮授权的人物、睡前故事、图片目标和邮箱小画要求内接通真实选择。新增通话自己的角色口吻/资料边界提示，不修改主聊天提示。插画只改小画合同和证据，不降低来信关系/历史事实校验。

本次全部工具测试 **143/143 通过**（0 失败/跳过/取消），**232 个 JS/MJS** 语法检查通过，**167 模块**确定性构建和 ESM 导入通过。最终构建指纹、包内文件清单/CRC/SHA 和鲜解压结果记录在随包外 `Hearttrace-r84.57-validation.json`。完整测试指此 checkout 可取得的全部工具测试，不冒充其他历史仓库未取得的全部测试。

浏览器证据为本地隔离样本：320/390 阅读排版、正文/操作尺寸、角色插画差异、模拟通话连接零请求、发送、识别、打断保留、切换和接口缺失提示。没有调用付费文本、生图、语音或麦克风。

## 未伪装成通过的现场项目

真实 SillyTavern/TT/Tauri/iPhone、真实 IndexedDB 配额边界、镜译 Fish 音频与麦克风权限、真实文本/生图供应商、系统 PDF 保存和角色创作质量仍需用户安装后的实际环境验收。旧记录中的首次半截正文与长请求超时没有新现场证据，不擅自关闭。做过独立人工式源码差异复核；未运行专用 Codex Security 扫描。

## 安装与回退

完整 ZIP 为单一 `tokimemo-main/` 根目录。覆盖当前心迹回廊插件目录的代码及 dist，与 manifest.json 同级，避免套两层目录或安装双份。不要删除聊天/图片/站点数据；旧内容不重新生成。刷新后确认 `0.99.12-r84.57-reviewed`。tools 为开发验证材料，运行时不依赖。保留上一版 ZIP 可回退代码；文档历史段落不能覆盖本节用户新决定。
